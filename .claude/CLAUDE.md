# Knowledge-Hub — Project Intelligence

> Agent: Read this file FIRST every session. Then load `.claude/memory/HOT.md` only.
> Do NOT load WARM/ or src/ until a specific task is assigned.

---

## Project
Brysen Group internal AI knowledge platform.
Languages: Japanese, English, Vietnamese, Korean + Asia.
Consumers: Web SPA, Teams bot, Slack bot (all via API).

## Tech Stack (non-negotiable)
- Backend: FastAPI (Python) — `/v1/query` `/v1/documents` `/v1/health` `/v1/metrics`
- RAG: Hybrid search (multilingual dense embeddings + BM25) + RBAC hard-filter + conflict detection
- Storage: PostgreSQL + pgvector (embeddings) + CJK-aware full-text search
- Auth: API-key (bots/service accounts) + OIDC/SSO Bearer (human users)
- Frontend: React/Vite SPA (thin client)

---

## ⚡ TOKEN BUDGET — STRICT

### Per-command context allowance
| Command        | Load these files                          | Hard limit | Model  |
|----------------|-------------------------------------------|------------|--------|
| /constitution  | CLAUDE.md only                            | 2k tokens  | sonnet |
| /specify       | CLAUDE.md + CONSTITUTION.md               | 3k tokens  | sonnet |
| /clarify       | spec file + WARM/<feature>                | 3k tokens  | sonnet |
| /checklist     | spec + sources + WARM/<feature> + HARD.md | 3k tokens  | haiku  |
| /plan          | spec + HOT.md + rules/ARCH.md             | 4k tokens  | sonnet |
| /tasks         | plan file + WARM/<feature>                | 3k tokens  | sonnet |
| /analyze       | task file + target src files (lines only) | 5k tokens  | sonnet |
| /implement     | task + analysis + WARM/<feature>          | 6k tokens  | sonnet |
| /reviewcode    | task definition + diff only               | 3k tokens  | opus   |
| /report        | spec + plans + tasks + WARM summary       | 4k tokens  | haiku  |
| /sync          | conversation summary (not full history)   | 2k tokens  | haiku  |
| /context       | HOT.md + requested WARM file              | 2k tokens  | haiku  |
| /rules         | rules/ dir + task scope                   | 2k tokens  | haiku  |

### PROMPT CACHE RULES (Policy v1)
- Prefix must be deterministic for the same command/task class.
- Keep stable block order and file load order fixed by command flow.
- Put volatile data (timestamp, random id, fresh diff, ad-hoc user notes) in suffix only.
- Route A (stable prefix) is default for all slash-command/subagent workflows.
- Route B (`cache_control`) is optional and only for direct Anthropic API integrations.
- Detailed playbook: `.claude/PROMPT_CACHING.md`.

### Compression triggers (agent auto-runs /sync when)
- Conversation exceeds 15 turns
- Switching features mid-session
- Before dispatching any subagent
- After completing a story

### Response format rules
- Lead with 3-line summary ALWAYS
- Expand only when user asks "detail" or "explain"
- Code: show signatures + key logic — never dump full file
- Lists over 7 items → summarize into groups

---

## 🧠 MEMORY LAYERS

### HOT — Load every session
`.claude/memory/HOT.md`
Sprint goal, in-progress stories (max 3), blockers, last 3 decisions.

### WARM — Load per feature
`.claude/memory/WARM/<feature>.mem.md`
Spec summary, decisions, files touched, open questions, status.
Create at /specify. Update after each phase.

### COLD — Never auto-load
`.claude/memory/COLD/<feature>.archive.md`
Completed features. Load only if explicitly referenced.

### Project-level (permanent)
`CLAUDE.md` — this file. Tech stack, rules, budgets.
`AGENTS.md` — subagent registry and orchestration protocol.
`CONSTITUTION.md` — generated by /constitution. Core principles.

---

## 📁 Directory Map
```
backend/
  api/        — FastAPI routes (api-agent scope)
  rag/        — Retriever, embeddings, BM25 (rag-agent scope)
  auth/       — OIDC, API-key, RBAC middleware (auth-agent scope)
  db/         — Models, migrations (db-agent scope)
  bots/       — Teams/Slack adapters (bot-agent scope)
frontend/     — React/Vite SPA (frontend-agent scope)
docs/
  specs/      — .spec.md files (Layers 1–3, output of /specify)
  sources/    — .sources.md files (AC traceability, output of /specify)
  clarify/    — .clarify.md files (Q&A gate, output of /clarify)
  plans/      — .plan.md files (output of /plan)
  tasks/      — .tasks.md files (output of /tasks)
  reviews/    — .checklist.md files (pre-plan gate, output of /checklist)
  reports/    — .report.md files (final summary, output of /report)
.claude/
  memory/     — HOT.md, WARM/<feature>.mem.md, COLD/<feature>.archive.md
  commands/   — slash command definitions
  rules/      — HARD.md, ARCH.md, SECURITY.md, PERF.md
  templates/
    spec/           — _template.spec.md (Layers 1–3)
    sources/        — _template.sources.md (AC traceability)
    checklist/      — _template.checklist.md (pre-plan validation)
    report/         — _template.report.md (final summary)
    constitution/   — _template.constitution.md
    plan/           — _template.plan.md
    tasks/          — _template.tasks.md
```

---

## 🔗 SDD Flow
```
/constitution → /specify → /clarify → /checklist
     → /plan → /tasks → /analyze → /implement → /reviewcode
          ↓
     → /report
```
Always spec-first. Never implement without a task file. Always finalize with `/report`.
