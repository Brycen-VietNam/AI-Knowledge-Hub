# Copy to .env and fill in values
# Never commit .env to git

# PostgreSQL (asyncpg driver — D03)
DATABASE_URL=postgresql+asyncpg://kh_user:kh_dev_password@localhost:5432/knowledge_hub

# Valkey / Redis (C016)
REDIS_URL=redis://localhost:6379/0

# LLM Provider (C015)
LLM_PROVIDER=ollama
