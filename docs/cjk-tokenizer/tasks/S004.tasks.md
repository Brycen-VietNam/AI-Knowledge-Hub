# Tasks: cjk-tokenizer / S004 — Tokenizer tests (unit + performance)
Created: 2026-04-06 | Agent: rag-agent | Status: TODO

---

## LAYER 1 — Story Task Summary

| Field | Value |
|---|---|
| Story | S004: Tokenizer tests — unit + performance |
| Total tasks | 2 |
| Parallel groups | G1: [T001], G2: [T002 after:T001] |
| Critical path | T001 → T002 |
| Agent | rag-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|---|---|---|---|---|
| T001 | Regression test: no whitespace for CJK | DONE | safe | T002 |
| T002 | Performance tests (pytest.mark.performance) | DONE | after:T001 | — |

> Note: Golden examples and unit tests are written in-task alongside each S001–S003 implementation (TDD).
> S004 adds the two test types that span all stories: regression guard + performance.

---

## LAYER 2 — Task Detail

### T001: Regression test — WhitespaceTokenizer not returned for CJK langs

**Status**: DONE

**File(s)**
- `tests/rag/test_tokenizers.py` — modify (add `TestCJKWhitespaceRegression`)

**Change description**
Add parametrized test asserting `TokenizerFactory.get(lang)` never returns `WhitespaceTokenizer`
for `"ja"`, `"ko"`, `"zh"`, `"vi"`. Guards against future Factory regressions (HARD R005).

**Review criteria**
- [ ] `@pytest.mark.parametrize("lang", ["ja", "ko", "zh", "vi"])`
- [ ] `assert not isinstance(TokenizerFactory.get(lang), WhitespaceTokenizer)`
- [ ] Test name: `test_whitespace_not_used_for_cjk`
- [ ] Covers AC4 (S004)

**Test command**
```bash
pytest tests/rag/test_tokenizers.py::TestCJKWhitespaceRegression -v
```

**Parallel**: safe (parallel with S003 tasks)
**Size estimate**: ~15 lines

---

### T002: Performance tests (pytest.mark.performance)

**Status**: DONE

**File(s)**
- `tests/rag/test_tokenizers.py` — modify (add `TestTokenizerPerformance`)
- `pytest.ini` — modify (register `performance` marker if not already registered)

**Change description**
Add `@pytest.mark.performance` tests measuring `tokenize()` wall-time for 200-char input
per language (ja/ko/zh/vi). Assert each < 200ms. Use `time.perf_counter()`.
TDD: write timing assertions first, then verify backends pass.

**Review criteria**
- [ ] `@pytest.mark.performance` decorator on each test
- [ ] 200-char sample text per language (realistic content, not repeated chars)
- [ ] Timing via `time.perf_counter()` — wall time, not CPU time
- [ ] Assert `elapsed < 0.2` (200ms) per language
- [ ] `pytest.ini` has `markers = performance: marks performance tests` registered
- [ ] Covers AC6 (S004)

**Test command**
```bash
pytest tests/rag/test_tokenizers.py::TestTokenizerPerformance -v -m performance
```

**Parallel**: after:T001
**Size estimate**: ~35 lines
