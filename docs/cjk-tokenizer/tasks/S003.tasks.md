# Tasks: cjk-tokenizer / S003 — Language auto-detection
Created: 2026-04-06 | Agent: rag-agent | Status: TODO

---

## LAYER 1 — Story Task Summary

| Field | Value |
|---|---|
| Story | S003: Language auto-detection at tokenization entry |
| Total tasks | 2 |
| Parallel groups | G1: [T001], G2: [T002 after:T001] |
| Critical path | T001 → T002 |
| Agent | rag-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|---|---|---|---|---|
| T001 | detect_language() implementation + tests | DONE | safe | T002 |
| T002 | Export detect_language from __init__.py | DONE | after:T001 | — |

---

## LAYER 2 — Task Detail

### T001: detect_language() + tests

**Status**: DONE

**File(s)**
- `backend/rag/tokenizers/detection.py` — create
- `tests/rag/test_tokenizers.py` — modify (add `TestDetectLanguage`)

**Change description**
Implement `detect_language(text: str) -> str`.
Guard order: empty string → error, `len < 8` → error, `len > 512` → truncate, then detect.
Use `langdetect.detect_langs()` (returns probability list) — take top result, check confidence ≥ 0.85.
Map supported langs (`ja`, `ko`, `zh-cn`→`zh`, `vi`, `en`) or unknown-with-confidence → `"en"`.
TDD: write all `TestDetectLanguage` cases first (7 cases), then implement.

**Review criteria**
- [ ] Empty string → `LanguageDetectionError` (AC6)
- [ ] `len(text) < 8` → `LanguageDetectionError` immediately, no detect call (D06)
- [ ] `len(text) > 512` → truncated to `text[:512]` before detection (AC5 / SECURITY S003)
- [ ] `LangDetectException` caught → re-raised as `LanguageDetectionError`
- [ ] confidence < 0.85 → `LanguageDetectionError` (AC3)
- [ ] Unsupported lang detected (e.g. `"fr"`) with confidence ≥ 0.85 → returns `"en"` (AC2)
- [ ] `langdetect.detect_langs()` used (not `detect()`) — to read confidence value
- [ ] `zh-cn` / `zh-tw` mapped to `"zh"` (jieba handles both)
- [ ] Function does NOT call TokenizerFactory — detection only

**Test command**
```bash
pytest tests/rag/test_tokenizers.py::TestDetectLanguage -v
```

**Parallel**: safe (parallel with S004 tasks — different file)
**Size estimate**: ~40 lines

---

### T002: Export detect_language from __init__.py

**Status**: DONE

**File(s)**
- `backend/rag/tokenizers/__init__.py` — modify
- `tests/rag/test_tokenizers.py` — modify (extend import smoke test)

**Change description**
Add `detect_language` to `__init__.py` exports and `__all__`.

**Review criteria**
- [ ] `detect_language` in `__all__`
- [ ] `from backend.rag.tokenizers import detect_language` works

**Test command**
```bash
pytest tests/rag/test_tokenizers.py::test_public_api_exports -v
```

**Parallel**: after:T001
**Size estimate**: ~3 lines
