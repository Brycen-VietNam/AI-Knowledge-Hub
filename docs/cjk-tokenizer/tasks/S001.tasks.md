# Tasks: cjk-tokenizer / S001 — Per-language tokenizer backends
Created: 2026-04-06 | Agent: rag-agent | Status: TODO

---

## LAYER 1 — Story Task Summary

| Field | Value |
|---|---|
| Story | S001: Per-language tokenizer backends |
| Total tasks | 5 |
| Parallel groups | G1: [T001], G2: [T002,T003,T004,T005 after:T001] |
| Critical path | T001 → T002 → T003 → T004 → T005 |
| Agent | rag-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|---|---|---|---|---|
| T001 | BaseTokenizer ABC + exceptions skeleton | DONE | safe | T002,T003,T004,T005 |
| T002 | JapaneseTokenizer (MeCab) + test | DONE | after:T001 | — |
| T003 | KoreanTokenizer (kiwipiepy) + test | DONE | after:T001 | — |
| T004 | ChineseTokenizer (jieba) + VietnameseTokenizer (underthesea) + WhitespaceTokenizer + test | DONE | after:T001 | — |
| T005 | Dockerfile MeCab install | DONE | after:T002 | — |

---

## LAYER 2 — Task Detail

### T001: BaseTokenizer ABC + exceptions skeleton

**Status**: DONE

**File(s)**
- `backend/rag/tokenizers/__init__.py` — create (empty init, exports added later)
- `backend/rag/tokenizers/base.py` — create
- `backend/rag/tokenizers/exceptions.py` — create
- `tests/rag/test_tokenizers.py` — create (import smoke test only)

**Change description**
Define `BaseTokenizer` as an ABC with single abstract method `tokenize(text: str) -> list[str]`.
Define `UnsupportedLanguageError` and `LanguageDetectionError` as distinct exception classes.
Create empty `__init__.py`. Create test file with import smoke test.

**Review criteria**
- [ ] `BaseTokenizer` uses `abc.ABC` + `@abstractmethod`
- [ ] `tokenize` signature: `(self, text: str) -> list[str]`
- [ ] `UnsupportedLanguageError(ValueError)` — subclasses ValueError for easy catching
- [ ] `LanguageDetectionError(RuntimeError)` — distinct from UnsupportedLanguageError
- [ ] No business logic in this task — scaffold only

**Test command**
```bash
pytest tests/rag/test_tokenizers.py::test_imports -v
```

**Parallel**: safe
**Size estimate**: ~25 lines

---

### T002: JapaneseTokenizer (MeCab) + test

**Status**: DONE

**File(s)**
- `backend/rag/tokenizers/japanese.py` — create
- `tests/rag/test_tokenizers.py` — modify (add `TestJapaneseTokenizer`)

**Change description**
Implement `JapaneseTokenizer(BaseTokenizer)` using `MeCab.Tagger()`.
Parse MeCab output to extract surface forms (`node.surface`).
Filter empty surfaces. Return `list[str]`.
TDD: write `TestJapaneseTokenizer` with 3 golden cases first, then implement.

**Review criteria**
- [ ] Uses `MeCab.Tagger("-Owakati")` or equivalent surface-form output
- [ ] Empty string → returns `[]` (AC8)
- [ ] Golden test 1: `"東京都に住んでいます"` → list of surface tokens
- [ ] Golden test 2: `"AI knowledge hub"` (mixed CJK+ASCII) → tokens include ASCII words
- [ ] Golden test 3: `"会議"` (2 chars) → returns non-empty list

**Test command**
```bash
pytest tests/rag/test_tokenizers.py::TestJapaneseTokenizer -v
```

**Parallel**: after:T001
**Size estimate**: ~35 lines

---

### T003: KoreanTokenizer (kiwipiepy) + ChineseTokenizer (jieba) + VietnameseTokenizer (underthesea) + WhitespaceTokenizer + tests

**Status**: DONE

**File(s)**
- `backend/rag/tokenizers/korean.py` — create
- `backend/rag/tokenizers/chinese.py` — create
- `backend/rag/tokenizers/vietnamese.py` — create
- `backend/rag/tokenizers/whitespace.py` — create
- `tests/rag/test_tokenizers.py` — modify (add 4 test classes)

**Change description**
`KoreanTokenizer`: `Kiwi().tokenize(text)` → extract `.form` from each token tuple → `list[str]`. All morphemes returned (no POS filter).
`ChineseTokenizer`: `jieba.cut(text)` → filter whitespace → `list[str]`.
`VietnameseTokenizer`: `underthesea.word_tokenize(text)` → `list[str]`.
`WhitespaceTokenizer`: `text.split()` — en only, no CJK.
All: empty string → `[]`.
TDD: golden tests first for each.

**Review criteria**
- [ ] Korean: returns `form` strings only, not full kiwipiepy tuples
- [ ] Chinese: `jieba.cut()` strips whitespace tokens from output
- [ ] Vietnamese: uses `word_tokenize`, NOT `tokenize`
- [ ] WhitespaceTokenizer: `text.split()` — no strip/lower — preserve original form
- [ ] All 4: empty string → `[]` (AC8)
- [ ] Golden test per language: ≥2 cases (short + long)

**Test command**
```bash
pytest tests/rag/test_tokenizers.py::TestKoreanTokenizer tests/rag/test_tokenizers.py::TestChineseTokenizer tests/rag/test_tokenizers.py::TestVietnameseTokenizer tests/rag/test_tokenizers.py::TestWhitespaceTokenizer -v
```

**Parallel**: after:T001
**Size estimate**: ~60 lines (4 small files, ~15 lines each)

---

### T004: Update __init__.py exports

**Status**: DONE

**File(s)**
- `backend/rag/tokenizers/__init__.py` — modify (add exports)
- `tests/rag/test_tokenizers.py` — modify (test public API imports)

**Change description**
Export all public symbols from `__init__.py`:
`BaseTokenizer`, `JapaneseTokenizer`, `KoreanTokenizer`, `ChineseTokenizer`, `VietnameseTokenizer`, `WhitespaceTokenizer`, `UnsupportedLanguageError`, `LanguageDetectionError`.
Add test asserting all exports importable from package root.

**Review criteria**
- [ ] `from backend.rag.tokenizers import JapaneseTokenizer` works
- [ ] No circular import (exceptions.py imported by base.py is fine; base.py not importing backends)
- [ ] `__all__` defined explicitly

**Test command**
```bash
pytest tests/rag/test_tokenizers.py::test_public_api_exports -v
```

**Parallel**: after:T002,T003
**Size estimate**: ~20 lines

---

### T005: Dockerfile — MeCab system install

**Status**: DONE

**File(s)**
- `Dockerfile` — modify (add MeCab apt install layer)

**Change description**
Add apt-get install layer for MeCab before Python package installs:
```dockerfile
RUN apt-get update && apt-get install -y --no-install-recommends \
    mecab libmecab-dev mecab-ipadic-utf8 \
 && rm -rf /var/lib/apt/lists/*
```
Place before `pip install` layer to maximize Docker layer caching.

**Review criteria**
- [ ] `--no-install-recommends` to minimize image size
- [ ] `rm -rf /var/lib/apt/lists/*` in same RUN layer (cache cleanup)
- [ ] Positioned before Python `pip install` step
- [ ] Does not break existing layers above it
- [ ] DevOps review required before merge (WARN-01)

**Test command**
```bash
pytest tests/rag/test_tokenizers.py::TestJapaneseTokenizer -v
```
_(Verifies MeCab is importable inside container — run in Docker build CI)_

**Parallel**: after:T002
**Size estimate**: ~5 lines
