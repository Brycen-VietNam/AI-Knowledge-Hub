# Tasks: cjk-tokenizer / S002 — TokenizerFactory with lazy loading
Created: 2026-04-06 | Agent: rag-agent | Status: TODO

---

## LAYER 1 — Story Task Summary

| Field | Value |
|---|---|
| Story | S002: TokenizerFactory with lazy loading |
| Total tasks | 2 |
| Parallel groups | G1: [T001], G2: [T002 after:T001] |
| Critical path | T001 → T002 |
| Agent | rag-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|---|---|---|---|---|
| T001 | TokenizerFactory class + tests | DONE | safe | T002 |
| T002 | Export Factory from __init__.py | DONE | after:T001 | — |

---

## LAYER 2 — Task Detail

### T001: TokenizerFactory class + tests

**Status**: DONE

**File(s)**
- `backend/rag/tokenizers/factory.py` — create
- `tests/rag/test_tokenizers.py` — modify (add `TestTokenizerFactory`)

**Change description**
Implement `TokenizerFactory` with class-level `_registry: dict[str, BaseTokenizer]` and `threading.Lock`.
`get(lang)` checks registry first (singleton), then lazy-imports and instantiates on miss.
Unknown lang raises `UnsupportedLanguageError`.
TDD: write `TestTokenizerFactory` tests first (all 5 langs + error case + singleton identity check), then implement.

**Review criteria**
- [ ] `get("ja")` → `JapaneseTokenizer` instance (AC1)
- [ ] `get("ko")` → `KoreanTokenizer` instance (AC2)
- [ ] `get("zh")` → `ChineseTokenizer` instance (AC3)
- [ ] `get("vi")` → `VietnameseTokenizer` instance (AC4)
- [ ] `get("en")` → `WhitespaceTokenizer` instance (AC5)
- [ ] `get("xx")` → raises `UnsupportedLanguageError` (AC6)
- [ ] Two calls to `get("ja")` return the **same object** (singleton identity: `is`, not `==`) (AC7 lazy + singleton)
- [ ] Import of `japanese.py` happens inside `get()`, not at module import time (AC7)
- [ ] `threading.Lock` wraps registry write (AC8)

**Test command**
```bash
pytest tests/rag/test_tokenizers.py::TestTokenizerFactory -v
```

**Parallel**: safe (depends on S001 files being present)
**Size estimate**: ~40 lines

---

### T002: Export Factory from __init__.py

**Status**: DONE

**File(s)**
- `backend/rag/tokenizers/__init__.py` — modify
- `tests/rag/test_tokenizers.py` — modify (add import test for Factory)

**Change description**
Add `TokenizerFactory` to `__init__.py` exports and `__all__`.
Add smoke test asserting `from backend.rag.tokenizers import TokenizerFactory` works.

**Review criteria**
- [ ] `TokenizerFactory` in `__all__`
- [ ] No circular import introduced

**Test command**
```bash
pytest tests/rag/test_tokenizers.py::test_public_api_exports -v
```

**Parallel**: after:T001
**Size estimate**: ~5 lines
