# Analysis: S002/T002 — Update embedding.py with Vector(1024) column + test
Depth: shallow | Date: 2026-03-18

---

## Code Map

### backend/db/models/embedding.py (TOUCH — modify)
```
imports: uuid, datetime, ForeignKey, String, func, Mapped, mapped_column, Base
class Embedding(Base):
  __tablename__ = "embeddings"
  id: Mapped[uuid.UUID]       — UUID PK
  doc_id: Mapped[uuid.UUID]   — FK → documents.id ON DELETE CASCADE
  chunk_index: Mapped[int]
  lang: Mapped[str]           — String(2)
  user_group_id: Mapped[int]  — denormalized, no FK (R001)
  created_at: Mapped[datetime]
  # comment L23: embedding Vector(1024) added in S002/T002  ← replace with actual column
```
Change needed: add `embedding: Mapped[...] = mapped_column(Vector(1024), nullable=True)`
New import: `from pgvector.sqlalchemy import Vector`

### tests/db/test_models.py (TOUCH — modify)
```
engine fixture: SQLite in-memory, Base.metadata.create_all()
14 tests currently — all pass

Existing relevant test (L52-56):
  test_embeddings_has_no_vector_column(engine)
    → asserts "embedding" NOT in cols   ← ❌ CONFLICT — must invert after S002
```

---

## ❌ CONFLICT — Test inversion required

`test_embeddings_has_no_vector_column` (L52) asserts `"embedding" NOT in cols`.
After adding `Vector(1024)` to the ORM model, this test will **FAIL**.

**Fix required (TDD order):**
1. Rename existing test → `test_embeddings_has_no_vector_column` becomes obsolete
2. Replace with `test_embeddings_has_vector_column` asserting `"embedding" IN cols`

However: SQLite does not support `vector` type natively. `pgvector.sqlalchemy.Vector`
registers as a custom type. With SQLite in-memory engine:
- `Base.metadata.create_all()` will create the column but SQLAlchemy maps Vector → unknown type
- `inspect(engine).get_columns("embeddings")` will show the column name but type may differ

**Solution**: test column *presence* (name check), not type. Type is enforced by migration 002
(already applied on Docker DB and reviewed). The ORM test only needs to verify the column exists.

---

## Patterns to Follow

### pgvector SQLAlchemy 2.0 pattern
```python
from pgvector.sqlalchemy import Vector
from sqlalchemy.orm import Mapped, mapped_column

class Embedding(Base):
    embedding: Mapped[list[float]] = mapped_column(Vector(1024), nullable=True)
```
`Mapped[list[float]]` — correct type hint for Vector in SQLAlchemy 2.0.
`nullable=True` — intentional: rag-agent populates after ingestion.

### Header comment update
Replace: `# embedding Vector(1024) column added in S002/T002 after pgvector extension`
With: actual column definition + updated task comment.

---

## Gaps Found

⚠️  `test_embeddings_has_no_vector_column` will FAIL after this change — must be replaced
    by `test_embeddings_has_vector_column` in same edit (TDD: write new test first)

⚠️  SQLite in-memory does not natively support vector type — test checks column *name* only,
    not column *type*. Type correctness guaranteed by migration 002 (already REVIEWED).

✅  No security violations — pure ORM model addition, no SQL interpolation
✅  A001 satisfied: `pgvector.sqlalchemy` is allowed in db-agent scope
✅  `pgvector==0.3.6` already in requirements.txt — no new dependency needed

---

## Recommended Implementation Order (TDD)

1. **Modify test first** (failing state):
   - Replace `test_embeddings_has_no_vector_column` → `test_embeddings_has_vector_column`
   - New assertion: `assert "embedding" in cols`
   - Run pytest → FAIL (column not yet in model)

2. **Modify embedding.py**:
   - Add `from pgvector.sqlalchemy import Vector`
   - Replace comment L23 with actual column: `embedding: Mapped[list[float]] = mapped_column(Vector(1024), nullable=True)`
   - Update header comment (task ref → S002/T002)

3. **Run pytest** → 14 tests pass (renamed test now passes)

---

## Token Budget
Estimated implementation: ~15 lines total (5 embedding.py + 8 test + 2 import)
Analysis saved: docs/tasks/db-schema-embeddings/S002-T002.analysis.md
Next: /implement db-schema-embeddings --story S002 --task T002
