# Tasks: document-ingestion / S004 — BM25 index update (CJK-aware)
Created: 2026-04-07 | Agent: rag-agent | Status: TODO

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S004: BM25 index update (CJK-aware) |
| Total tasks | 3 |
| Parallel groups | G1: [T001], G2: [T002 after:T001], G3: [T003 after:T002] |
| Critical path | T001 → T002 → T003 |
| Agent | rag-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | bm25_indexer scaffold + tokenize_for_fts() + test | DONE | safe | T002 |
| T002 | FTS UPDATE query + unknown lang fallback + test | DONE | after:T001 | T003 |
| T003 | status=ready update + pipeline wire-up + test | DONE | after:T002 | — |

---

## LAYER 2 — Task Detail

### T001: bm25_indexer scaffold + tokenize_for_fts()

**Status**: TODO

**File(s)**
- `backend/rag/bm25_indexer.py` — action: create
- `tests/rag/test_bm25_indexer.py` — action: create

**Change description**
Create `bm25_indexer.py` (new file — D08, not a cjk-tokenizer leftover).
Add `tokenize_for_fts(text: str, lang: str) -> str` that:
- Calls `TokenizerFactory.get(lang).tokenize(text)` for CJK (ja/ko/zh/vi)
- Falls back to `text.split()` for latin/other
- Catches `UnsupportedLanguageError` → log warning, return `text` (AC3 fallback)
- Returns `" ".join(tokens)` — space-separated for PostgreSQL `to_tsvector`
TDD: write tokenization tests before code.

**Review criteria**
- [ ] `from backend.rag.tokenizers.factory import TokenizerFactory, UnsupportedLanguageError`
- [ ] CJK langs (ja/ko/zh/vi) → `TokenizerFactory.get(lang)` (C005, C006, R005)
- [ ] `UnsupportedLanguageError` caught → warning logged, fallback to `text.split()`
- [ ] Test: lang="ja" → MeCab tokenizer called (mock TokenizerFactory)
- [ ] Test: lang="en" → whitespace split
- [ ] Test: lang="xx" (unsupported) → warning logged, no exception raised
- [ ] Rule satisfied: R005, C005, C006

**Test command**
```bash
pytest tests/rag/test_bm25_indexer.py::test_tokenize_for_fts -v
```

**Parallel**: safe
**Size estimate**: ~25 lines

---
<!-- End T001 -->

### T002: content_fts UPDATE query

**Status**: TODO

**File(s)**
- `backend/rag/bm25_indexer.py` — action: modify (add `update_fts` function)
- `tests/rag/test_bm25_indexer.py` — action: modify (add FTS update tests)

**Change description**
Add `async update_fts(doc_id: uuid, chunks: list[Chunk], db: AsyncSession)`.
Joins all chunk tokens into single string, runs:
`UPDATE documents SET content_fts = to_tsvector('simple', :tokens) WHERE id = :doc_id`
Uses SQLAlchemy `text()` with named params (no string interpolation — S001).

**Review criteria**
- [ ] Single UPDATE statement — not per-chunk (P004)
- [ ] Uses `text(...).bindparams(tokens=..., doc_id=...)` — no f-string interpolation (S001)
- [ ] `to_tsvector('simple', :tokens)` — `simple` config matches retriever's `to_tsquery('simple', ...)` pattern
- [ ] `await db.execute(...)` + `await db.commit()`
- [ ] Test: correct SQL executed (mock db, assert call args)
- [ ] Test: multi-chunk doc → all tokens joined in single UPDATE
- [ ] Rule satisfied: S001 (SQL injection), P004 (no N+1)

**Test command**
```bash
pytest tests/rag/test_bm25_indexer.py::test_update_fts -v
```

**Parallel**: after:T001
**Size estimate**: ~20 lines

---
<!-- End T002 -->

### T003: status=ready update + pipeline wire-up

**Status**: TODO

**File(s)**
- `backend/rag/bm25_indexer.py` — action: modify (add status update)
- `backend/api/routes/documents.py` — action: modify (wire bm25_indexer into `ingest_pipeline` after embedder commit)
- `tests/rag/test_bm25_indexer.py` — action: modify (add status=ready test)

**Change description**
After `await db.commit()` in `update_fts`, run:
`UPDATE documents SET status = 'ready' WHERE id = :doc_id`
Wire into `ingest_pipeline`: after `insert_embeddings` commits → call `update_fts` → `status='ready'`.
Ordering guarantee: BM25 update only runs after embeddings committed (AC4).

**Review criteria**
- [ ] `status = 'ready'` set in same function as FTS update (after commit)
- [ ] `ingest_pipeline` order: chunk → embed+insert (commit) → update_fts (commit+ready)
- [ ] Test: after `update_fts` completes → doc.status="ready"
- [ ] Test: if `update_fts` called before embed commit → test ordering (mock sequence)
- [ ] Test: full pipeline integration — doc starts "processing", ends "ready"
- [ ] Rule satisfied: S004 AC4 (ordering), P005 (fail visibly)

**Test command**
```bash
pytest tests/rag/test_bm25_indexer.py::test_status_ready_after_fts tests/rag/test_bm25_indexer.py::test_pipeline_ordering -v
```

**Parallel**: after:T002
**Size estimate**: ~10 lines bm25_indexer + ~10 lines documents.py + ~25 lines tests

---
<!-- End T003 -->
