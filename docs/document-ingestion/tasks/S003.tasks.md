# Tasks: document-ingestion / S003 — Batch embedding generation
Created: 2026-04-07 | Agent: rag-agent | Status: TODO

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S003: Batch embedding generation |
| Total tasks | 3 |
| Parallel groups | G1: [T001], G2: [T002 after:T001], G3: [T003 after:T002] |
| Critical path | T001 → T002 → T003 |
| Agent | rag-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | OllamaEmbedder class + EmbedderError + env config + test | DONE | safe | T002 |
| T002 | batch_embed() — batching + asyncio.gather + test | DONE | after:T001 | T003 |
| T003 | Embedding DB insert + failure → status=failed + pipeline wire-up + test | DONE | after:T002 | — |

---

## LAYER 2 — Task Detail

### T001: OllamaEmbedder class + EmbedderError + env config

**Status**: TODO

**File(s)**
- `backend/rag/embedder.py` — action: create
- `tests/rag/test_embedder.py` — action: create

**Change description**
Create `OllamaEmbedder` with `OLLAMA_BASE_URL` and `EMBEDDING_MODEL` from env vars (D10).
Define `EmbedderError` exception. Add `async _embed_one(text: str) -> list[float]` that POSTs to `{OLLAMA_BASE_URL}/api/embeddings` with `{"model": EMBEDDING_MODEL, "prompt": text}`.
TDD: mock `httpx.AsyncClient` and test the HTTP call shape.

**Review criteria**
- [ ] `OLLAMA_BASE_URL = os.getenv("OLLAMA_BASE_URL", "http://localhost:11434")`
- [ ] `EMBEDDING_MODEL = os.getenv("EMBEDDING_MODEL", "mxbai-embed-large")`
- [ ] Uses `httpx.AsyncClient` (async HTTP — consistent with llm-provider pattern)
- [ ] `EmbedderError` raised on non-200 HTTP response
- [ ] Test: mock returns `{"embedding": [0.1, ...]}` → `_embed_one` returns list[float]
- [ ] Test: mock returns 500 → `EmbedderError` raised
- [ ] No hardcoded model name or URL (S005 SECURITY.md)
- [ ] Rule satisfied: D10, C015 (configurable via env)

**Test command**
```bash
pytest tests/rag/test_embedder.py::test_embed_one -v
```

**Parallel**: safe
**Size estimate**: ~30 lines

---
<!-- End T001 -->

### T002: batch_embed() — batching + asyncio.gather

**Status**: TODO

**File(s)**
- `backend/rag/embedder.py` — action: modify (add `batch_embed`)
- `tests/rag/test_embedder.py` — action: modify (add batch tests)

**Change description**
Implement `async batch_embed(chunks: list[Chunk], batch_size: int = 32) -> list[list[float]]`.
Split chunks into batches of `batch_size`. For each batch call `asyncio.gather(*[_embed_one(c.text) for c in batch])`.
Maintains chunk order in output (zip with input).

**Review criteria**
- [ ] `batch_size` defaults to 32 (C012: never < 32)
- [ ] Uses `asyncio.gather` for concurrent calls within each batch (P002)
- [ ] Output list same length and order as input `chunks`
- [ ] Test: 10 chunks, batch_size=32 → single gather call (1 batch)
- [ ] Test: 70 chunks, batch_size=32 → 3 batches (32+32+6)
- [ ] Test: order preserved — chunk at index N maps to embedding at index N
- [ ] No per-chunk sequential loop (C012)
- [ ] Rule satisfied: C012, P002

**Test command**
```bash
pytest tests/rag/test_embedder.py::test_batch_embed -v
```

**Parallel**: after:T001
**Size estimate**: ~20 lines

---
<!-- End T002 -->

### T003: Embedding DB insert + failure path + pipeline wire-up

**Status**: TODO

**File(s)**
- `backend/rag/embedder.py` — action: modify (add `insert_embeddings`)
- `backend/api/routes/documents.py` — action: modify (wire embedder into `ingest_pipeline`)
- `tests/rag/test_embedder.py` — action: modify (add DB insert + failure tests)

**Change description**
Add `async insert_embeddings(chunks, vectors, doc, db)` that bulk-inserts `Embedding` rows.
`user_group_id` copied from `doc.user_group_id` (never from request — R002).
`text` copied from `chunk.text` — stores chunk text for RAG retrieval (D11).
Wire into `ingest_pipeline`: call `batch_embed` → `insert_embeddings` → on `EmbedderError` set `doc.status = "failed"` and log (no partial insert).

**Review criteria**
- [ ] `Embedding.user_group_id` set from `doc.user_group_id` — NOT from chunk or request (R002)
- [ ] `Embedding.text` set from `chunk.text` (D11 — chunk text stored here, not in documents)
- [ ] Bulk insert: `db.add_all([...])` then single `await db.commit()` (P004: no N+1)
- [ ] On `EmbedderError`: `doc.status = "failed"`, `await db.commit()`, log error, return early
- [ ] No partial insert: embeddings only committed after all vectors fetched
- [ ] Test: successful path → all Embedding rows inserted with correct `text`, doc.status unchanged
- [ ] Test: `EmbedderError` raised → 0 embeddings inserted, doc.status="failed"
- [ ] Rule satisfied: R002, P004, C002

**Test command**
```bash
pytest tests/rag/test_embedder.py::test_insert_embeddings tests/rag/test_embedder.py::test_embedder_failure_sets_status_failed -v
```

**Parallel**: after:T002
**Size estimate**: ~30 lines embedder + ~15 lines documents.py + ~35 lines tests

---
<!-- End T003 -->
