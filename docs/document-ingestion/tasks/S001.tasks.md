# Tasks: document-ingestion / S001 — POST /v1/documents — Upload & validate
Created: 2026-04-07 | Agent: api-agent | Status: TODO

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S001: POST /v1/documents — Upload & validate document |
| Total tasks | 5 |
| Parallel groups | G1: [T001], G2: [T002 after:T001], G3: [T003,T004 after:T002], G4: [T005 after:T003,T004] |
| Critical path | T001 → T002 → T003 → T005 |
| Agent | api-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | DocumentUpload schema + router scaffold | DONE | safe | T002 |
| T002 | auth_type write gate (D09/WARN-3) + test | DONE | after:T001 | T003,T004 |
| T003 | Input validation (lang, content, size) + test | DONE | after:T002 | T005 |
| T004 | RBAC group membership check + test | DONE | after:T002 | T005 |
| T005 | Document INSERT + BackgroundTasks dispatch + 202 response + test | DONE | after:T003,T004 | — |

---

## LAYER 2 — Task Detail

### T001: DocumentUpload Pydantic schema + router scaffold

**Status**: TODO

**File(s)**
- `backend/api/routes/documents.py` — action: create
- `backend/api/routes/__init__.py` — action: modify (register router)
- `tests/api/test_documents_upload.py` — action: create (import smoke test)

**Change description**
Create `documents.py` with `DocumentUpload` Pydantic model and empty `POST /v1/documents` router stub with `Depends(verify_token)`.
Register router in `__init__.py`. Write smoke test that imports the module without error.

**Review criteria**
- [ ] `DocumentUpload` fields: `title: str`, `content: str`, `lang: str`, `user_group_id: int | None = None`
- [ ] `title` max_length=500 (Q4 default)
- [ ] Route: `@router.post("/v1/documents", dependencies=[Depends(verify_token)])`
- [ ] Rule satisfied: R003 (verify_token), R004 (/v1/ prefix)
- [ ] Router registered in `__init__.py`

**Test command**
```bash
pytest tests/api/test_documents_upload.py::test_import_documents_router -v
```

**Parallel**: safe
**Size estimate**: ~30 lines

---
<!-- End T001 -->

### T002: auth_type write gate — OIDC users get 403

**Status**: TODO

**File(s)**
- `backend/api/routes/documents.py` — action: modify (add auth_type check)
- `tests/api/test_documents_upload.py` — action: modify (add auth gate tests)

**Change description**
In the route handler, check `current_user.auth_type != "api_key"` immediately after token validation.
Raise 403 with structured error for OIDC callers (D09, WARN-3).
TDD: write test with mock OIDC user → expect 403 before writing the gate code.

**Review criteria**
- [ ] Check is FIRST logic in handler (before any DB call)
- [ ] Error shape: `{"error": {"code": "FORBIDDEN", "message": "...", "request_id": "..."}}` (A005)
- [ ] Test: OIDC caller → 403 FORBIDDEN
- [ ] Test: API-key caller → not 403 (proceeds to next validation)
- [ ] Rule satisfied: D09, ARCH A005

**Test command**
```bash
pytest tests/api/test_documents_upload.py::test_oidc_caller_returns_403 tests/api/test_documents_upload.py::test_apikey_caller_passes_gate -v
```

**Parallel**: after:T001
**Size estimate**: ~10 lines handler + ~20 lines tests

---
<!-- End T002 -->

### T003: Input validation — lang, content empty, content size

**Status**: TODO

**File(s)**
- `backend/api/routes/documents.py` — action: modify (add validation logic)
- `tests/api/test_documents_upload.py` — action: modify (add validation tests)

**Change description**
Validate three conditions in order:
1. `content` empty/whitespace-only → 422 INVALID_INPUT
2. `content` length > `MAX_DOC_CHARS` (env, default 100000) → 413 DOC_TOO_LARGE
3. `lang` not valid ISO 639-1 (2-char alpha) → 422 INVALID_INPUT

**Review criteria**
- [ ] `MAX_DOC_CHARS = int(os.getenv("MAX_DOC_CHARS", "100000"))`
- [ ] 413 used for oversized content (not 422)
- [ ] 422 used for empty content and invalid lang
- [ ] Error codes: `DOC_TOO_LARGE`, `INVALID_INPUT` with `request_id`
- [ ] Test: empty string → 422
- [ ] Test: whitespace-only → 422
- [ ] Test: content len > MAX_DOC_CHARS → 413
- [ ] Test: lang="xx" (invalid) → 422; lang="ja" → passes
- [ ] Rule satisfied: A005, R004

**Test command**
```bash
pytest tests/api/test_documents_upload.py::test_input_validation -v
```

**Parallel**: after:T002
**Size estimate**: ~20 lines handler + ~30 lines tests

---
<!-- End T003 -->

### T004: RBAC group membership check

**Status**: TODO

**File(s)**
- `backend/api/routes/documents.py` — action: modify (add RBAC check)
- `tests/api/test_documents_upload.py` — action: modify (add RBAC tests)

**Change description**
Validate that `body.user_group_id` is in `current_user.user_group_ids` (if not None).
If caller provides a `user_group_id` they don't belong to → 403 FORBIDDEN.
`user_group_id=None` (public) → allowed for any API-key caller.

**Review criteria**
- [ ] Check: `body.user_group_id not in current_user.user_group_ids` → 403
- [ ] `user_group_id=None` → skip group check (public document)
- [ ] Error shape: `{"error": {"code": "FORBIDDEN", ...}}` (A005)
- [ ] Test: caller in group → passes
- [ ] Test: caller NOT in group → 403
- [ ] Test: user_group_id=None → passes (public)
- [ ] Rule satisfied: R001 (RBAC), C001

**Test command**
```bash
pytest tests/api/test_documents_upload.py::test_rbac_group_check -v
```

**Parallel**: after:T002
**Size estimate**: ~10 lines handler + ~25 lines tests

---
<!-- End T004 -->

### T005: Document INSERT + BackgroundTasks dispatch + 202 response

**Status**: TODO

**File(s)**
- `backend/api/routes/documents.py` — action: modify (add DB insert + bg task)
- `tests/api/test_documents_upload.py` — action: modify (add success path tests)

**Change description**
After all validation passes:
1. INSERT `Document(title, lang, user_group_id, status="processing")` → get `doc_id` — NO `content` field (D11: raw content not stored in documents)
2. `bg.add_task(ingest_pipeline, doc_id, body.content)` — pass `content` as argument through memory, not from DB
3. Return `JSONResponse(status_code=202, content={"doc_id": str(doc_id), "status": "processing"})`

**Review criteria**
- [ ] Response code is 202 (not 200 or 201)
- [ ] Response body: `{"doc_id": "<uuid>", "status": "processing"}`
- [ ] `content` NOT stored in `documents` table (D11) — passed to `ingest_pipeline` as argument
- [ ] `BackgroundTasks.add_task(ingest_pipeline, doc_id, body.content)` — two args: doc_id + content
- [ ] DB insert uses ORM — `Document(title=..., lang=..., user_group_id=..., status="processing")` (no string interpolation — S001)
- [ ] Test: success → 202 + doc_id in response
- [ ] Test: doc_id is valid UUID
- [ ] Test: `ingest_pipeline` called once (mock assert)
- [ ] Rule satisfied: A005, R003, R004, S001 (no SQL injection)

**Test command**
```bash
pytest tests/api/test_documents_upload.py::test_successful_upload_returns_202 -v
```

**Parallel**: after:T003,T004
**Size estimate**: ~25 lines handler + ~30 lines tests

---
<!-- End T005 -->
