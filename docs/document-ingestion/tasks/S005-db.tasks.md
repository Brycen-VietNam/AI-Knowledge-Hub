# Tasks: document-ingestion / S005-db — Migration 006 + Document model update
Created: 2026-04-07 | Agent: db-agent | Status: TODO

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S005-db: Migration 006 + Document model update |
| Total tasks | 2 |
| Parallel groups | G1: [T001], G2: [T002 after:T001] |
| Critical path | T001 → T002 |
| Agent | db-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Migration 006 — add status column | DONE | safe | T002 |
| T002 | Document model — add status field + test | DONE | after:T001 | — |

---

## LAYER 2 — Task Detail

### T001: Migration 006 — add status to documents + text to embeddings

**Status**: TODO

**File(s)**
- `backend/db/migrations/006_add_document_status_and_chunk_text.sql` — action: create

**Change description**
Two changes in one migration (D11 — 2026-04-07):
1. Add `status VARCHAR(20) NOT NULL DEFAULT 'processing'` with CHECK constraint to `documents` table.
2. Add `text TEXT NOT NULL` to `embeddings` table — stores chunk text for RAG retrieval (replaces storing raw content in documents — DB bloat avoided).

Follows migration convention: numbered file, rollback section at bottom (C010, A006).

**Review criteria**
- [ ] File named `006_add_document_status_and_chunk_text.sql` (NNN_description pattern)
- [ ] `documents.status`: CHECK constraint includes exactly: `'processing'`, `'ready'`, `'failed'`
- [ ] `documents.status`: DEFAULT value is `'processing'`
- [ ] `embeddings.text`: `TEXT NOT NULL`
- [ ] Rollback section present as comment at bottom (drops both columns)
- [ ] Rule satisfied: A006 (migration strategy), C010 (schema changes via migration)

**Test command**
```bash
psql $DATABASE_URL -c "\d documents" | grep status
psql $DATABASE_URL -c "\d embeddings" | grep text
```

**Rollback**
```sql
-- Rollback:
ALTER TABLE embeddings DROP COLUMN text;
ALTER TABLE documents DROP COLUMN status;
```

**Parallel**: safe
**Size estimate**: ~8 lines SQL

---
<!-- End T001 -->

### T002: ORM model updates — Document.status + Embedding.text + test

**Status**: TODO

**File(s)**
- `backend/db/models/document.py` — action: modify (add `status` field)
- `backend/db/models/embedding.py` — action: modify (add `text` field)
- `tests/db/test_document_model.py` — action: create

**Change description**
Two ORM updates after migration 006 is applied (D11):
1. `Document`: add `status: Mapped[str] = mapped_column(String(20), default="processing")`
2. `Embedding`: add `text: Mapped[str] = mapped_column(Text(), nullable=False)` — chunk text for RAG retrieval

TDD: write tests first.

**Review criteria**
- [ ] `Document.status: Mapped[str] = mapped_column(String(20), default="processing")`
- [ ] `Embedding.text: Mapped[str] = mapped_column(Text(), nullable=False)`
- [ ] Both added AFTER migration file exists (C010)
- [ ] Test: `Document(...)` without `status` → default is `"processing"`
- [ ] Test: `Document.status` accepts `"ready"` and `"failed"`
- [ ] Test: `Embedding(text="chunk text", ...)` → `text` stored correctly
- [ ] No other fields changed in either model

**Test command**
```bash
pytest tests/db/test_document_model.py -v
```

**Parallel**: after:T001
**Size estimate**: ~3 lines in document.py, ~2 lines in embedding.py, ~25 lines in test

---
<!-- End T002 -->
