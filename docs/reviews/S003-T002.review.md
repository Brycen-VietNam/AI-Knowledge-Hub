# Code Review: S003/T002 — Update document.py with content_fts column
Level: quick | Date: 2026-03-19 | Reviewer: Claude | Verdict: APPROVED

## Task Review Criteria
- [x] `content_fts` column added with `TSVECTOR` type (PostgreSQL dialect) — via `Text().with_variant(TSVECTOR, "postgresql")`
- [x] Column is nullable (rag-agent populates asynchronously) — `nullable=True`
- [x] Import: `from sqlalchemy.dialects.postgresql import TSVECTOR`
- [x] No other columns changed
- [x] No imports from outside `backend/db/` (A001) ✅
- [x] `test_documents_has_content_fts_column` passes — 21/21 ✅

## Quick Checks
- [x] All review_criteria satisfied
- [x] Test command passes: 21/21 (pytest tests/db/test_models.py -v)
- [x] Only TOUCH files modified

## Notable: SQLite compatibility fix
`TSVECTOR` not supported in SQLite (used for in-memory unit tests).
`Text().with_variant(TSVECTOR, "postgresql")` — uses `TSVECTOR` on PostgreSQL, `TEXT` on SQLite.
Column name still visible via `inspect()` on SQLite → test passes. ✅

## Issues Found
None.

## Verdict
**APPROVED** ✅

S003 complete — T001 ✅ T002 ✅ (all REVIEWED).
