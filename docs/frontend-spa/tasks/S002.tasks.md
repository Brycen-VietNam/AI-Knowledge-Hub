# Tasks: frontend-spa / S002 — Query Page — Search Input & Language Selector
Created: 2026-04-16 | Agent: frontend-agent | Status: DONE

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S002: Query Page — Search Input & Language Selector |
| Total tasks | 6 |
| Parallel groups | G1[T001] → G2[T002∥T003∥T004] → G3[T005] → G4[T006] |
| Critical path | T001 → T002 → T005 → T006 |
| Agent | frontend-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | queryStore.ts — query state (no history) | DONE | safe | T002,T005 |
| T002 | SearchInput.tsx — 512-char limit + IME guard | DONE | after:T001 | T005 |
| T003 | LanguageSelector.tsx — 4 locales + localStorage | DONE | safe | T005 |
| T004 | i18n setup + 4 locale JSON files (S002 keys) | DONE | safe | T005 |
| T005 | QueryPage.tsx — wire SearchInput + LanguageSelector | DONE | after:T001,T002,T003,T004 | T006 |
| T006 | App.tsx — add /query route + update tests | DONE | after:T005 | — |

---

## LAYER 2 — Task Detail

---

### T001: queryStore.ts — query state (no history)

**Status**: DONE

**File(s)**
- `frontend/src/store/queryStore.ts` — create

**Change description**
Create Zustand store with query state only (history deferred to S004).
Fields: `query: string`, `isLoading: boolean`, `error: string|null`.
Actions: `setQuery(q)`, `setLoading(b)`, `setError(e)`, `reset()`.
No API call here — S003 will add `submitQuery()` action when results panel is added.
TDD: write test first — `queryStore.test.ts` covers initial state + all actions.

**Review criteria**
- [ ] No localStorage for query state (session-only per D004)
- [ ] `isLoading` prevents double-submit (used by SearchInput T002)
- [ ] `reset()` clears query + error + isLoading
- [ ] Test file included in TOUCH list; test written before implementation

**Test command**
```bash
npm run test -- src/store/queryStore.test.ts
```

**Parallel**: safe
**Size estimate**: ~35 lines (store) + ~40 lines (test)

---
<!-- End T001 -->

---

### T002: SearchInput.tsx — 512-char limit + IME guard

**Status**: DONE

**File(s)**
- `frontend/src/components/query/SearchInput.tsx` — create
- `tests/frontend/src/components/query/SearchInput.test.tsx` — create

**Change description**
Search input component: textarea + submit button.
- `maxLength` enforced client-side: disable submit when `value.trim() === ""` OR `value.length > 512`
- Character counter: display `{length}/512` (i18n key: `search.char_count`)
- IME guard: `onKeyDown` checks `e.nativeEvent.isComposing` → block Enter during CJK composition
- `disabled` prop passed through when `isLoading === true` (both input + button)
- Props: `value`, `onChange`, `onSubmit`, `isLoading`, `disabled?`
TDD: test IME guard, 512-limit disable, empty-disable, loading-disable.

**Review criteria**
- [ ] `e.nativeEvent.isComposing` used (not `e.isComposing`) — standard cross-browser
- [ ] Submit blocked when `value.length > 512` (AC7)
- [ ] Submit blocked when `value.trim() === ""` (AC8)
- [ ] Input + button both disabled when `isLoading` (AC9)
- [ ] i18n key `search.placeholder`, `search.button`, `search.char_count` used via `useTranslation`
- [ ] Test file in TOUCH list; failing test written before component

**Test command**
```bash
npm run test -- src/components/query/SearchInput.test.tsx
```

**Parallel**: after:T001
**Size estimate**: ~45 lines (component) + ~50 lines (test)

---
<!-- End T002 -->

---

### T003: LanguageSelector.tsx — 4 locales + localStorage

**Status**: DONE

**File(s)**
- `frontend/src/components/query/LanguageSelector.tsx` — create
- `tests/frontend/src/components/query/LanguageSelector.test.tsx` — create

**Change description**
Dropdown/select component for UI language selection.
Options: `[{value:"ja",label:"日本語"},{value:"en",label:"English"},{value:"vi",label:"Tiếng Việt"},{value:"ko",label:"한국어"}]`.
`onChange`: calls `i18n.changeLanguage(lang)` + `localStorage.setItem("lang", lang)`.
Init order: `localStorage.getItem("lang")` → `navigator.language.split("-")[0]` → `"en"`.
TDD: test init from localStorage, init from navigator, fallback to "en", onChange fires i18n.changeLanguage.

**Review criteria**
- [ ] Language persist in `localStorage` key `"lang"` (D003) — only the lang code, not token
- [ ] Init priority: localStorage → navigator.language → "en" (per plan notes)
- [ ] `i18n.changeLanguage()` called on change
- [ ] i18n key `lang.selector_label` used for aria-label / label
- [ ] Test covers all 3 init paths + onChange

**Test command**
```bash
npm run test -- src/components/query/LanguageSelector.test.tsx
```

**Parallel**: safe (no dependency on T001/T002)
**Size estimate**: ~35 lines (component) + ~45 lines (test)

---
<!-- End T003 -->

---

### T004: i18n setup + 4 locale JSON files (S002 keys only)

**Status**: DONE

**File(s)**
- `frontend/src/i18n/index.ts` — create (react-i18next init)
- `frontend/src/i18n/locales/ja.json` — create
- `frontend/src/i18n/locales/en.json` — create
- `frontend/src/i18n/locales/vi.json` — create
- `frontend/src/i18n/locales/ko.json` — create
- `frontend/package.json` — modify (add `react-i18next`, `i18next` if not present)
- `tests/frontend/src/i18n/i18n.test.ts` — create

**Change description**
Initialize react-i18next with 4 locale resources.
S002 keys to include: `search.placeholder`, `search.button`, `search.char_count`, `lang.selector_label`.
Also carry over S001 auth keys already in locales (merge, not replace).
`i18n/index.ts`: `i18next.use(initReactI18next).init({resources, lng: savedLang, fallbackLng:"en"})`.
TDD: test that all 4 S002 keys exist in all 4 locales; test fallback to "en" for missing key.

**Review criteria**
- [ ] All 4 keys × 4 locales — no missing translations (test enforces this)
- [ ] `fallbackLng: "en"` set (S003/S004 will add more keys — fallback prevents blank UI)
- [ ] `lng` init reads from `localStorage.getItem("lang")` (consistent with T003 init)
- [ ] `react-i18next` and `i18next` in `package.json` dependencies
- [ ] Smoke test: `import i18n from './i18n'` passes; t('search.button') returns non-empty string

**Test command**
```bash
npm run test -- src/i18n/i18n.test.ts
```

**Parallel**: safe (independent of T001/T002/T003)
**Size estimate**: ~30 lines (index.ts) + ~25 lines (4 JSON files) + ~20 lines (test)

---
<!-- End T004 -->

---

### T005: QueryPage.tsx — wire SearchInput + LanguageSelector

**Status**: DONE

**File(s)**
- `frontend/src/pages/QueryPage.tsx` — create
- `tests/frontend/src/pages/QueryPage.test.tsx` — create

**Change description**
Page component that composes SearchInput + LanguageSelector.
Reads `query`, `isLoading`, `setQuery`, `setLoading`, `setError` from `queryStore`.
`onSubmit`: sets `isLoading(true)`, calls `/v1/query` POST via Axios client (stub for now — S003 moves API call into store; here just log + setLoading(false)).
Results area: empty placeholder `<div id="results-area" />` — S003 will inject AnswerPanel here.
TDD: test renders SearchInput + LanguageSelector; test onSubmit sets isLoading; test placeholder exists.

**Review criteria**
- [ ] `useProtectedRoute()` or `ProtectedRoute` wrapping confirmed in App.tsx (R003 equivalent for frontend)
- [ ] `isLoading` passed to SearchInput — double-submit blocked
- [ ] Results placeholder `id="results-area"` present (S003 integration point)
- [ ] No direct `localStorage` access for token (client.ts interceptor handles Bearer header)
- [ ] i18n `useTranslation` imported and passed to child components

**Test command**
```bash
npm run test -- src/pages/QueryPage.test.tsx
```

**Parallel**: after:T001,T002,T003,T004
**Size estimate**: ~45 lines (page) + ~40 lines (test)

---
<!-- End T005 -->

---

### T006: App.tsx — add /query route + update tests

**Status**: DONE

**File(s)**
- `frontend/src/App.tsx` — modify (add `/query` route inside ProtectedRoute)
- `tests/frontend/src/App.test.tsx` — modify (add route render test)

**Change description**
Add `/query` route to existing Router setup from S001.
Route: `<Route path="/query" element={<ProtectedRoute><QueryPage /></ProtectedRoute>} />`.
After login success, redirect to `/query` (update LoginPage.tsx `onSuccess` navigate target if needed).
TDD: test unauthenticated user at `/query` redirects to `/login`; test authenticated user renders QueryPage.

**Review criteria**
- [ ] `/query` is inside `ProtectedRoute` — unauthenticated access redirects to `/login` (R003 frontend equivalent)
- [ ] `<Navigate replace>` used in ProtectedRoute (no back-loop — confirmed pattern from S001)
- [ ] LoginPage redirects to `/query` after successful login (not back to `/`)
- [ ] Existing S001 tests (55 pass) still pass — no regression

**Test command**
```bash
npm run test -- src/App.test.tsx
```

**Parallel**: after:T005
**Size estimate**: ~10 lines changed (App.tsx) + ~15 lines (test additions)

---
<!-- End T006 -->
