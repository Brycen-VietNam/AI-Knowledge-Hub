# Tasks: frontend-spa / S003 — Query Results: Answer + Citations Display
Created: 2026-04-16 | Agent: frontend-agent | Status: DONE

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S003: Query Results — Answer + Citations Display |
| Total tasks | 6 |
| Parallel groups | G1[T001], G2[T002∥T003∥T005], G3[T004], G4[T006] |
| Critical path | T001 → T002 → T004 → T006 |
| Agent | frontend-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | queryStore — add submitQuery action + API call + error states | DONE | safe | T002,T003,T004 |
| T002 | ConfidenceBadge.tsx — score thresholds + badge colors | DONE | after:T001 | T004 |
| T003 | CitationItem.tsx — title + score% + chunk preview | DONE | after:T001 | T004 |
| T004 | CitationList.tsx — collapsed > 3 + show-more toggle | DONE | after:T002,T003 | T006 |
| T005 | LowConfidenceWarning.tsx + i18n results keys (4 locales) | DONE | after:T001 | T006 |
| T006 | AnswerPanel.tsx + QueryPage integration + react-markdown | DONE | after:T004,T005 | — |

---

## LAYER 2 — Task Detail

---

### T001: queryStore — add submitQuery action + API call + error states

**Status**: TODO

**File(s)**
- `frontend/src/store/queryStore.ts` — modify (add submitQuery, isLoading, error, result state)
- `frontend/tests/store/queryStore.test.ts` — modify (add submitQuery tests)

**Change description**
Extend existing queryStore (S002) with:
- `result: QueryResult | null` — `{ answer: string, citations: Citation[], confidence: number }`
- `isLoading: boolean`, `error: string | null`
- `submitQuery(query, lang)`: POST `/v1/query` via Axios client, sets `isLoading` during call, writes result on success, sets error string on 429/5xx/network failure; clears previous result+error on submit

**Review criteria**
- [ ] POST `/v1/query` uses Axios instance from `client.ts` (Bearer injected via interceptor — not manually)
- [ ] 429 → `error = t("results.error_rate_limit")`; 5xx → `error = t("results.error_service")`
- [ ] 401 handled by Axios interceptor (not in submitQuery)
- [ ] `isLoading = false` in finally block (never stuck loading on error)
- [ ] TDD: failing tests written before implementation

**Test command**
```bash
cd frontend && npm run test -- tests/store/queryStore.test.ts --reporter=verbose
```

**Parallel**: safe
**Size estimate**: ~45 lines changed (store) + ~30 lines (tests)

---
<!-- End T001 -->

---

### T002: ConfidenceBadge.tsx — score thresholds + badge colors

**Status**: TODO

**File(s)**
- `frontend/src/components/results/ConfidenceBadge.tsx` — create
- `frontend/tests/components/results/ConfidenceBadge.test.tsx` — create

**Change description**
Pure display component: `ConfidenceBadge({ score: number })`.
- `score >= 0.7` → badge label "HIGH", green (`bg-green-100 text-green-800`)
- `score >= 0.4` → badge label "MEDIUM", yellow (`bg-yellow-100 text-yellow-800`)
- `score < 0.4` → badge label "LOW", red (`bg-red-100 text-red-800`)
No i18n needed — badge labels are fixed identifiers, not user-facing prose.

**Review criteria**
- [ ] All 3 threshold branches rendered correctly
- [ ] Component is pure (no store access, props only)
- [ ] Boundary values: 0.7 → HIGH, 0.4 → MEDIUM, 0.399 → LOW
- [ ] TDD: tests cover all 3 branches + boundaries

**Test command**
```bash
cd frontend && npm run test -- tests/components/results/ConfidenceBadge.test.tsx --reporter=verbose
```

**Parallel**: after:T001
**Size estimate**: ~25 lines (component) + ~30 lines (tests)

---
<!-- End T002 -->

---

### T003: CitationItem.tsx — title + score% + chunk preview

**Status**: TODO

**File(s)**
- `frontend/src/components/results/CitationItem.tsx` — create
- `frontend/tests/components/results/CitationItem.test.tsx` — create

**Change description**
Display component: `CitationItem({ citation: Citation })` where `Citation = { doc_id, title, score, chunk_preview }`.
- Shows `title` (full, no truncation — truncation is CitationList's job)
- Score formatted as `Math.round(score * 100) + "%"` → "91%" (D012)
- `chunk_preview`: render as plain text, clamped to 2 lines via CSS (`line-clamp-2`)
- `data-testid="citation-item"` for test targeting

**Review criteria**
- [ ] Score formatted as integer % (no decimals) — `Math.round(0.914) → "91%"`
- [ ] chunk_preview rendered as plain text (no ReactMarkdown — XSS risk on partial chunks)
- [ ] Component is pure (props only, no store)
- [ ] TDD: test score formatting, truncation class present, title render

**Test command**
```bash
cd frontend && npm run test -- tests/components/results/CitationItem.test.tsx --reporter=verbose
```

**Parallel**: after:T001
**Size estimate**: ~25 lines (component) + ~25 lines (tests)

---
<!-- End T003 -->

---

### T004: CitationList.tsx — collapsed > 3 + show-more toggle

**Status**: TODO

**File(s)**
- `frontend/src/components/results/CitationList.tsx` — create
- `frontend/tests/components/results/CitationList.test.tsx` — create

**Change description**
`CitationList({ citations: Citation[] })`.
- `citations.length <= 3`: render all `CitationItem`s flat
- `citations.length > 3`: show first 3, hide rest behind "Show X more" button (X = remaining count)
- Toggle state: `expanded` (local `useState`) — button label switches to `t("results.hide")` when expanded
- Uses `CitationItem` for each row
- `citations.length === 0`: render nothing (empty fragment — AnswerPanel handles the empty state message)

**Review criteria**
- [ ] Default collapsed when > 3 citations
- [ ] "Show X more" count = total − 3 (not total)
- [ ] Toggle cycles correctly: collapsed → expanded → collapsed
- [ ] Empty array renders nothing (no orphan DOM)
- [ ] TDD: test collapsed default, show-more click, hide click, empty state

**Test command**
```bash
cd frontend && npm run test -- tests/components/results/CitationList.test.tsx --reporter=verbose
```

**Parallel**: after:T002,T003
**Size estimate**: ~35 lines (component) + ~35 lines (tests)

---
<!-- End T004 -->

---

### T005: LowConfidenceWarning.tsx + i18n results keys (4 locales)

**Status**: TODO

**File(s)**
- `frontend/src/components/results/LowConfidenceWarning.tsx` — create
- `frontend/src/i18n/locales/en.json` — modify (add `results.*` keys)
- `frontend/src/i18n/locales/ja.json` — modify
- `frontend/src/i18n/locales/vi.json` — modify
- `frontend/src/i18n/locales/ko.json` — modify
- `frontend/tests/components/results/LowConfidenceWarning.test.tsx` — create
- `frontend/tests/i18n/i18n.test.ts` — modify (add results key assertions)

**Change description**
`LowConfidenceWarning`: simple banner, no props — shows `t("results.low_confidence_warning")`. Styled as amber/warning box.

i18n keys to add to all 4 locales:
```
results.no_results, results.low_confidence_warning,
results.show_more, results.hide,
results.error_rate_limit, results.error_service,
results.no_source_warning
```

**Review criteria**
- [ ] All 7 keys present in all 4 locale files
- [ ] No key missing in any locale (i18n test covers all)
- [ ] Component renders `t("results.low_confidence_warning")` (no hardcoded strings)
- [ ] TDD: i18n test added for results.* keys before component test

**Test command**
```bash
cd frontend && npm run test -- tests/i18n/i18n.test.ts tests/components/results/LowConfidenceWarning.test.tsx --reporter=verbose
```

**Parallel**: after:T001
**Size estimate**: ~15 lines (component) + ~28 lines (locale diffs) + ~15 lines (tests)

---
<!-- End T005 -->

---

### T006: AnswerPanel.tsx + QueryPage integration + react-markdown

**Status**: TODO

**File(s)**
- `frontend/src/components/results/AnswerPanel.tsx` — create
- `frontend/src/pages/QueryPage.tsx` — modify (wire result panel below search input)
- `frontend/package.json` — modify (add `react-markdown`)
- `frontend/tests/components/results/AnswerPanel.test.tsx` — create
- `frontend/tests/pages/QueryPage.test.tsx` — modify (add integration render test)

**Change description**
`AnswerPanel({ answer, citations, confidence, isLoading, error })`:
- `isLoading` → show skeleton/spinner, hide result area
- `error` → show error string (pre-localised from store), no answer/citations
- `answer && citations.length === 0` → show answer + `t("results.no_source_warning")` banner
- `!answer && citations.length === 0` → show `t("results.no_results")`
- `confidence < 0.4` → render `<LowConfidenceWarning />`
- Answer rendered via `<ReactMarkdown>{answer}</ReactMarkdown>`
- `<CitationList citations={citations} />` below answer
- `<ConfidenceBadge score={confidence} />` in panel header

QueryPage wires: `const { result, isLoading, error, submitQuery } = useQueryStore()` — calls `submitQuery(query, selectedLang)` on form submit; passes result props to `AnswerPanel`.

**Review criteria**
- [ ] `react-markdown` added to `package.json` dependencies (not devDependencies)
- [ ] All 4 state branches tested: loading / error / empty / results
- [ ] `no_source_warning` shown when answer present but citations empty
- [ ] `LowConfidenceWarning` rendered only when `confidence < 0.4`
- [ ] QueryPage test: submitting SearchInput calls `submitQuery` with correct args
- [ ] Rule R003: no auth bypass — QueryPage is behind ProtectedRoute (already enforced by App.tsx; verify not regressed)
- [ ] TDD: AnswerPanel tests written before wiring QueryPage

**Test command**
```bash
cd frontend && npm run test -- tests/components/results/AnswerPanel.test.tsx tests/pages/QueryPage.test.tsx --reporter=verbose
```

**Parallel**: after:T004,T005
**Size estimate**: ~50 lines (AnswerPanel) + ~15 lines (QueryPage delta) + ~50 lines (tests)

---
<!-- End T006 -->
