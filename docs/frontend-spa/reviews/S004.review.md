# Code Review: frontend-spa S004 — Query History
**Level**: quick | **Date**: 2026-04-17 | **Reviewer**: Claude Haiku

## Task Summary
S004 implements session-level query history with 5 tasks:
- T001: QueryHistoryItem type + history state
- T002: HistoryItem component (CJK-safe truncation)
- T003: HistoryPanel container (returns null when empty)
- T004: addHistory in submitQuery (only on success)
- T005: Wire HistoryPanel into QueryPage + logout → clearHistory

## Review Criteria Verification

### Task T001 — QueryHistoryItem type + history state
- [x] `QueryHistoryItem` exported from queryStore.ts
- [x] `history` initialised as `[]` in store state
- [x] `addHistory` prepends and caps at 20 (`.slice(0, 20)`)
- [x] `reset()` does NOT reset history (D004 — session-only)
- [x] `selectHistory` restores query + result correctly

### Task T002 — HistoryItem component
- [x] Truncation uses `[...str].slice(0, 60)` (CJK-safe, NOT str.slice)
- [x] Truncated text ends with `…` (U+2026)
- [x] Timestamp is HH:mm format (via padStart)
- [x] `onClick` calls `onSelect(item)`

### Task T003 — HistoryPanel component
- [x] Returns `null` when `history.length === 0`
- [x] `<aside>` element with `aria-label={t('history.title')}`
- [x] Clear button calls `clearHistory()`
- [x] i18n keys added to all 4 locales (en, ja, vi, ko)

### Task T004 — addHistory in submitQuery
- [x] `get()` accessible in `submitQuery` (factory signature confirmed)
- [x] `addHistory(query, answer, citations)` called after success
- [x] Error path (429/5xx) does NOT call `addHistory`
- [x] Tests verify both success and error paths

### Task T005 — Wire QueryPage + clearHistory on logout
- [x] `<HistoryPanel />` rendered in QueryPage sidebar
- [x] `useQueryStore.getState().clearHistory()` called FIRST in logout
- [x] HistoryPanel absent when history empty (returns null)
- [x] No regression in S003 tests

## Test Results
- **All Tests Pass**: 208/208 ✅
  - queryStore: 26 tests
  - HistoryItem: 5 tests
  - HistoryPanel: 5 tests
  - QueryPage: 10 tests
  - authStore: 8 tests

## Files Modified
- frontend/src/store/queryStore.ts ✅
- frontend/src/store/authStore.ts ✅
- frontend/src/components/history/HistoryItem.tsx ✅ (new)
- frontend/src/components/history/HistoryPanel.tsx ✅ (new)
- frontend/src/pages/QueryPage.tsx ✅
- frontend/src/i18n/locales/{en,ja,vi,ko}.json ✅

## Issues Found
None. All criteria satisfied.

---

## Verdict
✅ **APPROVED**

All 5 tasks complete and correct. Implementation is robust (20-item history cap, session-only with logout clear), CJK-safe, fully i18n'd, and well-tested. Ready for S005.
