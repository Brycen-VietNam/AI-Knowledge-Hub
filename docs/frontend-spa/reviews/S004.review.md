## Code Review: S004 — Query History (Session-level)
Level: quick | Date: 2026-04-16 | Reviewer: Claude

---

### Task Review Criteria

#### T001 — QueryHistoryItem type + history state
- [x] `QueryHistoryItem` exported from queryStore.ts
- [x] `history` initialised as `[]` in store state
- [x] `addHistory` prepends and caps at 20 (`.slice(0, 20)`)
- [x] `reset()` does NOT reset history (D004 — session-only)
- [x] `selectHistory` restores query + result correctly
- [x] All new tests pass: queryStore.test.ts

#### T002 — HistoryItem component
- [x] Truncation uses `[...str].slice(0, 60)` — NOT `str.slice(0, 60)`
- [x] Truncated text ends with `…` (`\u2026`)
- [x] Timestamp is HH:mm format (via `padStart(2,'0')`)
- [x] `onClick` calls `onSelect(item)`
- [x] All 5 tests pass: HistoryItem.test.tsx

#### T003 — HistoryPanel component
- [x] Returns `null` when `history.length === 0`
- [x] `<aside>` element with `aria-label={t('history.title')}`
- [x] Clear button calls `clearHistory()`
- [x] i18n keys (`history.title`, `history.clear`) added to all 4 locale files: en, ja, vi, ko
- [x] All 5 tests pass: HistoryPanel.test.tsx

#### T004 — addHistory in submitQuery
- [x] `get()` accessible in `submitQuery` — store factory signature is `(set, get)`
- [x] `addHistory(query, answer, citations)` called after `set({ result: response.data })`
- [x] Error path (4xx/5xx) does NOT call `addHistory`
- [x] `beforeEach` resets `history: []` alongside other fields (line 10 of queryStore.test.ts)
- [x] New tests pass alongside existing queryStore tests

#### T005 — Wire QueryPage + clearHistory on logout
- [x] `<HistoryPanel />` rendered in `QueryPage` beside `<main>` in flex container
- [x] `useQueryStore.getState().clearHistory()` called **first** in `logout` action
- [x] `HistoryPanel` absent when `history` is empty (component returns `null`)
- [x] QueryPage layout does not break existing S003 AnswerPanel tests
- [x] All suite tests pass

---

### Quick Level Checks
- [x] All review_criteria from task file satisfied (25/25)
- [x] Test command passes — 54 tests, 5 files, 0 failures
- [x] No files outside TOUCH list modified

---

### Issues Found

None.

---

### Verdict
[x] APPROVED  [ ] CHANGES REQUIRED  [ ] BLOCKED

All 54 tests pass. All 25 review criteria satisfied. Implementation matches spec exactly.
