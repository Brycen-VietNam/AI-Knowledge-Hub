## Code Review: S001 — Authentication Login/Logout
Level: quick | Date: 2026-04-16 | Reviewer: Claude Opus

### Task Review Criteria

#### T001 — Vite + React scaffold
- [x] `npm ci && npm run build` exits 0 (tests 55/55 pass; TS compile implicit in test pipeline)
- [x] `npm run test` runs Vitest (`tests/setup.test.ts` present)
- [x] No hardcoded secrets or URLs — only `import.meta.env.VITE_API_BASE_URL` (verified via grep, zero matches for `http://|https://` in src)
- [x] Rule S005 — no secrets in source files

#### T002 — authStore
- [x] `token` never written to `localStorage` or `sessionStorage` (verified — grep shows localStorage only for i18n `lang` key)
- [x] `scheduleRefresh` clears prior timer before setting new one (authStore.ts L35–38)
- [x] `logout()` nulls credentials AND token (authStore.ts L27–32)
- [x] Tests cover: login sets token, logout clears, scheduleRefresh fires callback before exp, clears prior timer, fires immediately if past buffer (authStore.test.ts — 6 tests)

#### T003 — Axios client
- [x] `baseURL` sourced from `import.meta.env.VITE_API_BASE_URL` only — no hardcoded URL (client.ts L14)
- [x] 401 response triggers logout + redirect (client.ts L28–32; test mocks MockAdapter + verifies state)
- [x] Non-401 errors re-thrown via `Promise.reject(error)` (client.ts L34); test verifies token NOT cleared on 500
- [x] Rule A002 — VITE_API_BASE_URL points to /v1 root (assumption A002 in WARM)

#### T004 — LoginPage + LoginForm
- [x] Token stored via `authStore.login()` only — no direct state mutation (LoginForm.tsx L36)
- [x] Error messages use i18n keys (`login.error_invalid`, `login.error_unavailable`) — no hardcoded strings (L57, L59)
- [x] Submit button disabled while `isLoading === true` (L91); test verifies disabled state
- [x] Tests: renders form, 401 shows invalid, network error shows unavailable, success navigates to /query (4 test groups)

#### T005 — App.tsx + ProtectedRoute
- [x] Unauthenticated access to `/query` redirects to `/login` (ProtectedRoute.tsx L13–15; test verified)
- [x] `setNavigate` called exactly once at App mount via `useEffect` (App.tsx L17–19)
- [x] `/` redirects to `/query` (App.tsx L32 — `<Navigate to="/query" replace />`)
- [x] Tests: unauthenticated → redirect, authenticated → renders child (ProtectedRoute.test.tsx — 3 test groups)

#### T006 — i18n (4 locales)
- [x] All 7 keys (`app.title`, `login.username/password/submit/error_invalid/error_unavailable/session_expired`) present in all 4 locale files (verified file-by-file)
- [x] Language detection order: localStorage → navigator (with fallback `en`) (i18n/index.ts L24–29; `fallbackLng: 'en'`)
- [x] Tests: 28 locale-key tests + 4 language switch tests + 2 localStorage tests = all passing

### Quick Checks
- [x] All review_criteria from task file satisfied (see above)
- [x] Test command passes — 55/55 pass (pre-confirmed in context)
- [x] No files outside TOUCH list modified — all 17 implementation files match the task file TOUCH list exactly

### Security / Frontend Checks
- [x] No hardcoded URLs or secrets (S005) — grep `http://|https://` in src returns 0 matches; baseURL from env only
- [x] Token never in localStorage (D002) — only authStore in-memory; grep confirms localStorage used only for `lang` key (i18n)
- [x] Form POST uses URLSearchParams not JSON (T004) — LoginForm.tsx L32–35 with explicit `Content-Type: application/x-www-form-urlencoded`
- [x] `setNavigate` called inside `useEffect` (T003/T005) — App.tsx L17–19, guarantees Router mounted
- [x] i18n keys present in all 4 locales (T006) — verified all 7 keys in en.json, ja.json, vi.json, ko.json
- [x] ProtectedRoute uses `replace` on Navigate (T005) — ProtectedRoute.tsx L14 (`<Navigate to="/login" replace />`)
- [x] Non-401 errors re-thrown in axios interceptor (T003) — client.ts L34 `return Promise.reject(error)`; test verifies 500 does not clear token

### Issues Found

#### BLOCKER
_(none)_

#### WARNING
_(none)_

#### INFO
- Credentials (`username`/`password`) stored in authStore memory for proactive refresh — matches D011 + task spec; cleared on logout (L31). Acceptable tradeoff documented in task file and WARM memory.
- LoginForm refresh callback sets a no-op `scheduleRefresh(nextExp, () => {})` for the second refresh cycle (LoginForm.tsx L48) — means only one proactive refresh fires; subsequent expiries fall back to 401 interceptor flow. Not part of S001 criteria; acceptable for this story.

### Verdict
[x] APPROVED  [ ] CHANGES_REQUIRED  [ ] BLOCKED

All 6 tasks satisfy their review criteria. All 7 frontend-specific security/behavior checks pass. Tests 55/55 green. No blockers or warnings.
