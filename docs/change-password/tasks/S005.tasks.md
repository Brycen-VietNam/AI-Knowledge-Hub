# Tasks: change-password / S005 — Frontend Force-Change Password Gate
Created: 2026-04-22 | Agent: frontend-agent | Status: DONE

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S005: Frontend Force-Change Password Gate (/change-password page, first login) |
| Total tasks | 5 |
| Parallel groups | G1: [T001,T002,T003], G2: [T004], G3: [T005] |
| Critical path | T001 → T004 → T005 |
| Agent | frontend-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Add CSS classes for force-change page to index.css | DONE | safe | T004 |
| T002 | Add i18n keys for force-change to all 4 lang files | DONE | safe | T004 |
| T003 | Extend authStore to store mustChangePassword from login | DONE | safe | T004 |
| T004 | Create ChangePasswordPage.tsx (force-change form) | DONE | after:T001,T002,T003 | T005 |
| T005 | Add /change-password route + ProtectedRoute gate in App.tsx | DONE | after:T004 | — |

---

## LAYER 2 — Task Detail

### T001: Add CSS classes for force-change page to index.css

**Status**: DONE

**File(s)**
- `frontend/src/index.css` — modify: append at end of file

**Change description**
Append the 6 CSS class definitions required by `ChangePasswordPage.tsx`.
All CSS global, no inline styles, no CSS modules, no Tailwind (D6).
Add comment header `/* ChangePasswordPage (force-change gate) */` before the block.

**CSS classes to add:**
```css
.force-change-page { }
.force-change-card { }
.force-change-title { }
.force-change-notice { }
.force-change-form { }
.force-change-submit { }
```

**Review criteria**
- [ ] All 6 classes present
- [ ] No overlap with `.change-password-*` or `.reset-password-*` namespaces
- [ ] Appended at end of file
- [ ] No inline styles (D6)

**Test command**
```bash
npm test -- --testPathPattern=ChangePasswordPage --passWithNoTests
```

**Parallel**: safe
**Size estimate**: ~15 lines

---
<!-- End T001 -->

### T002: Add i18n keys for force-change to all 4 lang files

**Status**: DONE

**File(s)**
- `frontend/src/i18n/ja.json` — modify: add `auth.force_change.*` keys
- `frontend/src/i18n/en.json` — modify: add `auth.force_change.*` keys
- `frontend/src/i18n/vi.json` — modify: add `auth.force_change.*` keys
- `frontend/src/i18n/ko.json` — modify: add `auth.force_change.*` keys

**Change description**
Add the following keys under `auth.force_change` in each language file:
- `title` — page heading
- `notice` — explanation text (admin set your password; you must change it now)
- `new_password` — label
- `confirm_password` — label
- `submit` — button text
- `error.mismatch` — new ≠ confirm
- `error.too_short` — length < 8
- `success` — redirect toast/message

**Review criteria**
- [ ] All 8 keys present in all 4 files
- [ ] Keys under `auth.force_change` namespace
- [ ] Notice text clearly explains the forced-change reason (not generic)

**Test command**
```bash
npm test -- --testPathPattern=ChangePasswordPage --passWithNoTests
```

**Parallel**: safe
**Size estimate**: ~32 lines (8 keys × 4 files)

---
<!-- End T002 -->

### T003: Extend authStore to store mustChangePassword from login

**Status**: DONE

**File(s)**
- `frontend/src/store/authStore.ts` — modify: add `mustChangePassword` field + populate from login response

**Change description**
Add `mustChangePassword: boolean` state field to `authStore`.
On login success: read `must_change_password` from the login response (Q1 resolved — it's in the token response) and set `authStore.mustChangePassword`.
On logout or successful self-service password change: set `mustChangePassword = false`.
Zustand or existing store pattern — match whatever pattern the store currently uses.

**Review criteria**
- [ ] `mustChangePassword` field initialized `false`
- [ ] Set from login response `must_change_password` field
- [ ] Cleared on logout
- [ ] Cleared after successful PATCH /v1/users/me/password (so gate deactivates)
- [ ] OIDC login: backend returns `false` → field is `false` → gate never activates
- [ ] Tech debt note: `authStore.password` stored for silent pass — already documented as DEFERRED-SEC-001

**Test command**
```bash
npm test -- --testPathPattern=authStore -v
```

**Parallel**: safe
**Size estimate**: ~15 lines changed

---
<!-- End T003 -->

### T004: Create ChangePasswordPage.tsx (force-change full-page form)

**Status**: DONE

**File(s)**
- `frontend/src/pages/ChangePasswordPage.tsx` — create

**Change description**
Full-page layout (not a modal). Uses `.force-change-page` / `.force-change-card` CSS classes.
Fields: New Password, Confirm Password only — NO "Current Password" field shown (D5 / Q2).
Behavior:
- Silent pass: reads `authStore.password` as `current_password` in the PATCH body (user unaware)
- Client validate: new === confirm, len ≥ 8 → show `.force-change-notice` style error
- On submit: `PATCH /v1/users/me/password` with `{current_password: authStore.password, new_password}`
- On success: set `authStore.mustChangePassword = false` → navigate to `/`
- No Cancel/Skip button — force is mandatory (D5)
- Loading state on `.force-change-submit` button

**Review criteria**
- [ ] No current-password field visible to user (D5)
- [ ] `authStore.password` used silently in request body (Q2)
- [ ] No cancel/skip/dismiss path (D5)
- [ ] All `.force-change-*` CSS classes used
- [ ] i18n keys via `useTranslation()`
- [ ] `mustChangePassword = false` set in store on success before redirect
- [ ] Test: renders without current-password field; submit success; mismatch error; too-short error

**Test command**
```bash
npm test -- --testPathPattern=ChangePasswordPage -v
```

**Parallel**: after:T001,T002,T003
**Size estimate**: ~45 lines

---
<!-- End T004 -->

### T005: Add /change-password route + ProtectedRoute gate in App.tsx

**Status**: DONE

**File(s)**
- `frontend/src/App.tsx` — modify: add `/change-password` route
- `frontend/src/components/auth/ProtectedRoute.tsx` — modify lines 11–17: add mustChangePassword redirect

**Change description**

**App.tsx:**
Add `<Route path="/change-password" element={<ChangePasswordPage />}>` to the router.
This route must NOT be wrapped in the gate guard (the gate IS the guard — no circular redirect).

**ProtectedRoute.tsx:**
Extend the existing guard: after auth check, if `authStore.mustChangePassword === true` and `location.pathname !== "/change-password"` → `<Navigate to="/change-password" replace>`.
Order: auth check first, then mustChangePassword check.

**Review criteria**
- [ ] `/change-password` route added to App.tsx
- [ ] `ProtectedRoute` guard redirects to `/change-password` when `mustChangePassword === true`
- [ ] Guard does NOT redirect when already on `/change-password` (no loop)
- [ ] OIDC users: `mustChangePassword` always `false` → guard never fires
- [ ] Test: unauthenticated user still redirected to login (existing behavior preserved)
- [ ] Test: authenticated user with `mustChangePassword=true` redirected to `/change-password`
- [ ] Test: authenticated user with `mustChangePassword=false` passes through normally

**Test command**
```bash
npm test -- --testPathPattern=ChangePasswordPage|ProtectedRoute -v
```

**Parallel**: after:T004
**Size estimate**: ~20 lines changed (split across 2 files)

---
<!-- End T005 -->
