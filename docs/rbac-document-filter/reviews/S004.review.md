# Code Review: S004 — ORM nullable user_group_id
Level: full | Date: 2026-04-03 | Reviewer: Claude Opus

---

## Task Review Criteria

### T001 (ORM model changes)
- [x] `document.py`: `Mapped[Optional[int]]` + `nullable=True` on `user_group_id` (L21)
- [x] `embedding.py`: `Mapped[Optional[int]]` + `nullable=True` on `user_group_id` (L23)
- [x] Both files have `from typing import Optional` import (L6 in both)
- [x] FK `ForeignKey("user_groups.id")` on `document.user_group_id` preserved (L21)
- [x] Comment `# denormalized, no FK (R001) — NULL = public (D01)` on `embedding.py` (L23)
- [x] No other columns modified — scope is ONLY `user_group_id` nullability

### T002 (test_models.py additions)
- [x] `test_document_user_group_id_accepts_null` present and passes (AC3) — L167
- [x] `test_embedding_user_group_id_accepts_null` present and passes (AC3) — L180
- [x] `test_documents_user_group_id_is_nullable` inspector check present (AC1) — L198
- [x] `test_embeddings_user_group_id_is_nullable` inspector check present (AC2) — L205
- [x] Existing `test_document_insert` still passes (non-NULL case still valid) — L108 unchanged
- [x] Existing `test_embedding_fk_to_documents` still passes (FK not broken) — L84 unchanged
- [x] No existing tests deleted — only additions (4 new tests appended after L162)

---

## Full Checks
- [x] No magic numbers or hardcoded values introduced
- [x] No commented-out dead code
- [x] No CONSTITUTION violations
- [x] Decision D01 referenced in updated comment (`embedding.py` L23: `NULL = public (D01)`)

---

## Issues Found

### BLOCKER
None.

### WARNING
None.

### NOTE
- Minor style inconsistency: `document.py` L24 uses `Mapped[str | None]` (PEP 604 union) for `content_fts`, while the S004 change uses `Mapped[Optional[int]]` (typing.Optional) for `user_group_id`. Both are valid in SQLAlchemy 2.x and Python 3.10+. The task file explicitly permits either form. Not actionable — cosmetic only.

---

## Verdict
[x] APPROVED  [ ] CHANGES REQUIRED  [ ] BLOCKED

All T001 and T002 review criteria pass. Model changes are minimal and scoped correctly. Tests cover both NULL acceptance (roundtrip flush) and schema introspection (inspector nullable check). No rule violations detected (R001, R002, S001 all clean). FK on `document.user_group_id` preserved. No existing tests removed or modified.
