## Code Review: S002 — Retriever RBAC filter (dense + BM25)
Level: security | Date: 2026-04-03 | Reviewer: Claude Opus

### Task Review Criteria — T001
- [x] `backend/rag/__init__.py` exists — re-exports `retrieve`, `RetrievedDocument`, `QueryTimeoutError`
- [x] `RetrievedDocument` has all fields: `doc_id`, `chunk_index`, `score`, `user_group_id`, `content`
- [x] `user_group_id: int | None` (nullable — `None = public document` comment present)
- [x] `retrieve()` signature matches spec: `query_embedding`, `user_group_ids`, `top_k`, `session` keyword-only, `bm25_query`
- [x] Smoke tests pass — `test_imports`, `test_retrieved_document_fields`, `test_retrieved_document_with_group`, `test_retrieve_is_callable`

### Task Review Criteria — T002
- [x] `WHERE (e.user_group_id = ANY(:group_ids) OR e.user_group_id IS NULL)` present verbatim on embeddings
- [x] Filter BEFORE `ORDER BY distance` and `LIMIT :top_k`
- [x] `text().bindparams()` used — zero f-string SQL
- [x] `group_ids=[]` works — PostgreSQL `ANY([])` = FALSE; OR IS NULL still matches public docs
- [x] Test: group match (`test_group1_retrieves_own_doc_not_group2`)
- [x] Test: empty groups + public-only (`test_empty_group_ids_returns_only_public`)

### Task Review Criteria — T003
- [x] `WHERE (d.user_group_id = ANY(:group_ids) OR d.user_group_id IS NULL)` on `documents` table
- [x] Filter BEFORE `ORDER BY rank DESC` and `LIMIT :top_k`
- [x] `text().bindparams()` — zero f-string SQL
- [x] Test: group match (`test_group1_retrieves_own_doc_not_group2`)
- [x] Test: empty groups (`test_empty_group_ids_returns_only_public`)

### Task Review Criteria — T004
- [x] `asyncio.gather` used for concurrent dense + BM25 (line 132)
- [x] `asyncio.wait_for(_inner(), timeout=1.8)` wraps full call (line 142)
- [x] `QueryTimeoutError` raised on timeout — `asyncio.TimeoutError` caught and re-raised as custom type (line 143-144)
- [x] Merge uses `RAG_DENSE_WEIGHT` + `RAG_BM25_WEIGHT` from env (lines 16-17, used in `_merge` lines 107-109)
- [x] `user_group_ids=[]` returns `[]` when no public docs, no error (`test_empty_groups_returns_empty_when_no_public_docs`)
- [x] Test: hybrid deduplication (`test_merge_deduplicates_by_doc_id`)
- [x] Test: empty groups → only NULL-group docs (`test_empty_group_ids_returns_only_public` in both TestDenseRBAC and TestBM25RBAC)
- [x] Test: `QueryTimeoutError` raised on timeout (`test_query_timeout_error_raised_on_timeout`)

### Full Level Checks
- [x] Error handling: `asyncio.TimeoutError` caught and wrapped as `QueryTimeoutError` — external DB calls go through session.execute (caller responsible for session error handling)
- [x] No magic numbers — `1.8` timeout corresponds to 1800ms SLA (R007/P001), weights from env vars
- [x] No commented-out dead code

### Security Checks
- [x] **R001**: RBAC WHERE clause present in both `_dense_search` (embeddings) and `_bm25_search` (documents), applied BEFORE ORDER BY/LIMIT — no Python post-filter
- [x] **R002**: `RetrievedDocument` fields are `doc_id`, `chunk_index`, `score`, `user_group_id`, `content` — no PII (email, name, etc.)
- [x] **S001**: Zero string interpolation in SQL — all params via `text().bindparams()` with named `bindparam()` objects
- [x] **S003**: No unvalidated user input directly in SQL — `query_embedding`, `user_group_ids`, `bm25_query` all bound via `bindparams()`
- [x] **S005**: No hardcoded secrets or URLs — weights read from `os.getenv()` with safe numeric defaults
- [x] **R006**: N/A — `audit_log.write()` is the responsibility of the API layer (`backend/api/routes/query.py`), not the retriever. The retriever is a pure search function called by the route handler which owns audit logging.

### Issues Found
#### BLOCKER — Must fix before merge
None

#### WARNING — Should fix
1. **Timeout test is indirect** (`tests/rag/test_retriever_rbac.py:266-296`): `TestTimeout.test_query_timeout_error_raised_on_timeout` replaces `retrieve` entirely via monkeypatch and then calls the local `_patched_retrieve` — it never exercises the actual `retrieve()` function's timeout path. The test proves the pattern works but does not validate the production code path. Consider a simpler approach: monkeypatch `_dense_search` to be slow and call the real `retrieve()` with a patched short timeout. Low severity because the production code at lines 141-144 is straightforward and correct.

### Verdict
[x] APPROVED / [ ] CHANGES_REQUIRED / [ ] BLOCKED
Blockers: 0
Warnings: 1 (timeout test indirection — low severity)
