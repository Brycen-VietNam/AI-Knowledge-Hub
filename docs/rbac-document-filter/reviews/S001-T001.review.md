## Code Review: S001/T001 — Migration 005 DROP NOT NULL
Level: quick | Date: 2026-04-03 | Reviewer: Claude Opus

### Task Review Criteria

- [x] File named `005_nullable_user_group_id.sql` (sequential after 004)
- [x] Header comment block: migration name, spec reference, requires: migrations 001-004
  - Lines 1-4 include migration name, spec ref, task ref, and requires clause. Lines 5-8 add decision references and operational notes — thorough.
- [x] FORWARD has exactly 4 statements: 2 x ALTER + 2 x CREATE INDEX
  - Line 15: `ALTER TABLE documents ALTER COLUMN user_group_id DROP NOT NULL`
  - Line 18: `ALTER TABLE embeddings ALTER COLUMN user_group_id DROP NOT NULL`
  - Line 22: `CREATE INDEX IF NOT EXISTS idx_documents_public ...`
  - Line 26: `CREATE INDEX IF NOT EXISTS idx_embeddings_public ...`
- [x] `idx_embeddings_public` indexes `doc_id` (not `id`) — confirmed at line 26: `ON embeddings(doc_id)`
- [x] ROLLBACK uses `(SELECT MIN(id) FROM user_groups)` not hardcoded `1` — confirmed at lines 35-36
- [x] ROLLBACK updates NULLs BEFORE restoring NOT NULL — confirmed: UPDATE statements (lines 35-36) precede SET NOT NULL (lines 37-38)
- [x] FK `documents.user_group_id -> user_groups.id` NOT dropped — no DROP CONSTRAINT in FORWARD section
- [x] Rule satisfied: ARCH.md A006 — numbered migration file with commented rollback section at bottom

### Security Checks (DB scope)

| Rule | Status | Notes |
|------|--------|-------|
| R002 — No PII in vector metadata | PASS | Migration only alters nullable constraint and adds partial indexes. No metadata schema change. |
| S001 — No SQL string interpolation | PASS | Pure DDL statements. No dynamic SQL or string interpolation. |
| S005 — No hardcoded secrets | PASS | No connection strings, passwords, or secrets in file. |

### Pattern Consistency with 004

- Header block follows same convention: `-- Migration:`, `-- Spec:`, `-- Task:`, `-- Requires:`
- `IF NOT EXISTS` used on CREATE INDEX (consistent with 004 using `IF NOT EXISTS` on CREATE TABLE/INDEX)
- ROLLBACK section uses `IF EXISTS` guards on DROP (consistent with 004)
- Comments explain intent for each statement

### Issues Found

None.

### Verdict

- [x] APPROVED
- [ ] CHANGES_REQUIRED
- [ ] REJECTED
