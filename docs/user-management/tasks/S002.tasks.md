# Tasks: user-management / S002 — DELETE /v1/admin/users/{user_id} (Delete User)
Created: 2026-04-21 | Agent: api-agent | Status: TODO

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S002: DELETE /v1/admin/users/{user_id} |
| Total tasks | 3 |
| Parallel groups | G1: [T001], G2: [T002], G3: [T003] |
| Critical path | T001 → T002 → T003 |
| Agent | api-agent |
| Est. session | 1 (same session as S001) |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | DELETE handler — existence check + cascade deletes | TODO | after:S001 | T002 |
| T002 | Transaction rollback + 200 response | TODO | after:T001 | T003 |
| T003 | Tests: delete_user happy path + error cases | TODO | after:T001 | — |

---

## LAYER 2 — Task Detail

### T001: DELETE handler — existence check + cascade deletes
**Status**: TODO

**File(s)**
- `backend/api/routes/admin.py` — modify: add `admin_delete_user` route handler

**Change description**
Add `@router.delete("/v1/admin/users/{user_id}", dependencies=[Depends(require_admin)])`.
Handler: SELECT user by id — 404 `NOT_FOUND` if missing (A005 shape).
Inside transaction: DELETE from `api_keys WHERE user_id = :uid`; DELETE from
`user_group_memberships WHERE user_id = :uid`; DELETE from `users WHERE id = :uid`.
All three DELETEs use `text().bindparams()`. No pre-delete of `audit_logs` needed
— FK is `ON DELETE SET NULL` (migration 011).

**Review criteria**
- [ ] Route has `dependencies=[Depends(require_admin)]` (R003)
- [ ] 404 returned before any DELETE if user not found (A005)
- [ ] Delete order: api_keys → user_group_memberships → users (FK safety)
- [ ] All DELETE SQL via `text().bindparams()` — no f-string (S001)
- [ ] `audit_logs.user_id` NOT deleted — FK set null by DB (migration 011)

**Test command**
```bash
pytest tests/api/test_admin_users.py::test_delete_user_success -v
```

**Parallel**: after:S001-complete
**Size estimate**: ~35 lines

---

### T002: Transaction rollback + 200 response
**Status**: TODO

**File(s)**
- `backend/api/routes/admin.py` — modify: wrap `admin_delete_user` body in explicit transaction + return 200

**Change description**
Wrap the three DELETE statements in `async with db.begin()` (or equivalent transaction
block matching existing pattern in admin.py). On any DB exception: rollback + return 500 A005.
On success: `await db.commit()` + return `JSONResponse({"deleted": str(user_id)})`.

**Review criteria**
- [ ] All DELETEs are inside one transaction (atomic)
- [ ] DB error triggers rollback; 500 returned in A005 shape
- [ ] Success response: `200 {"deleted": "<uuid>"}` exactly (not 204, not empty)
- [ ] `user_id` in response is the same value passed in path param

**Test command**
```bash
pytest tests/api/test_admin_users.py::test_delete_user_success tests/api/test_admin_users.py::test_delete_user_db_error -v
```

**Parallel**: after:T001
**Size estimate**: ~15 lines (wrapping existing T001 code)

---

### T003: Tests — full S002 coverage
**Status**: TODO

**File(s)**
- `tests/api/test_admin_users.py` — modify: add S002 test cases to existing module

**Change description**
Append S002 test cases to `test_admin_users.py` (same file as S001 tests):
- `test_delete_user_success` — 200, `{"deleted": user_id}` shape
- `test_delete_user_not_found` — 404 NOT_FOUND A005 shape
- `test_delete_user_non_admin` — 403 FORBIDDEN
- `test_delete_user_cascades_api_keys` — api_keys rows gone after delete
- `test_delete_user_cascades_memberships` — group memberships gone after delete
- `test_delete_user_preserves_audit_logs` — audit_log rows remain (user_id = NULL)

**Review criteria**
- [ ] All 7 ACs have at least 1 test
- [ ] Cascade tests verify DB state (not just response code)
- [ ] Audit log preservation test confirms `user_id IS NULL` — not deleted
- [ ] 404 body matches A005 shape exactly

**Test command**
```bash
pytest tests/api/test_admin_users.py -v -k "delete"
```

**Parallel**: after:T001
**Size estimate**: ~60 lines
