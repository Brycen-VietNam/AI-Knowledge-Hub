# Tasks: user-management / S006 — UserFormModal.tsx (Create User)
Created: 2026-04-21 | Agent: frontend-agent | Status: TODO

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S006: UserFormModal component (NEW) |
| Total tasks | 4 |
| Parallel groups | G1: [T001], G2: [T002], G3: [T003, T004 — same file, seq] |
| Critical path | T001 → T002 → T003 → T004 |
| Agent | frontend-agent |
| Est. session | 1 (Session D, parallel with S007) |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Component scaffold + form fields + submit | TODO | after:S005 | T002 |
| T002 | Generate-password button + show/hide toggle | TODO | after:T001 | T003 |
| T003 | Error handling — 409, 422 inline errors | TODO | after:T002 | T004 |
| T004 | Tests — full AC coverage | TODO | after:T001 | — |

---

## LAYER 2 — Task Detail

### T001: Component scaffold + form fields + submit
**Status**: TODO

**File(s)**
- `frontend/admin-spa/src/components/UserFormModal.tsx` — create: new component
- `frontend/admin-spa/src/components/UserFormModal.test.tsx` — create: test file

**Change description**
Create `UserFormModal.tsx` following `GroupFormModal.tsx` pattern (read that file first).
Props: `{ onSave: (user: UserItem) => void; onClose: () => void; groups: GroupItem[] }`.
Fields: `sub` (text, required, pattern `^[a-zA-Z0-9_.@-]+$`), `email` (email, optional),
`display_name` (text, optional), `password` (type="password", min 12), Groups (checkboxes from props).
Submit: disabled when `isSubmitting`; calls `createUser(payload)` → `onSave(result)` → `onClose()`.
All visible strings via `t()` — no hardcoded English.

**Review criteria**
- [ ] Password field uses `type="password"` (NOT `type="text"`) by default
- [ ] `sub` field has pattern validation matching spec `^[a-zA-Z0-9_.@-]+$`
- [ ] Submit button disabled when `isSubmitting === true`
- [ ] `t()` used for all UI strings — no English string literals in JSX
- [ ] Props interface matches spec: `onSave`, `onClose`, `groups`
- [ ] `group_ids` sent as number[] to `createUser` (AC2)

**Test command**
```bash
cd frontend/admin-spa && npx vitest run src/components/UserFormModal.test.tsx
```

**Parallel**: after:S005-complete
**Size estimate**: ~70 lines (component)

---

### T002: Generate-password button + show/hide toggle
**Status**: TODO

**File(s)**
- `frontend/admin-spa/src/components/UserFormModal.tsx` — modify: add generate + show/hide

**Change description**
Add "Generate password" button next to password field.
On click: `crypto.getRandomValues(new Uint8Array(12))` → map to alphanumeric charset → 16-char string
→ set password field value + temporarily reveal (set `showPassword = true`).
Add eye-icon toggle button: `showPassword` state toggles field `type` between `"password"` and `"text"`.
(This is the only case where type="text" is allowed — user explicitly triggered it.)

**Review criteria**
- [ ] Uses `crypto.getRandomValues` — NOT `Math.random()` (S005 — crypto-safe)
- [ ] Generated password is 16 chars alphanumeric (AC3 spec)
- [ ] Reveal is via state toggle — NOT `innerHTML` manipulation
- [ ] Show/hide toggle is in the same form, not a separate component
- [ ] Default state: `showPassword = false` (type="password")

**Test command**
```bash
cd frontend/admin-spa && npx vitest run src/components/UserFormModal.test.tsx -t "generate"
```

**Parallel**: after:T001
**Size estimate**: ~20 lines delta

---

### T003: Error handling — 409, 422 inline errors
**Status**: TODO

**File(s)**
- `frontend/admin-spa/src/components/UserFormModal.tsx` — modify: add error state + display

**Change description**
Add `error: string | null` state. In `createUser` catch block:
- HTTP 409 → `setError(t('user.create.error.duplicate_sub'))` ("Username already taken")
- HTTP 422 → `setError(serverMessage)` (use error message from response body)
- Other → `setError(t('common.error.unexpected'))`
Display error below form fields as inline `<p className="form-error">`. Modal stays open on error (AC5, AC6).
Clear error on re-submit.

**Review criteria**
- [ ] 409 → inline error, modal does NOT close (AC5)
- [ ] 422 → inline error with server message, modal does NOT close (AC6)
- [ ] Error cleared when user resubmits
- [ ] Error text rendered via `t()` key (not hardcoded English for 409)
- [ ] No `console.error` left in production code path

**Test command**
```bash
cd frontend/admin-spa && npx vitest run src/components/UserFormModal.test.tsx -t "error"
```

**Parallel**: after:T002
**Size estimate**: ~20 lines delta

---

### T004: Tests — full S006 AC coverage
**Status**: TODO

**File(s)**
- `frontend/admin-spa/src/components/UserFormModal.test.tsx` — modify: full test suite

**Change description**
Tests (using vitest + @testing-library/react):
- `renders all required fields` — sub, email, display_name, password, groups checkboxes
- `submit disabled while submitting` — loading state disables button
- `generate password fills field and reveals it` — button click → field gets value + visible
- `409 shows inline error, modal stays open` — mock createUser → 409 → error text visible
- `422 shows server message, modal stays open` — mock createUser → 422 → error from response
- `success calls onSave and onClose` — mock createUser success → both callbacks fired
- `i18n: no hardcoded English in JSX` — verify t() is called for button/label strings

**Review criteria**
- [ ] All 9 ACs have at least 1 test
- [ ] `createUser` is mocked (not real HTTP) in all tests
- [ ] Modal-stays-open verified by asserting modal is still in DOM after error
- [ ] `onSave` callback is asserted with the returned user object

**Test command**
```bash
cd frontend/admin-spa && npx vitest run src/components/UserFormModal.test.tsx
```

**Parallel**: after:T001 (can start writing tests in parallel with T002, T003)
**Size estimate**: ~80 lines
