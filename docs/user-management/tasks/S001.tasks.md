# Tasks: user-management / S001 — POST /v1/admin/users (Create User)
Created: 2026-04-21 | Agent: api-agent | Status: DONE ✅

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S001: POST /v1/admin/users |
| Total tasks | 4 |
| Parallel groups | G1: [T001], G2: [T002, T003 seq], G3: [T004] |
| Critical path | T001 → T002 → T003 → T004 |
| Agent | api-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | UserCreate Pydantic model | DONE ✅ | safe | T002 |
| T002 | POST /v1/admin/users handler (duplicate check + insert) | DONE ✅ | after:T001 | T003 |
| T003 | Group membership insert in same transaction | DONE ✅ | after:T002 | T004 |
| T004 | Tests: create_user happy path + error cases | DONE ✅ | after:T001 | — |

---

## LAYER 2 — Task Detail

### T001: UserCreate Pydantic model
**Status**: DONE ✅

**File(s)**
- `backend/api/routes/admin.py` — modify: add `UserCreate` model class + imports

**Change description**
Add `UserCreate` Pydantic model and required imports (`Optional`, `List`, `EmailStr`, `passlib`).
Model fields: `sub` (str, 3–200, pattern `^[a-zA-Z0-9_.@-]+$`), `email` (Optional[EmailStr]),
`display_name` (Optional[str], max 200), `password` (str, min 12), `group_ids` (List[int] default []).
Place after existing imports block, before route handlers.

**Review criteria**
- [ ] Pattern `^[a-zA-Z0-9_.@-]+$` is on `sub` field
- [ ] `password` min_length=12 enforced by Pydantic
- [ ] `email` uses `EmailStr` (pydantic[email] / email-validator)
- [ ] `group_ids` default is `[]` (not mutable default — use `Field(default_factory=list)`)
- [ ] Rule S003: no unvalidated fields

**Test command**
```bash
pytest tests/api/test_admin_users.py::test_user_create_model_validation -v
```

**Parallel**: safe
**Size estimate**: ~20 lines

---

### T002: POST /v1/admin/users handler — auth, duplicate check, bcrypt, INSERT user
**Status**: DONE ✅

**File(s)**
- `backend/api/routes/admin.py` — modify: add `admin_create_user` route handler

**Change description**
Add `@router.post("/v1/admin/users", dependencies=[Depends(require_admin)], status_code=201)`.
Handler: strip whitespace from string fields; query for existing `sub` → 409 `SUB_CONFLICT`;
bcrypt hash password (rounds=12 via passlib); INSERT into `users` via `text().bindparams()`;
return 201 JSON with `{id, sub, email, display_name, is_active}`. No group return yet (T003 adds that).

**Review criteria**
- [ ] Route has `dependencies=[Depends(require_admin)]` (R003)
- [ ] Route path starts with `/v1/admin/` (R004)
- [ ] Duplicate `sub` → 409 `SUB_CONFLICT` in A005 shape
- [ ] `password` hashed with bcrypt rounds=12; plaintext never logged or stored (S005)
- [ ] All INSERT values use `text().bindparams()` — no f-string (S001)
- [ ] Strip control chars from `sub`, `display_name` fields (S003)
- [ ] DB error → rollback; 500 in A005 shape

**Test command**
```bash
pytest tests/api/test_admin_users.py::test_create_user_success tests/api/test_admin_users.py::test_create_user_duplicate_sub -v
```

**Parallel**: after:T001
**Size estimate**: ~45 lines

---

### T003: Group membership insert in same transaction
**Status**: DONE ✅

**File(s)**
- `backend/api/routes/admin.py` — modify: extend `admin_create_user` to INSERT memberships + return groups

**Change description**
Inside the same transaction as user INSERT (T002), if `group_ids` is non-empty:
INSERT into `user_group_memberships (user_id, group_id)` for each id, using
`ON CONFLICT DO NOTHING`. After commit, SELECT groups for the user and include
`{"groups": [...]}` in the 201 response. (P004: single IN query, not N+1.)

**Review criteria**
- [ ] Membership INSERT is in the same transaction as user INSERT
- [ ] Uses `ON CONFLICT DO NOTHING` (no error on duplicate)
- [ ] Group fetch is a single SELECT WHERE `group_id = ANY(...)` — not N+1 (P004)
- [ ] SQL parameterized — group_ids passed as array param (S001)
- [ ] Response 201 includes `groups` list

**Test command**
```bash
pytest tests/api/test_admin_users.py::test_create_user_with_groups tests/api/test_admin_users.py::test_create_user_invalid_group_ids -v
```

**Parallel**: after:T002
**Size estimate**: ~25 lines

---

### T004: Tests — full S001 coverage
**Status**: DONE ✅

**File(s)**
- `tests/api/test_admin_users.py` — create: full test module for S001 AC1–AC10

**Change description**
TDD test file covering all 10 ACs. Tests to include:
- `test_user_create_model_validation` — invalid pattern, too-short password, bad email
- `test_create_user_success` — 201, response shape, bcrypt hash not in response
- `test_create_user_duplicate_sub` — 409 SUB_CONFLICT shape (A005)
- `test_create_user_with_groups` — membership created, groups in response
- `test_create_user_non_admin` — 403 FORBIDDEN
- `test_create_user_strips_whitespace` — leading/trailing spaces stripped
- `test_create_user_invalid_group_ids` — unknown group ids handled gracefully

**Review criteria**
- [ ] All 10 ACs have at least 1 test
- [ ] No plaintext password appears in assertions
- [ ] 409 response body matches A005 shape exactly
- [ ] Tests use `async def` + `pytest-asyncio` (consistent with existing test suite)

**Test command**
```bash
pytest tests/api/test_admin_users.py -v
```

**Parallel**: after:T001
**Size estimate**: ~80 lines (test file)
