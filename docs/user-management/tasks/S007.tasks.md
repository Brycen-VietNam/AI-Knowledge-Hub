# Tasks: user-management / S007 — ApiKeyPanel.tsx (per-user key management)
Created: 2026-04-21 | Agent: frontend-agent | Status: TODO

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S007: ApiKeyPanel component (NEW) |
| Total tasks | 4 |
| Parallel groups | G1: [T001], G2: [T002], G3: [T003], G4: [T004] |
| Critical path | T001 → T002 → T003 → T004 |
| Agent | frontend-agent |
| Est. session | 1 (Session D, parallel with S006) |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Component scaffold + key list display | TODO | after:S005 | T002 |
| T002 | Generate key flow + one-time dialog | TODO | after:T001 | T003 |
| T003 | Revoke key flow via DeleteConfirmDialog | TODO | after:T001 | T004 |
| T004 | Tests — full AC coverage | TODO | after:T001 | — |

---

## LAYER 2 — Task Detail

### T001: Component scaffold + key list display
**Status**: TODO

**File(s)**
- `frontend/admin-spa/src/components/ApiKeyPanel.tsx` — create: new component
- `frontend/admin-spa/src/components/ApiKeyPanel.test.tsx` — create: test file

**Change description**
Create `ApiKeyPanel.tsx`. Props: `{ userId: string }`.
On mount: call `listApiKeys(userId)` → store in `keys: ApiKeyItem[]` state.
Render list: each row shows `key_prefix`, `name`, `created_at`. No `key_hash` shown — not even in data.
Show "Generate Key" button and "Revoke" button per row. All strings via `t()`.
Handle loading state while fetching.

**Review criteria**
- [ ] Props: `{ userId: string }` only (AC7)
- [ ] `key_hash` is NOT rendered — not in DOM, not in state derived from `ApiKeyItem`
- [ ] `listApiKeys` called on mount (useEffect with userId dep)
- [ ] Loading state visible while fetching (spinner or skeleton)
- [ ] All strings via `t()` (AC8)

**Test command**
```bash
cd frontend/admin-spa && npx vitest run src/components/ApiKeyPanel.test.tsx -t "list"
```

**Parallel**: after:S005-complete
**Size estimate**: ~50 lines (component scaffold)

---

### T002: Generate key flow + one-time dialog
**Status**: TODO

**File(s)**
- `frontend/admin-spa/src/components/ApiKeyPanel.tsx` — modify: add generate key flow + dialog

**Change description**
Add state: `generatedKey: string | null`, `keyName: string`.
"Generate Key" button → show name input → on confirm: call `generateApiKey(userId, keyName)` →
set `generatedKey = result.key` → add new `ApiKeyItem` to list (from response minus `key` field).
One-time dialog (shown when `generatedKey !== null`):
- Shows plaintext key in a `<code>` block
- Copy-to-clipboard button (navigator.clipboard.writeText)
- "I've copied it" dismiss button → `setGeneratedKey(null)`
- Warning message: "Copy this key now — it won't be shown again" (via `t()`)

**Review criteria**
- [ ] Plaintext key shown ONCE in dialog — dismissed state clears it from component state
- [ ] Copy-to-clipboard uses `navigator.clipboard.writeText` (not execCommand)
- [ ] After dismiss: `generatedKey` state set to `null` — key no longer accessible
- [ ] New key item appended to list after generation (no full refetch required)
- [ ] "Won't be shown again" warning present (AC3)

**Test command**
```bash
cd frontend/admin-spa && npx vitest run src/components/ApiKeyPanel.test.tsx -t "generate"
```

**Parallel**: after:T001
**Size estimate**: ~35 lines delta

---

### T003: Revoke key flow via DeleteConfirmDialog
**Status**: TODO

**File(s)**
- `frontend/admin-spa/src/components/ApiKeyPanel.tsx` — modify: add revoke flow

**Change description**
Per-row "Revoke" button → set `revokingKeyId: string | null` state.
When `revokingKeyId` is set: render `<DeleteConfirmDialog>` (read existing component props first).
On confirm: call `revokeApiKey(userId, revokingKeyId)` → remove key from `keys` state (filter).
On cancel: `setRevokingKeyId(null)`. Error handling: show inline error if revoke fails.

**Review criteria**
- [ ] Uses existing `DeleteConfirmDialog` — no new confirm dialog component (AC5)
- [ ] After revoke: key removed from list state (AC6)
- [ ] On cancel: list unchanged, dialog closed
- [ ] Error on revoke: inline error shown, key remains in list
- [ ] Read `DeleteConfirmDialog` props before implementing (risk: unknown API)

**Test command**
```bash
cd frontend/admin-spa && npx vitest run src/components/ApiKeyPanel.test.tsx -t "revoke"
```

**Parallel**: after:T001 (independent of T002)
**Size estimate**: ~25 lines delta

---

### T004: Tests — full S007 AC coverage
**Status**: TODO

**File(s)**
- `frontend/admin-spa/src/components/ApiKeyPanel.test.tsx` — modify: full test suite

**Change description**
Tests (vitest + @testing-library/react):
- `renders key list with prefix, name, created_at` — listApiKeys mocked, verify columns
- `does not render key_hash` — assert no hash-like string in DOM
- `generate key shows one-time dialog with key` — mock generateApiKey, click button, verify dialog
- `copy to clipboard button works` — mock clipboard API, assert writeText called
- `dismiss clears plaintext key` — after dismiss, dialog gone, key not in DOM
- `revoke opens confirm dialog` — click revoke, verify DeleteConfirmDialog renders
- `confirm revoke removes key from list` — mock revokeApiKey, confirm → key row gone

**Review criteria**
- [ ] All 8 ACs have at least 1 test
- [ ] `listApiKeys`, `generateApiKey`, `revokeApiKey` are all mocked
- [ ] `test_does_not_render_key_hash` — queries DOM for hash pattern (64-char hex) and asserts not found
- [ ] Clipboard mock: `vi.stubGlobal('navigator', {clipboard: {writeText: vi.fn()}})`

**Test command**
```bash
cd frontend/admin-spa && npx vitest run src/components/ApiKeyPanel.test.tsx
```

**Parallel**: after:T001 (can write tests in parallel with T002, T003)
**Size estimate**: ~80 lines
