# Analysis: S003/T001 — Write migration 003_add_fts_column.sql
Depth: shallow | Date: 2026-03-18

## Code Map

### backend/db/migrations/001_create_core_schema.sql (reference)
- `documents` table: columns `id, title, lang, user_group_id, created_at, updated_at`
- NO `content_fts` column — correct, added in migration 003
- Pattern: `IF NOT EXISTS` on all DDL (idempotent)

### backend/db/migrations/002_add_pgvector_hnsw.sql (reference)
- Pattern: `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`
- Pattern: `CREATE INDEX IF NOT EXISTS ... USING hnsw(...)`
- Pattern: `-- ROLLBACK` section at bottom with DROP statements

## Patterns to Follow
- Migration file header: `-- Migration: NNN_description.sql` + `-- Spec:` + `-- Task:` comment block
- All DDL: `IF NOT EXISTS` (idempotent, re-runnable)
- Rollback section: commented at bottom, DROP in reverse order
- GIN index syntax: `CREATE INDEX USING gin(column)`

## Conflicts / Gaps Found
None.

## TSVECTOR note
- `tsvector` is a PostgreSQL native type — no extension required
- `ALTER TABLE documents ADD COLUMN IF NOT EXISTS content_fts tsvector` — straightforward
- Column nullable by default (app-layer populated asynchronously, not at INSERT time)
- GIN index is the correct index type for tsvector FTS (B-tree cannot index tsvector)

## No Trigger Rule (D02)
- D02: CJK tokenization done in app layer, NOT pg trigger
- Migration must NOT create a trigger — only column + GIN index
- Add SQL comment: "Populated by application layer (rag-agent). PostgreSQL built-in parser does not support CJK."

## Test approach (TDD)
- T001 test is a Docker DB psql verify (not pytest): apply migration, then `\d documents | grep content_fts`
- No unit test file change — SQLite in-memory test for TSVECTOR column name added in T002

## Recommended Implementation
1. Header comment block (spec, task, decision refs)
2. `ALTER TABLE documents ADD COLUMN IF NOT EXISTS content_fts tsvector;`
3. SQL comment explaining app-layer population + D02
4. `CREATE INDEX IF NOT EXISTS idx_documents_fts ON documents USING gin(content_fts);`
5. Rollback section: `DROP INDEX IF EXISTS idx_documents_fts; ALTER TABLE documents DROP COLUMN IF EXISTS content_fts;`

## Token budget
Estimated implementation: ~18 lines SQL
Analysis saved: docs/tasks/db-schema-embeddings/S003-T001.analysis.md
