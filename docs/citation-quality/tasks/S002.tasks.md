# Tasks: S002 — Extend CitationObject + Wire `_parse_citations` into `query.py`
Feature: citation-quality | Agent: api-agent | Group: G2
Status: DONE ✅
Created: 2026-04-15 | Completed: 2026-04-15
Depends: S001 ✅

---

## Story
Extend `CitationObject` with `cited: bool = False` (backward-compatible additive field)
and wire `_parse_citations` into `query.py` so every `CitationObject` in the response
carries accurate `cited` provenance.

## Tasks

| ID | Task | Status |
|----|------|--------|
| T001 | Edit `citation.py` — add `cited: bool = False` | DONE ✅ |
| T002 | Edit `query.py` — import + `content_idx` + `cited_set` + update list comp | DONE ✅ |
| T003 | Smoke-check — 42 prior tests PASS (no regression) | DONE ✅ |

## Touch List
- `backend/api/models/citation.py` (MODIFIED — added `cited: bool = False`)
- `backend/api/routes/query.py` (MODIFIED — import, content_idx, cited_set, cited= in list comp)

## Test Command
```bash
pytest tests/api/test_citation.py tests/api/test_query.py -x --tb=short -q
```

## Result
```
42 passed in 3.21s
```

## Diff Summary
```diff
--- backend/api/models/citation.py
+++ backend/api/models/citation.py
@@ class CitationObject(BaseModel):
     lang: str
+    cited: bool = False  # D-CQ-01: True only when LLM emitted [N] marker for this doc

--- backend/api/routes/query.py
+++ backend/api/routes/query.py
@@ imports
+from backend.rag.citation_parser import _parse_citations

@@ citations list comp
+    content_idx = {id(d): i for i, d in enumerate(content_docs)}
+    cited_set = (
+        _parse_citations(llm_response.answer, len(content_docs))
+        if llm_response.inline_markers_present
+        else set()
+    )
     citations = [
         CitationObject(
             ...
+            cited=(content_idx.get(id(d), -1) in cited_set),
         )
         for d in docs
     ]
```

## Rules Satisfied
- A001: api-agent scope only — imports from `backend.rag` via defined interface ✅
- A002: dependency direction `api → rag` — no reverse ✅
- D-CQ-01: `cited: bool = False` default — zero consumer breakage ✅
- D-CQ-02: fast path when `inline_markers_present=False` ✅
- D-CQ-05: non-content docs (not in `content_docs`) always `cited=False` via `content_idx.get(id(d), -1)` ✅
