# Tasks: S005 — Login page + HistoryPanel + HistoryItem styling
Generated: 2026-04-17 | Story: frontend-theme | Agent: frontend-agent

---

## LAYER 1 — Task Summary

| Task | Title | Type | Dependencies | Est. Tokens | Status |
|------|-------|------|--------------|-------------|--------|
| T001 | Add login page + history panel CSS to `index.css` | implementation | None | 1.0k | TODO |
| T002 | Update `LoginPage.tsx` + `LoginForm.tsx` | implementation | after:T001 | 0.7k | TODO |
| T003 | Update `HistoryPanel.tsx` + `HistoryItem.tsx` | implementation | after:T001 | 0.6k | TODO |

**Parallel groups**: T002 and T003 can run in parallel after T001

**Total story tokens**: ~2.3k (within 2.4k plan estimate)

---

## LAYER 2 — Per-Task Definition

### T001: Add login page + history panel CSS to `index.css`

**Type**: implementation | **Parallel**: safe (no dependencies)

**Description**:
Append CSS classes for login page and history sidebar to `frontend/src/index.css`. Include:

**Login page:**
- `.login-page` — `<main>` layout: flex center, min-height 100vh, bg gradient backdrop
- `.login-card` — centered card: surface bg, border, shadow-lg, border-radius, max-width 380px, padding 32px
- `.login-brand` — brand strip at top of card: indigo gradient, padding, flex center, border-radius top
- `.login-brand-title` — Playfair Display, white, 18px
- `.login-brand-sub` — uppercase 10px, #a5b4fc
- `.login-body` — card content padding
- `.form-group` — label + input wrapper (margin-bottom)
- `.form-label` — label styling (12px, font-weight 600, text-2 color)
- `.form-input` — text input (border, border-radius, padding, width 100%, focus ring indigo)
- `.form-input:focus` — border-color var(--indigo), box-shadow 0 0 0 3px var(--indigo-light)
- `.btn-primary` — full-width submit button: indigo gradient, white, border-none, border-radius, padding 10px 16px, font-weight 600
- `.btn-primary:hover` — translateY(-1px) + shadow
- `.btn-primary:disabled` — opacity 0.5, cursor not-allowed
- `.login-error` — error alert (red-light bg, red border, red text, border-radius, padding)

**History panel:**
- `.history-panel` — sticky sidebar card (surface, border, radius, shadow, fit-content height)
- `.history-header` — header bar (surface-2 bg, border-bottom, flex space-between)
- `.history-title` — title text (13px, font-weight 700, flex gap 7px)
- `.btn-clear` — clear button (red-light bg, red color, no border, border-radius, padding)
- `.history-list` — list container (padding 8px, flex-col gap 2px)
- `.history-item` — row (flex, align-start, gap 8px, padding, border-radius, cursor pointer, hover surface-2)
- `.history-bullet` — dot (6×6px circle, border color, flex-shrink)
- `.history-content` — text block (flex 1, min-width 0)
- `.history-q` — query text (12.5px, font-weight 500, truncate with text-overflow)
- `.history-time` — timestamp (DM Mono 10.5px, text-3 color, margin-top 2px)

**Files touched**:
- **Modify**: `frontend/src/index.css` (append ~120 lines)

**Review criteria**:
- ✅ `.form-input:focus` has indigo focus ring (`box-shadow: 0 0 0 3px var(--indigo-light)`)
- ✅ `.login-card` max-width 380px, padding 32px
- ✅ `.history-time` uses `font-family: 'DM Mono', monospace`
- ✅ `.history-q` has `overflow: hidden; text-overflow: ellipsis; white-space: nowrap`
- ✅ Build clean

---

### T002: Update `LoginPage.tsx` + `LoginForm.tsx`

**Type**: implementation | **Parallel**: after:T001

**Description**:
Replace inline styles in LoginPage and bare elements in LoginForm with CSS class names.

`LoginPage.tsx`:
- `<main style={{ display: 'flex', ... }}>` → `<main className="login-page">`
- `<section>` → `<div className="login-card">`
- Add brand strip: `<div className="login-brand"><span className="login-brand-title">Knowledge Hub</span><span className="login-brand-sub">BRYSEN GROUP</span></div>`
- Add `<div className="login-body">` wrapper around `<LoginForm />`
- Remove `<h1>` (brand strip replaces it)

`LoginForm.tsx` — add classNames only (no logic changes):
- `<form>` → `<form className="login-form" ...>`
- each field `<div>` → `<div className="form-group">`
- `<label>` → add `className="form-label"`
- `<input>` → add `className="form-input"`
- `<button type="submit">` → add `className="btn-primary"`
- error `<div role="alert">` → `<div role="alert" className="login-error">`

**Files touched**:
- **Modify**: `frontend/src/pages/LoginPage.tsx`
- **Modify**: `frontend/src/components/auth/LoginForm.tsx`

**Acceptance criteria**:
1. No inline `style=` props remain in LoginPage.tsx
2. `role="alert"` preserved on error div
3. `aria-label="login-form"` preserved on form
4. All 6 LoginPage tests pass
5. `getByLabelText(/username/i)` and `getByLabelText(/password/i)` still find inputs (labels connected via `htmlFor`)

**Review criteria**:
- ✅ No `style={{` in LoginPage.tsx
- ✅ Brand strip with "Knowledge Hub" + "BRYSEN GROUP" present
- ✅ `role="alert"` on error element preserved
- ✅ All LoginPage tests green (6 tests)

---

### T003: Update `HistoryPanel.tsx` + `HistoryItem.tsx`

**Type**: implementation | **Parallel**: after:T001

**Description**:
Add CSS class names to HistoryPanel and HistoryItem. No logic changes.

`HistoryPanel.tsx`:
- `<aside>` → `<aside className="history-panel" ...>`
- `<h2>` + `<button>` → wrap in `<div className="history-header">`, `<span className="history-title">`, `<button className="btn-clear">`
- `<ul>` → `<div className="history-list">`
- Items map: keep `<HistoryItem>` unchanged

`HistoryItem.tsx`:
- `<li role="listitem" onClick=... style={{ cursor: 'pointer' }}>` → `<li role="listitem" onClick=... className="history-item">`
- Wrap contents in `<div className="history-bullet" />` + `<div className="history-content">`
- `<span data-testid="history-query">` → `<span data-testid="history-query" className="history-q">`
- `<time data-testid="history-time">` → `<time data-testid="history-time" className="history-time">`
- Remove `style={{ cursor: 'pointer' }}`

**Files touched**:
- **Modify**: `frontend/src/components/history/HistoryPanel.tsx`
- **Modify**: `frontend/src/components/history/HistoryItem.tsx`

**Acceptance criteria**:
1. `data-testid="history-query"` preserved
2. `data-testid="history-time"` preserved
3. `role="listitem"` preserved (HistoryItem test uses `getByRole('listitem')`)
4. No inline `style=` on HistoryItem
5. All 5 HistoryPanel + 5 HistoryItem tests pass

**Review criteria**:
- ✅ `role="listitem"` preserved on `<li>`
- ✅ `data-testid` attrs preserved
- ✅ No `style={{` in HistoryItem.tsx
- ✅ HistoryPanel uses `<aside>` (test: `getByRole('complementary')` matches `<aside>`)
- ✅ 5+5 = 10 history tests green

---

## ACs Traceability

| AC | Description | Task | Verified by |
|----|-------------|------|-------------|
| AC1 | LoginPage card layout (flex center, card, brand strip) | T001+T002 | Visual |
| AC2 | Brand strip: indigo gradient, "Knowledge Hub" / "BRYSEN GROUP" | T001+T002 | Visual |
| AC3 | LoginForm: input + button styled (border, focus ring, gradient) | T001+T002 | Visual + LoginPage tests |
| AC4 | HistoryPanel sticky sidebar card | T001+T003 | Visual |
| AC5 | HistoryItem clickable rows with hover (surface-2) | T001+T003 | Visual |
| AC6 | DM Mono timestamp on HistoryItem | T001+T003 | HistoryItem test |
| AC7 | All Tailwind/inline styles removed | T002+T003 | Code review |
| AC8 | Full test suite 208 tests pass | T002+T003 | `npm run test` |

---

## Context & Notes

**Design reference**: `docs/knowledge-hub.html`
- History sidebar: `.history-panel`, `.history-header`, `.history-item`, `.history-time` (lines 345–400)
- Login page: not in reference HTML (standalone card layout per plan spec)

**Test constraints**:
- `HistoryPanel.test.tsx`: `getByRole('complementary')` → must keep `<aside>`
- `HistoryItem.test.tsx`: `getByRole('listitem')` → must keep `<li role="listitem">`
- `LoginPage.test.tsx`: `getByLabelText(/username/i)` → `<label htmlFor="username">` must stay
