# Tasks: S003 — Search area + LanguageSelector styling
Generated: 2026-04-17 | Story: frontend-theme | Agent: frontend-agent

---

## LAYER 1 — Task Summary

| Task | Title | Type | Dependencies | Est. Tokens | Status |
|------|-------|------|--------------|-------------|--------|
| T001 | Add search area CSS classes to `index.css` | implementation | None | 0.8k | TODO |
| T002 | Apply CSS classNames to `SearchInput.tsx` | implementation | after:T001 | 0.8k | TODO |
| T003 | Apply CSS classNames to `LanguageSelector.tsx` | implementation | after:T001 | 0.5k | TODO |

**Parallel groups**: T002 and T003 can run in parallel (both after T001)

**Total story tokens**: ~2.1k (under 2.4k plan estimate)

---

## LAYER 2 — Per-Task Definition

### T001: Add search area CSS classes to `index.css`

**Type**: implementation | **Parallel**: safe (no dependencies)

**Description**:
Append CSS classes for the search area to `frontend/src/index.css`, extracted from `docs/knowledge-hub.html` lines 130–200. Include:
- `.search-panel` — card container (surface background, border, shadow, border-radius)
- `.search-header` — header bar with language selector (surface-2 bg, border-bottom)
- `.inline-lang` — language pill inside search header (border, border-radius, indigo dot)
- `.search-input-wrap` — textarea wrapper (padding)
- `textarea.search-input` — clean textarea (no border, DM Sans, transparent bg)
- `.search-footer` — bottom row (border-top, flex)
- `.token-badge` — character count badge (DM Mono, surface-2 bg)
- `.btn-search` — submit button (indigo gradient, hover lift + shadow)
- `.btn-search:disabled` — disabled state (opacity 0.5, no hover effects)

**Files touched**:
- **Modify**: `frontend/src/index.css` (append ~60 lines)

**Acceptance criteria**:
1. `.search-panel` uses `var(--surface)`, `var(--border)`, `var(--radius)`, `var(--shadow)`
2. `.btn-search` uses exact gradient: `linear-gradient(135deg, var(--indigo), var(--indigo-dark))`
3. `.btn-search:hover` has `transform: translateY(-1px)` + shadow increase
4. `.token-badge` uses `font-family: 'DM Mono', monospace`
5. `textarea.search-input` has no border, no outline, transparent background
6. Build passes with zero errors

**Review criteria**:
- ✅ All selectors from reference present
- ✅ No hardcoded hex colors (use CSS vars)
- ✅ `.btn-search:disabled` has `opacity: 0.5; cursor: not-allowed`
- ✅ Build clean

---

### T002: Apply CSS classNames to `SearchInput.tsx`

**Type**: implementation | **Parallel**: after:T001

**Description**:
Replace bare `<div>` and `<textarea>` elements in `SearchInput.tsx` with CSS classNames:
- Outer `<div>` → `<div className="search-panel">`
- `<textarea>` wrap `<div>` → `<div className="search-input-wrap">`
- `<textarea>` → add `className="search-input"`
- Bottom `<div>` → `<div className="search-footer">`
- `<span>` char count → `<span className="token-badge">`
- `<button>` → add `className="btn-search"`

No logic changes. No new props. Tests must still pass.

**Files touched**:
- **Modify**: `frontend/src/components/query/SearchInput.tsx` (~6 className attrs added)

**Acceptance criteria**:
1. All 6 CSS classNames applied to correct elements
2. Existing test assertions (`getByRole`, `getByText`) still pass
3. `npm run test -- --testPathPattern="SearchInput"` passes (all tests green)

**Review criteria**:
- ✅ No inline `style=` props
- ✅ No logic/behavior changes
- ✅ `aria-label` preserved on textarea
- ✅ SearchInput tests: all pass

---

### T003: Apply CSS classNames to `LanguageSelector.tsx`

**Type**: implementation | **Parallel**: after:T001

**Description**:
Replace bare `<div>` in `LanguageSelector.tsx` with styled wrapper using `.inline-lang` class.
- Outer `<div>` → `<div className="inline-lang">`
- Add `<span className="lang-dot" />` before the label (visual color dot per reference)
- `<label>` stays (for accessibility; visually hidden by layout)
- `<select>` stays as-is (CSS in `.inline-lang select` handles styling)

Note: In S002, `LanguageSelector` was moved to the App header. In the header context it uses a different style (`.lang-selector` with white-on-dark). The `LanguageSelector` component will use `.inline-lang` for its own container — matching the header visually is acceptable since both use the same component but styled by context via the `.app-header .inline-lang` override if needed. Per reference, inline-lang works in both dark header and light search contexts.

**Files touched**:
- **Modify**: `frontend/src/components/query/LanguageSelector.tsx` (~3 lines changed)

**Acceptance criteria**:
1. Outer div has `className="inline-lang"`
2. `<span className="lang-dot" />` present before label
3. `getByRole('combobox')` still found in tests
4. `LanguageSelector` tests: all 6 pass

**Review criteria**:
- ✅ No inline `style=` props
- ✅ `lang-dot` span added (visual indigo dot)
- ✅ All 6 LanguageSelector tests pass

---

## ACs Traceability

| AC | Description | Task | Verified by |
|----|-------------|------|-------------|
| AC1 | Search panel card (surface bg, border, shadow, radius) | T001+T002 | Visual + build |
| AC2 | Search input: clean textarea, no border, DM Sans | T001+T002 | Visual |
| AC3 | Submit button: indigo gradient | T001+T002 | Visual |
| AC4 | Submit button hover: translateY(-1px) + shadow | T001 | Code review |
| AC5 | Char count badge: DM Mono, right-aligned | T001+T002 | Visual |
| AC6 | Language selector pill: inline-lang class | T001+T003 | Visual |
| AC7 | Visual parity with knowledge-hub.html reference | T001+T002+T003 | Visual check |
| AC8 | All 208 tests pass | T002+T003 | `npm run test` |

---

## Context & Notes

**Design reference**: `docs/knowledge-hub.html` lines 130–200
- `.search-panel` — card wrapper
- `.search-header` + `.inline-lang` — language selector bar
- `.search-input-wrap` + `textarea.search-input` — input field
- `.search-footer` + `.token-badge` + `.btn-search` — actions row
