# Tasks: S002 — Header + app shell layout
Generated: 2026-04-17 | Story: frontend-theme | Agent: frontend-agent

---

## LAYER 1 — Task Summary

| Task | Title | Type | Dependencies | Est. Tokens | Status |
|------|-------|------|--------------|-------------|--------|
| T001 | Add header + app-body CSS classes to `index.css` | implementation | None | 0.8k | TODO |
| T002 | Update `App.tsx` with sticky header markup | implementation | after:T001 | 0.9k | TODO |
| T003 | Update `QueryPage.tsx` grid + fix `QueryPage.test.tsx` | implementation | after:T002 | 0.7k | TODO |

**Parallel groups**: None (T001 → T002 → T003 sequential)

**Total story tokens**: ~2.4k (matches plan estimate)

---

## LAYER 2 — Per-Task Definition

### T001: Add header + app-body CSS classes to `index.css`

**Type**: implementation | **Parallel**: safe (no dependencies)

**Description**:
Add CSS classes for the sticky header and app-body grid layout to `frontend/src/index.css`, extracted from `docs/knowledge-hub.html` lines 50–128. Include:
- `header.app-header` — sticky gradient header (60px height, indigo gradient)
- `.logo`, `.logo-icon`, `.logo-text`, `.logo-title`, `.logo-sub` — brand logo block
- `.header-sep` — flex spacer (flex: 1)
- `.header-divider` — 1px vertical divider in header
- `.user-pill`, `.avatar`, `.user-name` — user identity pill (right side of header)
- `.app-body` — page grid layout (1fr + 280px, 20px gap, max-width 1280px, centered)

**Files touched**:
- **Modify**: `frontend/src/index.css` (append ~70 lines after existing body reset)

**Acceptance criteria**:
1. `header.app-header` uses exact gradient: `linear-gradient(135deg, #1e1b4b 0%, #312e81 55%, #4338ca 100%)`
2. `header.app-header` is `position: sticky; top: 0; z-index: 100; height: var(--header-h)`
3. `.logo-title` uses `font-family: 'Playfair Display', serif`
4. `.app-body` grid: `grid-template-columns: 1fr 280px; gap: 20px; max-width: 1280px`
5. All values match `docs/knowledge-hub.html` reference exactly

**Test command**: `npm run build 2>&1 | Select-String -Pattern "error" -CaseSensitive`

**Review criteria**:
- ✅ Gradient hex values match reference exactly
- ✅ `z-index: 100` present on app-header
- ✅ `.app-body` uses CSS Grid (not flex)
- ✅ No duplicate selectors in file
- ✅ Build passes with zero errors

---

### T002: Update `App.tsx` with sticky header markup

**Type**: implementation | **Parallel**: after:T001

**Description**:
Add `<header className="app-header">` to `App.tsx` wrapping the existing Routes in a fragment. Header contains:
- Logo block: icon (✦) + "Knowledge Hub" title + "BRYSEN GROUP" subtitle
- `<div className="header-sep" />` (spacer)
- `<LanguageSelector />` (moved from QueryPage to header)
- User pill (conditional: `{token !== null && ...}`): avatar initial + username from `useAuthStore()`

D005: user pill hidden when `token === null` (i.e., on login page where user is not authenticated).
D006: Logo text = "Knowledge Hub" (Playfair) + "BRYSEN GROUP" (uppercase, 10px).

**Files touched**:
- **Modify**: `frontend/src/App.tsx` (~25 lines added, imports updated)

**Acceptance criteria**:
1. `<header className="app-header">` renders on ALL pages (including `/login`)
2. Logo icon: `<div className="logo-icon">✦</div>`; title: "Knowledge Hub"; subtitle: "BRYSEN GROUP"
3. `<LanguageSelector />` present in header (moved from QueryPage)
4. User pill renders only when `token !== null` (uses `useAuthStore().token`)
5. Avatar shows first letter of username (uppercase); `<span className="user-name">` shows full username
6. `npm run test` passes all tests (208 green)

**Test command**: `npm run test -- --testPathPattern="App" 2>&1 | tail -15`

**Review criteria**:
- ✅ `<>` fragment wraps header + Routes (not extra div)
- ✅ `useAuthStore` imported correctly
- ✅ `LanguageSelector` imported from correct path
- ✅ No `useLocation` import (not needed — token check is sufficient per D005)
- ✅ App.test.tsx: `getByRole('combobox')` finds exactly 1 (header LanguageSelector)
- ✅ App.test.tsx: user pill not visible when token=null (login test passes)

---

### T003: Update `QueryPage.tsx` + fix `QueryPage.test.tsx`

**Type**: implementation | **Parallel**: after:T002

**Description**:
- Remove `LanguageSelector` import and usage from `QueryPage.tsx` (now in App.tsx header)
- Replace inline `style={{ display: 'flex', gap: '1rem' }}` with `className="app-body"`
- Remove inline `style={{ flex: 1 }}` from `<main>`
- In `QueryPage.test.tsx`: replace "renders LanguageSelector" test (combobox no longer in page) with "renders main content area" (`getByRole('main')`) to maintain 208 test count

**Files touched**:
- **Modify**: `frontend/src/pages/QueryPage.tsx` (~4 lines changed)
- **Modify**: `frontend/tests/pages/QueryPage.test.tsx` (1 test description + assertion updated)

**Acceptance criteria**:
1. `QueryPage.tsx` has no `LanguageSelector` import or usage
2. `QueryPage.tsx` wraps content in `<div className="app-body">` (not inline style)
3. `<main>` has no inline style
4. `QueryPage.test.tsx` test count unchanged (same number of tests, 208 total)
5. Replaced test: `it('renders main content area', ...)` uses `getByRole('main')`
6. `npm run test` passes all 208 tests

**Test command**: `npm run test 2>&1 | tail -20`

**Review criteria**:
- ✅ No `LanguageSelector` in QueryPage.tsx
- ✅ No inline `style=` props in QueryPage.tsx
- ✅ `className="app-body"` present on outer div
- ✅ `getByRole('main')` works in test (QueryPage renders `<main>` element)
- ✅ All 208 tests pass

---

## ACs Traceability

| AC | Description | Task | Verified by |
|----|-------------|------|-------------|
| AC1 | Sticky header, z-index 100, 60px height | T001+T002 | Visual + build |
| AC2 | Exact gradient `linear-gradient(135deg, #1e1b4b 0%, #312e81 55%, #4338ca 100%)` | T001 | Code review |
| AC3 | Logo icon + "Knowledge Hub" + "BRYSEN GROUP" in Playfair | T002 | Visual |
| AC4 | User pill conditional on `token !== null` | T002 | App.test.tsx |
| AC5 | LanguageSelector in header | T002+T003 | App.test.tsx |
| AC6 | `.app-body` grid: 1fr + 280px, 20px gap | T001+T003 | Visual |
| AC7 | HistoryPanel in right column (280px) | T003 | QueryPage renders HistoryPanel as sibling |
| AC8 | Visual parity with knowledge-hub.html reference | T001+T002 | Visual check |
| AC9 | All 208 tests pass | T003 | `npm run test` |
| AC10 | Build clean (zero TS errors) | T001+T002+T003 | `npm run build` |

---

## Context & Notes

**Design reference**: `docs/knowledge-hub.html`
- Header CSS: lines 50–119
- Layout CSS (`.app-body`): lines 121–128

**Key decisions applied**:
- D005: user pill hidden when `token === null` (login page = no token)
- D006: "Knowledge Hub" + "BRYSEN GROUP" logo text
- D007: global CSS in `index.css`, no inline styles

**Test migration note**:
- `QueryPage.test.tsx` "renders LanguageSelector" → "renders main content area"
  - Reason: LanguageSelector moved to App.tsx header; QueryPage renders `<main>` instead
  - Impact: net 0 change in test count (1 replaced)
