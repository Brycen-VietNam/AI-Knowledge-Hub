# Tasks: S004 — Results area styling (AnswerPanel, ConfidenceBadge, Citations)
Generated: 2026-04-17 | Story: frontend-theme | Agent: frontend-agent

---

## LAYER 1 — Task Summary

| Task | Title | Type | Dependencies | Est. Tokens | Status |
|------|-------|------|--------------|-------------|--------|
| T001 | Add results area CSS classes to `index.css` | implementation | None | 1.0k | TODO |
| T002 | Update `ConfidenceBadge.tsx` + fix test | implementation | after:T001 | 0.5k | TODO |
| T003 | Update `AnswerPanel.tsx` + `LowConfidenceWarning.tsx` | implementation | after:T001 | 0.6k | TODO |
| T004 | Update `CitationList.tsx` + `CitationItem.tsx` | implementation | after:T001 | 0.5k | TODO |

**Parallel groups**: T002, T003, T004 can all run in parallel after T001

**Total story tokens**: ~2.6k (within 2.4k plan estimate + margin)

---

## LAYER 2 — Per-Task Definition

### T001: Add results area CSS classes to `index.css`

**Type**: implementation | **Parallel**: safe (no dependencies)

**Description**:
Append CSS classes for the results area to `frontend/src/index.css`, extracted from `docs/knowledge-hub.html` lines 200–310. Include:
- `.answer-panel` — card container (surface, border, radius, shadow-sm)
- `.answer-meta` — meta bar (surface-2 bg, border-bottom, flex align center gap)
- `.confidence-badge` — badge base + `.high` / `.medium` / `.low` variants (emerald/amber/red)
- `.warning-banner` — LowConfidenceWarning alert (amber bg, border-bottom, flex)
- `.warning-icon` — icon in warning banner
- `.answer-body` — answer text body (padding 18px 20px)
- `.answer-text` — prose text (font-size 14px, line-height 1.75)
- `.answer-loading` / `.answer-error` / `.answer-empty` / `.answer-no-source` — state classes
- `.sources-section` + `.sources-title` + `.source-list` — citations container
- `.source-item` — citation row card (border, radius-sm, hover indigo)
- `.source-num` + `.source-info` + `.source-name` + `.source-score` — citation metadata
- `.btn-more` — show-more button (full-width, border, hover indigo)
- `.line-clamp-2` — real CSS class (webkit-line-clamp: 2) to replace Tailwind name
- Scrollbar + fade-up animation rules

**Files touched**:
- **Modify**: `frontend/src/index.css` (append ~130 lines)

**Acceptance criteria**:
1. `.confidence-badge.high` uses `var(--emerald-light)` bg + `var(--emerald)` color
2. `.confidence-badge.medium` uses `var(--amber-light)` + `var(--amber)`
3. `.confidence-badge.low` uses `var(--red-light)` + `var(--red)`
4. `.warning-banner` uses `#fffbeb` bg + `#fde68a` border-bottom (per reference)
5. `.source-item:hover` has indigo border + indigo-light shadow
6. `.line-clamp-2` applies `-webkit-line-clamp: 2; display: -webkit-box; overflow: hidden`
7. Build passes with zero errors

**Review criteria**:
- ✅ All CSS vars used (no hardcoded colors except reference exceptions)
- ✅ `.confidence-badge` has `pulse-dot` animation on `::before`
- ✅ `.line-clamp-2` is a real CSS class (not Tailwind)
- ✅ Build clean

---

### T002: Update `ConfidenceBadge.tsx` + fix `ConfidenceBadge.test.tsx`

**Type**: implementation | **Parallel**: after:T001

**Description**:
Replace Tailwind class strings in `ConfidenceBadge.tsx` with new CSS class names. Update 3 test assertions in `ConfidenceBadge.test.tsx` that check the old Tailwind class names.

Component changes:
- `className = 'bg-green-100 text-green-800'` → `variant = 'high'`
- `className = 'bg-yellow-100 text-yellow-800'` → `variant = 'medium'`
- `className = 'bg-red-100 text-red-800'` → `variant = 'low'`
- `<span className={`inline-flex items-center px-2 py-0.5 rounded text-xs font-medium ${className}`}>` → `<span className={`confidence-badge ${variant}`}>`

Test changes (3 tests):
- HIGH: `expect(badge.className).toContain('bg-green-100')` → `expect(badge.className).toContain('high')`
- HIGH: `expect(badge.className).toContain('text-green-800')` → `expect(badge.className).toContain('confidence-badge')`
- MEDIUM: `expect(badge.className).toContain('bg-yellow-100')` → `expect(badge.className).toContain('medium')`
- MEDIUM: `expect(badge.className).toContain('text-yellow-800')` → `expect(badge.className).toContain('confidence-badge')`
- LOW: `expect(badge.className).toContain('bg-red-100')` → `expect(badge.className).toContain('low')`
- LOW: `expect(badge.className).toContain('text-red-800')` → `expect(badge.className).toContain('confidence-badge')`

**Files touched**:
- **Modify**: `frontend/src/components/results/ConfidenceBadge.tsx`
- **Modify**: `frontend/tests/components/results/ConfidenceBadge.test.tsx`

**Acceptance criteria**:
1. Component uses `confidence-badge high/medium/low` classes (no Tailwind)
2. All 9 ConfidenceBadge tests pass
3. Test assertions use new class names

**Review criteria**:
- ✅ No `bg-green-100`, `bg-yellow-100`, `bg-red-100` in component
- ✅ No `text-green-800`, `text-yellow-800`, `text-red-800` in component
- ✅ 9/9 ConfidenceBadge tests green

---

### T003: Update `AnswerPanel.tsx` + `LowConfidenceWarning.tsx`

**Type**: implementation | **Parallel**: after:T001

**Description**:
Replace Tailwind classNames with CSS classes. No logic changes. `role=` attributes must stay.

`AnswerPanel.tsx` mapping:
- loading `<div>` → `<div role="status" className="answer-loading">`
- loading spinner `<span className="animate-spin ...">` → `<span className="answer-spinner" />`
- error `<div>` → `<div role="alert" className="answer-error">`
- empty `<p>` → `<p className="answer-empty">`
- full result outer `<section>` → `<section className="answer-panel">`
- confidence meta wrapper `<div className="flex items-center gap-2">` → `<div className="answer-meta">`
- answer prose `<div className="prose ...">` → `<div className="answer-body"><div className="answer-text">`
- no-source `<p className="text-xs text-amber-700">` → `<p className="answer-no-source">`

`LowConfidenceWarning.tsx` mapping:
- `<div role="alert" className="rounded-md bg-amber-50 ...">` → `<div role="alert" className="warning-banner">`
- Add `<span className="warning-icon">⚠</span>` before text

**Files touched**:
- **Modify**: `frontend/src/components/results/AnswerPanel.tsx`
- **Modify**: `frontend/src/components/results/LowConfidenceWarning.tsx`

**Acceptance criteria**:
1. No Tailwind classNames remain in either file
2. `role="status"` on loading, `role="alert"` on error and warning
3. All AnswerPanel tests pass (12 tests green in results suite)
4. LowConfidenceWarning test passes (3 tests)

**Review criteria**:
- ✅ `role="status"` preserved on loading div
- ✅ `role="alert"` preserved on error div and LowConfidenceWarning
- ✅ No `animate-spin`, `prose`, `flex`, `items-center` Tailwind classes remain

---

### T004: Update `CitationList.tsx` + `CitationItem.tsx`

**Type**: implementation | **Parallel**: after:T001

**Description**:
Replace Tailwind classNames with CSS classes in CitationList and CitationItem.

`CitationList.tsx`:
- outer `<div className="flex flex-col divide-y divide-gray-100">` → `<div className="sources-section"><div className="source-list">`
- show-more button `className="mt-2 text-sm text-blue-600..."` → `className="btn-more"`
- close the extra wrapper div in JSX

`CitationItem.tsx`:
- outer `<div ... className="flex flex-col gap-1 py-2">` → `<div data-testid="citation-item" className="source-item">`
- meta row `<div className="flex items-center justify-between gap-2">` → `<div className="source-meta">`
- title `<span className="font-medium text-sm">` → `<span className="source-name">`
- score `<span className="text-xs text-gray-500 shrink-0">` → `<span className="source-score">`
- preview `<p data-testid="citation-preview" className="text-xs text-gray-600 line-clamp-2">` → keep `line-clamp-2` (now a real CSS class defined in T001)

**Files touched**:
- **Modify**: `frontend/src/components/results/CitationList.tsx`
- **Modify**: `frontend/src/components/results/CitationItem.tsx`

**Acceptance criteria**:
1. `data-testid="citation-item"` preserved on CitationItem root element
2. `data-testid="citation-preview"` preserved on preview paragraph
3. `preview.className.toContain('line-clamp-2')` test still passes
4. All CitationList + CitationItem tests pass

**Review criteria**:
- ✅ `data-testid` attributes preserved
- ✅ `line-clamp-2` kept in component className (backed by real CSS)
- ✅ No Tailwind classes remain (no `flex`, `text-sm`, `gap-1`, etc.)
- ✅ All 8 CitationItem + all CitationList tests green

---

## ACs Traceability

| AC | Description | Task | Verified by |
|----|-------------|------|-------------|
| AC1 | Confidence badge emerald HIGH variant | T001+T002 | ConfidenceBadge tests |
| AC2 | Confidence badge amber MEDIUM / red LOW variants | T001+T002 | ConfidenceBadge tests |
| AC3 | LowConfidenceWarning amber banner | T001+T003 | LowConfidenceWarning test |
| AC4 | AnswerPanel card styling | T001+T003 | Visual |
| AC5 | CitationList sources section | T001+T004 | Visual |
| AC6 | CitationItem source-item card with hover | T001+T004 | Visual |
| AC7 | Answer text typography (DM Sans, 14px, 1.75 line-height) | T001+T003 | Visual |
| AC8 | Loading state visible (role=status) | T003 | AnswerPanel tests |
| AC9 | Empty state + no-source warning visible | T003 | AnswerPanel tests |
| AC10 | Error state visible (role=alert) | T003 | AnswerPanel tests |
| AC11 | All Tailwind classes removed from 5 components | T002+T003+T004 | Code review |
| AC12 | All 208 tests pass | T002+T003+T004 | `npm run test` |
