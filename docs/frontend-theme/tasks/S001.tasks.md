# Tasks: S001 — Design tokens + global CSS baseline
Generated: 2026-04-17 | Story: frontend-theme | Agent: frontend-agent

---

## LAYER 1 — Task Summary

| Task | Title | Type | Dependencies | Est. Tokens | Status |
|------|-------|------|--------------|-------------|--------|
| T001 | Create `index.css` with design tokens + global reset | implementation | None | 1.2k | ✅ DONE |
| T002 | Import `index.css` in `main.tsx` + verify build + visual check | verification | after:T001 | 0.6k | ✅ DONE |

**Parallel groups**: None (T002 depends on T001 output)

**Total story tokens**: ~1.8k (well under 2.4k plan estimate)

---

## LAYER 2 — Per-Task Definition

### T001: Create `index.css` with design tokens + global reset

**Type**: implementation | **Parallel**: safe (no dependencies)

**Description**:
Create a new CSS file with all design tokens extracted from `docs/knowledge-hub.html` reference (lines 11–48). Include:
- Google Fonts `@import` statements (DM Sans 400/500/600, DM Mono 400/500, Playfair Display 700)
- CSS `:root` variables for colors (primary, surfaces, confidence, text shades)
- CSS `:root` variables for shadows, border-radius, spacing, header height
- Global `body` reset (font-family, background, color, margin, padding, box-sizing)
- No Tailwind classes, no component-specific styles

**Files touched**:
- **Create**: `frontend/src/index.css` (new, ~150 lines)

**Acceptance criteria**:
1. Google Fonts loads DM Sans (weights 400, 500, 600), DM Mono (400, 500), Playfair Display (700) synchronously via `@import url()`
2. `:root` block defines ≥20 CSS variables:
   - **Colors**: `--indigo`, `--indigo-dark`, `--indigo-light`, `--emerald`, `--amber`, `--red`, `--bg`, `--surface`, `--surface-2`, `--text`, `--text-2`, `--text-3`, `--border`, `--shadow` (shadow hex or rgba)
   - **Layout**: `--header-h: 60px;`
   - **Radius**: `--radius-sm`, `--radius-md`, `--radius-lg`
   - **Spacing**: (optional, but include if in reference)
3. Global `body` reset applied:
   - `font-family: "DM Sans", sans-serif;`
   - `background-color: var(--bg);`
   - `color: var(--text);`
   - `margin: 0;`
   - `padding: 0;`
   - `box-sizing: border-box;` (or universal selector `*`)
4. Reference values match `docs/knowledge-hub.html` lines 11–48 exactly (hex codes, gradient syntax)
5. File syntax: valid CSS, no errors in build
6. File size: <200 lines (keep concise, no component-specific overrides)

**Test command**: `npm run build 2>&1 | grep -E "error|Error" && exit 1 || echo "Build OK"`

**Review criteria**:
- ✅ File created at exact path `frontend/src/index.css`
- ✅ `:root` block is first in file, before any other rules
- ✅ Google Fonts @import loads all three families synchronously
- ✅ All ≥20 variables present, no typos in variable names
- ✅ Global `body` reset applied (font-family, bg-color, color, margin/padding reset, box-sizing)
- ✅ No unused variables, no duplicate variable names
- ✅ Build passes: `npm run build` output has zero errors
- ✅ All syntax correct (colons, semicolons, hex codes valid)

---

### T002: Import `index.css` in `main.tsx` + verify build + visual check

**Type**: verification | **Parallel**: after:T001

**Description**:
Import the newly created `index.css` in `frontend/src/main.tsx` (before React root render). Run full build and test suite. Visually verify that:
- Page background matches `--bg = #f0f2f7` (light warm gray)
- Body text color matches `--text = #1a1d2e` (dark navy blue)
- Google Fonts load (inspect Network tab / inspect fonts in DevTools)

**Files touched**:
- **Modify**: `frontend/src/main.tsx` (add import statement, 1 line)

**Acceptance criteria**:
1. Import statement added at top of `main.tsx`: `import './index.css';` (BEFORE React root ReactDOM.createRoot)
2. `npm run build` completes with zero TypeScript errors and zero CSS errors
3. `npm run test` passes all 208 tests (no test failures introduced)
4. Visual check in browser (after `npm run dev`):
   - Page background is light gray (`#f0f2f7`), not white or other color
   - Body text is dark navy (`#1a1d2e`), not black
   - Google Fonts **DM Sans** is loaded (inspect DevTools → Network/Fonts tab, or use `document.fonts` in console)
   - Page renders without visual glitches (no unstyled flash, no broken layout)
5. No runtime errors in browser console (no "Cannot find module", no CSS parse errors)

**Test command**: `npm run build && npm run test -- --testPathPattern="^" 2>&1 | tail -20`

**Review criteria**:
- ✅ Import statement present in `main.tsx`, at top of file, before `createRoot`
- ✅ `npm run build` succeeds (zero errors/warnings in output)
- ✅ `npm run test` passes all 208 tests (or reports exact count pass)
- ✅ Browser visual: background is `#f0f2f7` light gray (not white/off-white)
- ✅ Browser visual: text is `#1a1d2e` dark navy (not pure black)
- ✅ Google Fonts loaded (DevTools Network shows font files OR `document.fonts.ready` resolves)
- ✅ No console errors in DevTools (Error, Uncaught, undefined module)
- ✅ S001 story complete: all 8 ACs verified (tokens defined, imported, build clean, visual match reference)

---

## Context & Notes

**Design reference**: `docs/knowledge-hub.html` lines 8–48
- `:root` CSS variables (lines 11–37)
- Google Fonts @import (lines 8–10)
- Body reset (lines 38–48)

**Color palette from reference**:
```css
--indigo: #6366f1;
--indigo-dark: #4f46e5;
--indigo-light: #eef2ff;
--bg: #f0f2f7;
--surface: #ffffff;
--surface-2: #f7f8fc;
--emerald: #10b981;
--amber: #f59e0b;
--red: #ef4444;
--text: #1a1d2e;
--text-2: #5b6178;
--text-3: #9198b5;
```

**Header gradient** (for S002):
```css
linear-gradient(135deg, #1e1b4b 0%, #312e81 55%, #4338ca 100%)
```

**Font families**:
```css
@import url('https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=DM+Sans:wght@400;500;600&family=Playfair+Display:wght@700&display=swap');
```

---

## Next Steps

1. **Implement T001**: Create `frontend/src/index.css` with all tokens + imports
2. **Run T002**: Add import to `main.tsx`, build, test, visual verify
3. **Mark S001 complete**: All 8 ACs verified
4. **Proceed to S002**: Header + app shell layout (depends on S001 tokens defined)

---
