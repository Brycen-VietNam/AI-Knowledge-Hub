# Tasks: document-parser / P0 — Dependencies + Docker
Created: 2026-04-13 | Agent: rag-agent | Status: DONE

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | P0: Dependencies + Docker (pre-implementation gate) |
| Total tasks | 2 |
| Parallel groups | G1: [T001, T002] |
| Critical path | T001 ‖ T002 |
| Agent | rag-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Add libmagic1 to Dockerfile | DONE | safe | S003 |
| T002 | Add parser deps to requirements.txt | DONE | safe | S001, S002, S003 |

---

## LAYER 2 — Task Detail

### T001: Add libmagic1 to Dockerfile

**Status**: DONE

**File(s)**
- `Dockerfile` — modify

**Change description**
Add `libmagic1` to the existing `apt-get install` block (after the mecab line).
Add a comment noting WARN-02 resolution from the document-parser checklist.
No other changes to Dockerfile.

**Review criteria**
- [ ] `libmagic1` appears in apt-get install block
- [ ] Comment references WARN-02 / document-parser
- [ ] No unrelated Dockerfile changes

**Test command**
```bash
docker build . --no-cache && docker run --rm <image> python -c "import magic; print('OK')"
```

**Parallel**: safe
**Size estimate**: ~2 lines changed

---
<!-- End T001 -->

### T002: Add parser deps to requirements.txt

**Status**: DONE

**File(s)**
- `requirements.txt` — modify

**Change description**
Add a clearly-labelled section `# Document parser (document-parser feature)` with 4 packages:
`pdfplumber>=0.11.0`, `python-docx>=1.1.0`, `beautifulsoup4>=4.12.0`, `python-magic>=0.4.27`.
Do NOT add `python-magic-bin` — `libmagic1` is added via Dockerfile (T001).

**Review criteria**
- [ ] Section header comment present
- [ ] All 4 packages with correct version pins
- [ ] `python-magic-bin` absent (would conflict with T001)

**Test command**
```bash
pip install -r requirements.txt --dry-run 2>&1 | grep -E "pdfplumber|python-docx|beautifulsoup4|python-magic"
```

**Parallel**: safe
**Size estimate**: ~6 lines added

---
<!-- End T002 -->
