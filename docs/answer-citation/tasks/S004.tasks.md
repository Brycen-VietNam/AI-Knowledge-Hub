# Tasks: answer-citation / S004 — Consumer Rendering Contract (Documentation)
Created: 2026-04-14 | Agent: api-agent | Status: DONE ✅

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S004: Consumer Rendering Contract (Documentation) |
| Total tasks | 2 |
| Parallel groups | G1: [T001, T002] |
| Critical path | T001 (T002 is independent) |
| Agent | api-agent |
| Est. session | 0.5 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | citation-rendering-contract.md — write all 9 ACs | DONE ✅ | safe | — |
| T002 | api-reference.md — add link to rendering contract | DONE ✅ | safe | — |

---

## LAYER 2 — Task Detail

### T001: citation-rendering-contract.md — write all 9 ACs

**Status**: DONE ✅

**File(s)**
- `docs/answer-citation/citation-rendering-contract.md` — create

**Change description**
Create the consumer rendering contract document covering all 9 AC behaviors from spec.
Must have `Contract-Version: 1.0` header (AC1).

Document must cover:
1. **AC1** — `Contract-Version: 1.0` header and versioning policy
2. **AC2** — `[N]` maps to `citations[N-1]` (1-based marker → 0-indexed array); OOB N → render plain text
3. **AC3** — `low_confidence: true` in response → show warning banner; still render citations
4. **AC4** — `source_url: null` → render title-only link (no href); never throw on null
5. **AC5** — Citations ordering guarantee: same order as `sources` array
6. **AC6** — Empty `citations: []` → hide citation UI entirely (no empty list rendered)
7. **AC7** — `sources` field preserved for backward compat; consumers may use either field
8. **AC8** — Consumers MUST use permissive/lenient JSON parsing; extra fields must not cause parse failure (AC9 from spec)
9. **AC9** — `answer` may contain `[N]` markers even when `citations` array is shorter than N; consumer renders as plain text in that case

Format: Markdown with a table or numbered section per AC.
No code changes. No auth. No tests required.

**Review criteria**
- [ ] `Contract-Version: 1.0` present in frontmatter or header
- [ ] All 9 consumer behaviors documented (AC1–AC9 above)
- [ ] OOB marker behavior explicit: render `[N]` as plain text
- [ ] Lenient JSON parsing requirement stated clearly (AC8)
- [ ] Backward compat guarantee for `sources` field stated

**Test command**
```bash
# Documentation only — verify file exists
ls docs/answer-citation/citation-rendering-contract.md
```

**Parallel**: safe
**Size estimate**: ~80 lines (documentation)

---
<!-- End T001 -->

### T002: api-reference.md — add link to rendering contract

**Status**: DONE ✅

**File(s)**
- `docs/query-endpoint/api-reference.md` — modify

**Change description**
Add a single link line to the existing `api-reference.md` under the `/v1/query` response section.
Link format (exact):
```markdown
See [Citation Rendering Contract](../answer-citation/citation-rendering-contract.md)
```
Place under the `citations` field description in the response schema section.
No other changes to api-reference.md.

**Review criteria**
- [ ] Link present and path is correct relative to `docs/query-endpoint/`
- [ ] Link text matches exactly: `Citation Rendering Contract`
- [ ] No other lines in api-reference.md modified

**Test command**
```bash
grep -n "Citation Rendering Contract" docs/query-endpoint/api-reference.md
```

**Parallel**: safe
**Size estimate**: ~2 lines changed

---
<!-- End T002 -->
