# Tasks: answer-citation / S001 — DB Migration + RetrievedDocument Enrichment
Created: 2026-04-14 | Agent: db-agent | Status: DONE ✅

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S001: DB Migration + RetrievedDocument Enrichment |
| Total tasks | 5 |
| Parallel groups | G1: [T001], G2: [T002, T003], G3: [T004], G4: [T005] |
| Critical path | T001 → T002 → T004 → T005 |
| Agent | db-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Pre-migration lang NULL check | DONE ✅ | safe | T002 |
| T002 | Migration 007 — add source_url column | DONE ✅ | after:T001 | T004 |
| T003 | Document ORM — add source_url field | DONE ✅ | after:T001 | T004 |
| T004 | RetrievedDocument — add title/lang/source_url fields | DONE ✅ | after:T002,T003 | T005 |
| T005 | Retriever — enrich _dense_search + _bm25_search + test | DONE ✅ | after:T004 | — |

---

## LAYER 2 — Task Detail

### T001: Pre-migration lang NULL check

**Status**: DONE ✅

**File(s)**
- `backend/db/migrations/007_add_source_url.sql` — create (includes pre-check comment)

**Change description**
Add a pre-migration verification query as a SQL comment block at the top of migration 007.
The query `SELECT COUNT(*) FROM documents WHERE lang IS NULL` must be run manually before applying the migration.
If count > 0, the `d.lang or "und"` fallback in T004 is required (already planned).
Document the result expectation in the migration file header comment.

**Review criteria**
- [ ] Pre-check query present as `-- PRE-CHECK:` comment in migration file
- [ ] Migration file header documents expected result (count = 0 for clean installs)
- [ ] No actual schema change in this task — T002 handles DDL

**Test command**
```bash
# Manual verification only — no pytest for pre-check comment
grep -n "PRE-CHECK" backend/db/migrations/007_add_source_url.sql
```

**Rollback** _(DB/migration tasks only)_
```sql
-- Pre-check only — no schema change to roll back
```

**Parallel**: safe
**Size estimate**: ~10 lines (comment block only)

---
<!-- End T001 -->

### T002: Migration 007 — add source_url column

**Status**: DONE ✅

**File(s)**
- `backend/db/migrations/007_add_source_url.sql` — modify (add DDL after T001 comment block)

**Change description**
Add `ALTER TABLE documents ADD COLUMN source_url TEXT;` to migration file.
Column is nullable (no DEFAULT, no NOT NULL constraint) — zero-downtime safe in PostgreSQL.
Include rollback section at bottom: `ALTER TABLE documents DROP COLUMN source_url;`.
No index needed (source_url is display-only, never queried as filter).

**Review criteria**
- [ ] Column is `TEXT` nullable — no DEFAULT, no NOT NULL
- [ ] Rollback section present and correct
- [ ] Migration filename follows convention: `007_add_source_url.sql`
- [ ] Rule A006: migration file created before ORM update (T003 is parallel-safe here since ORM reflects new schema)

**Test command**
```bash
# Verify migration SQL is syntactically valid
grep -n "ADD COLUMN source_url" backend/db/migrations/007_add_source_url.sql
grep -n "DROP COLUMN source_url" backend/db/migrations/007_add_source_url.sql
```

**Rollback**
```sql
-- rollback
ALTER TABLE documents DROP COLUMN source_url;
```

**Parallel**: after:T001
**Size estimate**: ~15 lines

---
<!-- End T002 -->

### T003: Document ORM — add source_url field

**Status**: DONE ✅

**File(s)**
- `backend/db/models/document.py` — modify

**Change description**
Add `source_url: Mapped[str | None] = mapped_column(Text, nullable=True)` to the `Document` SQLAlchemy model.
Must match migration 007 column definition exactly (TEXT, nullable).
No migration file change — T002 handles DDL.
No default value in ORM — PostgreSQL default is NULL.

**Review criteria**
- [ ] Field type: `Mapped[str | None]`
- [ ] `mapped_column(Text, nullable=True)` — no server_default
- [ ] Existing `Document` fields unchanged
- [ ] Rule A006: ORM updated AFTER migration file exists (T002 sibling)

**Test command**
```bash
pytest tests/db/test_document_model.py -v
```

**Parallel**: after:T001
**Size estimate**: ~3 lines changed

---
<!-- End T003 -->

### T004: RetrievedDocument — add title/lang/source_url fields

**Status**: DONE ✅

**File(s)**
- `backend/rag/retriever.py` — modify (locate `RetrievedDocument` dataclass/model)

**Change description**
Add three optional fields to `RetrievedDocument` with `None` defaults so no existing callers break:
- `title: str | None = None`
- `lang: str | None = None`
- `source_url: str | None = None`

If `RetrievedDocument` is defined in a separate models file, modify that file instead.
Apply `d.lang or "und"` fallback pattern in retriever construction if lang NULL check (T001) found any rows.

**Review criteria**
- [ ] All 3 fields have `= None` defaults — existing tests must not break
- [ ] Rule R002: no PII fields added (title/lang/source_url are safe)
- [ ] `d.lang or "und"` fallback documented in code comment referencing migration 007

**Test command**
```bash
pytest tests/rag/test_retriever_rbac.py -v
```

**Parallel**: after:T002,T003
**Size estimate**: ~6 lines changed

---
<!-- End T004 -->

### T005: Retriever — enrich _dense_search + _bm25_search + test

**Status**: DONE ✅

**File(s)**
- `backend/rag/retriever.py` — modify (`_dense_search`, `_bm25_search`)
- `tests/rag/test_retriever_rbac.py` — modify (add enrichment AC12 test)

**Change description**
**TDD order: write failing test first, then implement.**

_bm25_search (already JOINs `documents d`):
- Add `d.title`, `d.lang`, `d.source_url` to existing SELECT clause.
- Map new columns to `RetrievedDocument` fields in result construction.

_dense_search (currently queries `embeddings e` only):
- Add `INNER JOIN documents d ON d.id = e.doc_id` after existing WHERE.
- Add `d.title`, `d.lang`, `d.source_url` to SELECT clause.
- Map new columns to `RetrievedDocument` fields.

_merge: no change needed if spread (`**vars()`) is already generic; verify and note.

AC7 check: count SQL calls before/after — must remain same number per `search()` call.

**Review criteria**
- [ ] `_dense_search`: INNER JOIN present, 3 new SELECT columns
- [ ] `_bm25_search`: 3 new SELECT columns added (no JOIN change)
- [ ] `_merge`: passes through new fields (verify — no code change if spread is generic)
- [ ] Rule R001: RBAC WHERE clause unchanged — new JOIN does not alter group filter
- [ ] Rule P004: no N+1 queries introduced
- [ ] AC7: same number of SQL queries per `search()` call
- [ ] Test `test_retrieved_document_enrichment`: asserts title/lang/source_url populated

**Test command**
```bash
pytest tests/rag/test_retriever_rbac.py -v -k "enrichment"
pytest tests/rag/test_retriever_rbac.py -v
```

**Parallel**: after:T004
**Size estimate**: ~25 lines changed, ~20 lines test

---
<!-- End T005 -->
