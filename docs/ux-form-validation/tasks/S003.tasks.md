# Tasks: ux-form-validation / S003 — LoginForm password toggle
Created: 2026-04-24 | Agent: frontend-agent | Status: DONE

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S003: LoginForm — password toggle (Frontend SPA) |
| Total tasks | 2 |
| Parallel groups | G1: [T001, T002] |
| Critical path | T001 ‖ T002 |
| Agent | frontend-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Add showPassword toggle to LoginForm | DONE | safe | — |
| T002 | Add show_password/hide_password keys to frontend-spa 4 locales | DONE | safe | — |

---

## LAYER 2 — Task Detail

### T001: Add showPassword toggle to LoginForm

**Status**: DONE

**File(s)**
- `frontend/src/components/auth/LoginForm.tsx` — modify

**Change description**
- Added `showPassword` state: `useState(false)`
- Wrapped password `<input>` in `<div className="password-field-row">`
- Input `type` now `showPassword ? 'text' : 'password'`
- Added `<button className="btn-eye">` with `aria-label` from i18n, toggling state on click
- Icon: `'🙈'` (hidden) / `'👁'` (visible) — no new dependency

**Review criteria**
- [x] `password-field-row` wrapper present
- [x] `btn-eye` button with `type="button"` (no accidental form submit)
- [x] `aria-label` uses `t('login.show_password')` / `t('login.hide_password')`
- [x] No new icon library imported
- [x] Input `id="password"` preserved (label `htmlFor` still valid)

**Test command**
```bash
cd frontend && npm run build 2>&1 | tail -5
```

**Parallel**: safe
**Size estimate**: ~12 lines changed

---

### T002: Add show/hide_password keys to frontend-spa 4 locales

**Status**: DONE

**File(s)**
- `frontend/src/i18n/locales/en.json` — modify
- `frontend/src/i18n/locales/vi.json` — modify
- `frontend/src/i18n/locales/ja.json` — modify
- `frontend/src/i18n/locales/ko.json` — modify

**Change description**
Added `login.show_password` and `login.hide_password` in all 4 locales.

**Review criteria**
- [x] Both keys present in en/vi/ja/ko under `login` object
- [x] No duplicate keys

**Parallel**: safe
**Size estimate**: ~2 lines × 4 files = 8 lines

---
