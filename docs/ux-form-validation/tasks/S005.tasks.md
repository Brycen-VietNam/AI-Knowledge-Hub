# Tasks: ux-form-validation / S005 — ResetPasswordModal + AssignGroupModal polish
Created: 2026-04-24 | Agent: frontend-agent | Status: DONE

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S005: ResetPasswordModal + AssignGroupModal — polish (Admin SPA) |
| Total tasks | 2 |
| Parallel groups | G1: [T001, T002] |
| Critical path | T001 ‖ T002 |
| Agent | frontend-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | ResetPasswordModal minLength/maxLength + hint; AssignGroupModal disable + error | DONE | safe | — |
| T002 | Add hint + assign_groups_hint_min keys to admin-spa 4 locales | DONE | safe | — |

---

## LAYER 2 — Task Detail

### T001: ResetPasswordModal + AssignGroupModal component changes

**Status**: DONE

**File(s)**
- `frontend/admin-spa/src/components/ResetPasswordModal.tsx` — modify
- `frontend/admin-spa/src/components/AssignGroupModal.tsx` — modify

**Change description**
ResetPasswordModal (manual mode only):
- Added `minLength={8}` `maxLength={128}` to `#rp-new` input
- Added `<p className="field-hint">{t('auth.reset_password.hint_password')}</p>` after input
- AC3 (copy timeout setTimeout 2000ms at line 71) already existed — no change needed

AssignGroupModal:
- Added `{selected.size === 0 && <p className="field-error">{t('assign_groups_hint_min')}</p>}` above actions
- Added `disabled={loading || selected.size === 0}` to Save button
- Note: `selected` is a `Set<number>` — used `.size` (not `.length`)

**Review criteria**
- [x] hint only shows in manual mode (inside `{mode === 'manual' && ...}` block)
- [x] Save button disabled when `selected.size === 0`
- [x] field-error message clears when user selects a group (reactive to Set state)
- [x] No inline styles added (D6)

**Test command**
```bash
cd frontend/admin-spa && npm run build 2>&1 | tail -5
```

**Parallel**: safe
**Size estimate**: ~8 lines changed across 2 files

---

### T002: Add hint keys to admin-spa 4 locales

**Status**: DONE

**File(s)**
- `frontend/admin-spa/src/i18n/locales/en.json` — modify
- `frontend/admin-spa/src/i18n/locales/vi.json` — modify
- `frontend/admin-spa/src/i18n/locales/ja.json` — modify
- `frontend/admin-spa/src/i18n/locales/ko.json` — modify

**Change description**
Added to all 4 admin-spa locales:
- `assign_groups_hint_min` (top-level, matches existing pattern)
- `auth.reset_password.hint_password` (new `auth` namespace block)

**Review criteria**
- [x] Both keys present in all 4 locales
- [x] `auth` block added as new top-level namespace (not previously present in admin-spa)
- [x] Valid JSON (build passes)

**Parallel**: safe
**Size estimate**: ~5 lines × 4 files

---
