# Tasks: ux-form-validation / S002 — UserFormModal 422 parsing + hints
Created: 2026-04-24 | Agent: frontend-agent | Status: DONE

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S002: UserFormModal — 422 parsing + hints (Admin SPA) |
| Total tasks | 2 |
| Parallel groups | G1: [T001, T002] |
| Critical path | T001 ‖ T002 |
| Agent | frontend-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Add fieldErrors state + 422 per-field parse to UserFormModal | DONE | safe | — |
| T002 | Add hint_sub/hint_email/hint_password keys to admin-spa 4 locales | DONE | safe | — |

---

## LAYER 2 — Task Detail

### T001: Add fieldErrors state + 422 per-field parse + render

**Status**: DONE

**File(s)**
- `frontend/admin-spa/src/components/UserFormModal.tsx` — modify

**Change description**
- Added `fieldErrors` state: `useState<Record<string, string>>({})`
- Updated 422 catch branch: if `detail` is Array, parse `d.loc[1] → d.msg` into `fieldErrors`; else fall back to general error
- Added `{fieldErrors.sub && <p className="field-error">}` under sub input
- Added `{fieldErrors.email && <p className="field-error">}` under email input
- Added `{fieldErrors.password && <p className="field-error">}` under password row
- Added `<p className="field-hint">` for sub, email, password (always visible)

**Review criteria**
- [x] `fieldErrors` state present
- [x] 422 Array detail → per-field errors; string detail → general error
- [x] `.field-error` only rendered when fieldErrors[field] is truthy
- [x] `.field-hint` always rendered for sub, email, password
- [x] No hardcoded error strings — all via i18n or server message

**Test command**
```bash
cd frontend/admin-spa && npm run build 2>&1 | tail -5
```

**Parallel**: safe
**Size estimate**: ~20 lines changed

---

### T002: Add hint keys to admin-spa 4 locales

**Status**: DONE

**File(s)**
- `frontend/admin-spa/src/i18n/locales/en.json` — modify
- `frontend/admin-spa/src/i18n/locales/vi.json` — modify
- `frontend/admin-spa/src/i18n/locales/ja.json` — modify
- `frontend/admin-spa/src/i18n/locales/ko.json` — modify

**Change description**
Added under `user.form`: `hint_sub`, `hint_email`, `hint_password` in all 4 locales.

**Review criteria**
- [x] All 3 hint keys present in en/vi/ja/ko
- [x] Nested under existing `user.form` object — no new top-level keys

**Parallel**: safe
**Size estimate**: ~3 lines × 4 files = 12 lines

---
