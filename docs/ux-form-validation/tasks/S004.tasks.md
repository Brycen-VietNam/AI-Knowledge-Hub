# Tasks: ux-form-validation / S004 — ChangePassword real-time confirm match
Created: 2026-04-24 | Agent: frontend-agent | Status: DONE

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S004: ChangePasswordModal + ChangePasswordPage — real-time confirm match |
| Total tasks | 2 |
| Parallel groups | G1: [T001, T002] |
| Critical path | T001 ‖ T002 |
| Agent | frontend-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Add confirmError + real-time match check to both ChangePassword components | DONE | safe | — |
| T002 | Add hint_password to auth.change_password in frontend-spa 4 locales | DONE | safe | — |

---

## LAYER 2 — Task Detail

### T001: Add confirmError + real-time match + hint to ChangePasswordModal + ChangePasswordPage

**Status**: DONE

**File(s)**
- `frontend/src/components/auth/ChangePasswordModal.tsx` — modify
- `frontend/src/pages/ChangePasswordPage.tsx` — modify

**Change description**
Both files:
- Added `confirmError` state: `useState('')`
- `confirmPassword` onChange: if value && value !== newPassword → `setConfirmError(mismatch key)` else clear
- Added `maxLength={128}` to new-password and confirm-password inputs
- Added `{confirmError && <p className="field-error">}` under confirm input
- Added `<p className="field-hint">{t('auth.change_password.hint_password')}</p>` under new-password input
- Note: `ChangePasswordPage` uses `auth.force_change.*` for labels but reuses `auth.change_password.hint_password` + `auth.change_password.error.mismatch` (existing key)

**Review criteria**
- [x] `confirmError` clears when confirm matches new password
- [x] `confirmError` fires on every onChange, not just onBlur
- [x] `maxLength={128}` on both new-password and confirm inputs
- [x] `.field-hint` uses existing `auth.change_password.hint_password` key (no duplication)
- [x] No duplicate JSX blocks

**Test command**
```bash
cd frontend && npm run build 2>&1 | tail -5
```

**Parallel**: safe
**Size estimate**: ~20 lines changed across 2 files

---

### T002: Add hint_password to auth.change_password in frontend-spa 4 locales

**Status**: DONE

**File(s)**
- `frontend/src/i18n/locales/en.json` — modify
- `frontend/src/i18n/locales/vi.json` — modify
- `frontend/src/i18n/locales/ja.json` — modify
- `frontend/src/i18n/locales/ko.json` — modify

**Change description**
Added `hint_password` under `auth.change_password` in all 4 locales: "8–128 characters" (en), "8–128 ký tự" (vi), "8〜128文字" (ja), "8~128자" (ko).

**Review criteria**
- [x] Key present in all 4 locales under `auth.change_password`
- [x] No duplicate keys

**Parallel**: safe
**Size estimate**: 1 line × 4 files

---
