## Code Review: S005 — Integration tests — query endpoint full coverage
Level: full | Date: 2026-04-13 | Reviewer: Claude Opus 4.6

### Task Review Criteria

**T001 — test_query.py AC1–AC6/AC9/AC11**
- [x] AC1 parametrized across ja/en/vi/ko — `test_ac1_happy_path_multilang` uses `@pytest.mark.parametrize("lang", ["ja", "en", "vi", "ko"])` and asserts 200 + answer per language
- [x] AC2 asserts `group_ids` argument to `mock_search` (not content comparison) — `test_ac2_rbac_group_ids_passed_to_search` inspects `search_mock.call_args.kwargs["user_group_ids"] == [7, 8, 9]`
- [x] AC3 all 3 invalid input variants trigger 422 — parametrized with (query>512, top_k=0, top_k=101)
- [x] AC9 asserts `low_confidence=True` in response body — `test_ac9_low_confidence_flag_set`
- [x] All test functions named `test_ac{N}_*` for traceability — AC1/AC2/AC3/AC4/AC5/AC6/AC9/AC11 all follow convention

**T002 — test_rate_limiter.py AC10**
- [x] AC10 test asserts 429 status + `RATE_LIMIT_EXCEEDED` code — `test_ac10_rate_limit_exceeded_returns_429`
- [x] AC10 test asserts `X-RateLimit-Remaining: 0` header — line 244
- [x] Complementary 200 test asserts `X-RateLimit-Remaining: 59` — `test_ac10_rate_limit_not_exceeded_returns_200_with_remaining`
- [x] No real Valkey connection in test (pure mock) — patches `_rate_limiter.check` directly; no Valkey client instantiated

**T003 — Coverage gate**
- [x] All tests pass (42/42 reported by caller)
- [x] Coverage 95% reported — well above 80% threshold
- [x] AC12 satisfied (`--cov-fail-under=80` exits 0 per caller confirmation)
- [x] No skips on AC1–AC6 happy-path tests

### Full Checks
- [x] All review_criteria from task file satisfied
- [x] Test command passes (42/42, coverage 95%)
- [x] No files outside TOUCH list modified — only `tests/api/test_query.py` and `tests/api/test_rate_limiter.py` in scope
- [x] Error handling: external calls have try/except — N/A (test-only story; production code not modified)
- [x] No magic numbers — `limit=60/window=60`, `top_k=0/101`, `confidence=0.3/0.9`, `query="x"*513` all map directly to documented AC boundaries (512-char limit, top_k range 1–100, confidence threshold 0.4). These are AC inputs, not unexplained constants.
- [x] No commented-out dead code — comments are purely spec/AC traceability pointers

### HARD/ARCH Post-Checks
- [x] R003 (Auth on every endpoint): `test_ac4_no_auth_returns_401` explicitly verifies 401 without `verify_token` override — correctly exercises real auth dependency
- [x] R006 (Audit log): `test_ac8_audit_log_called_as_background_task` present (pre-existing) and S005 tests preserve `mock_audit` usage
- [x] R002 (PII in vector metadata): `test_sources_are_doc_ids` asserts sources are doc_id strings, not content
- [x] A005 (Error response shape): AC6 error test validates `body["error"]["request_id"]`; AC10 validates `body["error"]["code"]`
- [x] S004 (Rate limiting): AC10 tests cover both exceeded and non-exceeded paths with headers

### Issues Found

#### ❌ BLOCKER
None.

#### ⚠️ WARNING
- **Minor — duplication of setup boilerplate** in `test_rate_limiter.py` AC10 tests. `test_ac10_rate_limit_exceeded_returns_429` and `test_ac10_rate_limit_not_exceeded_returns_200_with_remaining` each re-import FastAPI/TestClient/router/user types and rebuild the app locally rather than reusing a fixture. Same pattern already present in `test_rate_limit_headers_present` (pre-existing). Non-blocking, but a future refactor could extract a shared `query_app` fixture. Acceptable for a test-only story delivered under tight scope.
- **Minor — `raise_server_exceptions=False`** used only in `test_ac4_no_auth_returns_401` (intentional: `verify_token` is real and raises HTTPException 401 before DB access — confirmed correct per review notes).
- **Out-of-scope** — 7 pre-existing failures in `test_query_rbac.py` (stale patches) noted but explicitly tracked separately; not part of S005 scope.

### Verdict
[x] APPROVED  [ ] CHANGES REQUIRED  [ ] REJECTED

**Summary**: All 8 T001 review criteria, 4 T002 criteria, and 5 T003 criteria are satisfied. Tests use proper AC-numbered naming, parametrization where prescribed (AC1 languages, AC3 invalid inputs), and correctly assert RBAC contract via `call_args.kwargs`. Rate-limiter tests stay pure-mock with no real Valkey dependency. HARD rules R002/R003/R006 and ARCH A005 are exercised. 42/42 pass, 95% coverage exceeds the 80% gate. Warnings are cosmetic only and do not block approval.
