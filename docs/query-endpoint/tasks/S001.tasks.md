# Tasks: query-endpoint / S001 — Wire search() into POST /v1/query
Created: 2026-04-08 | Agent: api-agent | Status: REVIEWED

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S001: Wire search() into POST /v1/query |
| Total tasks | 5 |
| Parallel groups | G1: [T001], G2: [T002, T003], G3: [T004], G4: [T005] |
| Critical path | T001 → T002 → T004 → T005 |
| Agent | api-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Remove embed() helper + fix sources PII bug | DONE | safe | T002, T003 |
| T002 | Add lang field to QueryRequest schema | DONE | after:T001 | T004 |
| T003 | Scaffold test file with S001 fixtures | DONE | after:T001 | T004, T005 |
| T004 | Replace inline retrieval with search() + timeout | DONE | after:T002,T003 | T005 |
| T005 | Add audit log BackgroundTask + AC coverage tests | DONE | after:T004 | — |

---

## LAYER 2 — Task Detail

### T001: Remove embed() helper + fix sources PII bug

**Status**: TODO

**File(s)**
- `backend/api/routes/query.py` — modify: delete `embed()` helper (lines 57–64), change `sources` value

**Change description**
Delete the `embed()` helper function (lines 57–64) that bypasses language detection.
Change `sources=[d.content for d in docs]` to `sources=[str(d.doc_id) for d in docs]`.
This is a critical-path PII fix (R002) that must land before any other S001 change.
No new logic — deletion + one-line substitution only.

**Review criteria**
- [ ] `embed()` function no longer exists in `query.py`
- [ ] `sources` field uses `d.doc_id` (not `d.content`)
- [ ] R002 satisfied: no PII/content in response metadata
- [ ] No other references to removed `embed()` remain in the file

**Test command**
```bash
pytest tests/api/test_query.py::test_sources_are_doc_ids -v
```

**Parallel**: safe
**Size estimate**: ~8 lines deleted, ~1 line changed

---

### T002: Add lang field to QueryRequest schema

**Status**: TODO

**File(s)**
- `backend/api/schemas.py` — modify: add `lang: str | None = None` to `QueryRequest`

**Change description**
Add optional `lang: str | None = None` field to the `QueryRequest` Pydantic model.
This is the body-field override for language detection (A1 confirmed, D4 decision).
Field must be nullable — None means auto-detect; a string value (e.g. "ja") skips detection.
No validator needed — search() handles invalid lang codes gracefully.

**Review criteria**
- [ ] `QueryRequest.lang` field present with default `None`
- [ ] Pydantic model serializes correctly (lang absent in request = None)
- [ ] No hardcoded language fallback in schema

**Test command**
```bash
pytest tests/api/test_query.py::test_query_request_lang_optional -v
```

**Parallel**: after:T001
**Size estimate**: ~2 lines changed

---

### T003: Scaffold test file with S001 fixtures

**Status**: TODO

**File(s)**
- `tests/api/test_query.py` — create (or extend if exists): add fixtures + stubs for S001

**Change description**
Create (or extend) the test file with:
- `mock_search` fixture patching `backend.rag.search.search` to return 2 fake `RetrievedDocument` objects
- `mock_audit_log` fixture patching `backend.rag.audit.write`
- `client` fixture using FastAPI `TestClient` with `verify_token` dependency overridden
- Stub test `test_sources_are_doc_ids` and `test_query_request_lang_optional` as placeholder bodies

**Review criteria**
- [ ] `mock_search` returns `RetrievedDocument` with `doc_id` and `lang` fields
- [ ] `verify_token` override injects a valid `AuthenticatedUser`
- [ ] File importable without errors (`pytest --collect-only`)
- [ ] No real DB or Valkey connections at import time

**Test command**
```bash
pytest tests/api/test_query.py --collect-only
```

**Parallel**: after:T001
**Size estimate**: ~40 lines (fixtures only)

---

### T004: Replace inline retrieval with search() + timeout

**Status**: TODO

**File(s)**
- `backend/api/routes/query.py` — modify: replace stub with `search()` call wrapped in `asyncio.wait_for`

**Change description**
Import `search` from `backend.rag.search`.
Replace any remaining inline `retrieve()` or embed stub with:
`docs = await asyncio.wait_for(search(query=..., lang=request.lang, group_ids=...), timeout=1.0)`
Propagate `asyncio.TimeoutError` as `QueryTimeoutError` (must not be swallowed).
Pass `user.group_ids` from the injected `AuthenticatedUser`; 0-group users pass `[]` → public results (D04).

**Review criteria**
- [ ] `search()` called — no inline `embed()` or `retrieve()` remains
- [ ] `asyncio.wait_for(..., timeout=1.0)` wraps the search call
- [ ] `asyncio.TimeoutError` re-raised as `QueryTimeoutError`
- [ ] 0-group user gets results (not 403) — R001 satisfied at DB layer
- [ ] A003: `lang` passed through to `search()` (not hardcoded)

**Test command**
```bash
pytest tests/api/test_query.py -k "search" -v
```

**Parallel**: after:T002,T003
**Size estimate**: ~15 lines changed

---

### T005: Add audit log BackgroundTask + AC coverage tests

**Status**: TODO

**File(s)**
- `backend/api/routes/query.py` — modify: add `BackgroundTasks` param + `audit_log.write` call
- `tests/api/test_query.py` — modify: fill in AC1–AC8 test bodies

**Change description**
Add `background_tasks: BackgroundTasks` to the route signature.
Call `background_tasks.add_task(audit_log.write, user.user_id, [d.doc_id for d in docs], query_hash)`.
Then fill in test bodies for AC1 (happy path), AC6 (timeout → QueryTimeoutError), AC7 (0-group → results),
AC8 (audit log called as background task). Use `mock_search` and `mock_audit_log` fixtures from T003.

**Review criteria**
- [ ] R006 satisfied: audit log written for every retrieval
- [ ] Audit log is a `BackgroundTask` (non-blocking)
- [ ] `query_hash` derived from `hashlib.sha256(request.query.encode()).hexdigest()`
- [ ] AC1 test: POST /v1/query returns 200 with `answer=null` (no LLM yet), `sources=[doc_id]`
- [ ] AC6 test: `asyncio.TimeoutError` from `mock_search` → `QueryTimeoutError` raised
- [ ] AC7 test: 0-group user gets 200 with results
- [ ] AC8 test: `mock_audit_log.write` called once per request

**Test command**
```bash
pytest tests/api/test_query.py -k "ac1 or ac6 or ac7 or ac8" -v
```

**Parallel**: after:T004
**Size estimate**: ~30 lines code, ~35 lines tests

---
