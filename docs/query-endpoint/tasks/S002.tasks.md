# Tasks: query-endpoint / S002 — Wire generate_answer() for AI responses
Created: 2026-04-08 | Agent: api-agent | Status: DONE

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S002: Wire generate_answer() for AI responses |
| Total tasks | 4 |
| Parallel groups | G1: [T001], G2: [T002], G3: [T003, T004] |
| Critical path | T001 → T002 → T003 |
| Agent | api-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Add mock_generate fixture + AC scaffolding | DONE | safe | T002 |
| T002 | Call generate_answer() with LLM timeout | DONE | after:T001 | T003, T004 |
| T003 | Handle NoRelevantChunksError as 200 null answer | DONE | after:T002 | — |
| T004 | Set low_confidence + reason field + AC tests | DONE | after:T002 | — |

---

## LAYER 2 — Task Detail

### T001: Add mock_generate fixture + AC scaffolding

**Status**: TODO

**File(s)**
- `tests/api/test_query.py` — modify: add `mock_generate_answer` fixture + stub AC1–AC8 test functions

**Change description**
Add `mock_generate_answer` fixture that patches `backend.rag.generator.generate_answer`.
Default return: `LLMResponse(answer="test answer", confidence=0.9, sources=["doc-001"])`.
Scaffold stub test functions for S002 ACs: `test_answer_returned_on_success`,
`test_no_answer_on_empty_docs`, `test_llm_timeout_503`, `test_low_confidence_flag`,
`test_reason_field_when_null`, `test_llm_unavailable_503`.

**Review criteria**
- [ ] `mock_generate_answer` patches correct import path
- [ ] `LLMResponse` fields match actual model: `answer`, `confidence`, `sources`
- [ ] All stub tests collectible without errors

**Test command**
```bash
pytest tests/api/test_query.py --collect-only -q
```

**Parallel**: safe
**Size estimate**: ~30 lines

---

### T002: Call generate_answer() with LLM timeout

**Status**: TODO

**File(s)**
- `backend/api/routes/query.py` — modify: add `generate_answer()` call with `asyncio.wait_for(timeout=0.8)`

**Change description**
After `search()` returns non-empty `docs`, call:
`response = await asyncio.wait_for(generate_answer(docs=docs, query=request.query), timeout=0.8)`
Set `QueryResponse.answer = response.answer`.
If `docs` is empty, skip LLM call entirely — `answer` stays null (AC7).
`asyncio.TimeoutError` from LLM → HTTP 503 with `LLM_UNAVAILABLE` code.
`LLMUnavailableError` → HTTP 503 with same code (AC6).

**Review criteria**
- [ ] `generate_answer()` called only when `docs` is non-empty
- [ ] `asyncio.wait_for(..., timeout=0.8)` wraps the LLM call (A2: 0.8s budget)
- [ ] `asyncio.TimeoutError` → 503 `LLM_UNAVAILABLE`
- [ ] Total pipeline stays within 1.8s SLA (R007 / P001)
- [ ] `LLMUnavailableError` handled → 503

**Test command**
```bash
pytest tests/api/test_query.py -k "answer or llm" -v
```

**Parallel**: after:T001
**Size estimate**: ~18 lines changed

---

### T003: Handle NoRelevantChunksError as 200 null answer

**Status**: TODO

**File(s)**
- `backend/api/routes/query.py` — modify: add `except NoRelevantChunksError` block inside route handler

**Change description**
Wrap the `generate_answer()` call in a try/except for `NoRelevantChunksError`.
On catch: return `QueryResponse(answer=None, reason="no_relevant_chunks", sources=[], low_confidence=False)`.
Status code must be 200 — NOT 4xx/5xx (D09 decision).
`reason` field must only be populated when `answer` is None.

**Review criteria**
- [ ] `NoRelevantChunksError` caught and returns 200
- [ ] Response body: `{answer: null, reason: "no_relevant_chunks", sources: []}`
- [ ] D09 decision respected — no error HTTP status for this case
- [ ] `reason` absent (or null) on success responses

**Test command**
```bash
pytest tests/api/test_query.py::test_no_relevant_chunks_200 -v
```

**Parallel**: after:T002
**Size estimate**: ~8 lines changed

---

### T004: Set low_confidence + reason field + AC tests

**Status**: TODO

**File(s)**
- `backend/api/routes/query.py` — modify: set `low_confidence` and `reason` from `LLMResponse`
- `tests/api/test_query.py` — modify: fill AC test bodies for low_confidence and reason

**Change description**
After successful `generate_answer()`: set `low_confidence = (response.confidence < 0.4)` (C014).
Populate `QueryResponse.sources` from `[str(d.doc_id) for d in docs]` (already fixed in S001).
`reason` field: set to `None` on success (not "no_relevant_chunks").
Fill test bodies: `test_low_confidence_flag` (mock confidence=0.3 → low_confidence=True),
`test_reason_field_when_null` (success response → reason absent or null).

**Review criteria**
- [ ] `low_confidence = True` when `response.confidence < 0.4`
- [ ] `low_confidence = False` when `response.confidence >= 0.4`
- [ ] C014 threshold (0.4) not hardcoded as magic number — use named constant or env var
- [ ] `reason` is None/absent on success responses

**Test command**
```bash
pytest tests/api/test_query.py -k "confidence or reason" -v
```

**Parallel**: after:T002
**Size estimate**: ~10 lines code, ~20 lines tests

---
