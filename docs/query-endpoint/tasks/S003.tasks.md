# Tasks: query-endpoint / S003 — Rate limiting — 60 req/min per user
Created: 2026-04-08 | Agent: api-agent | Status: DONE ✅ 2026-04-13

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S003: Rate limiting — 60 req/min per user |
| Total tasks | 4 |
| Parallel groups | G1: [T001], G2: [T002], G3: [T003, T004] |
| Critical path | T001 → T002 → T003 |
| Agent | api-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Add VALKEY_URL to config + scaffold test file | DONE ✅ | safe | T002 |
| T002 | Implement RateLimiter sliding window class | DONE ✅ | after:T001 | T003, T004 |
| T003 | Register middleware in app.py + add headers | DONE ✅ | after:T002 | — |
| T004 | Fail-open behavior + AC coverage tests | DONE ✅ | after:T002 | — |

---

## LAYER 2 — Task Detail

### T001: Add VALKEY_URL to config + scaffold test file

**Status**: TODO

**File(s)**
- `backend/api/config.py` — modify: add `VALKEY_URL: str = os.getenv("VALKEY_URL", "valkey://localhost:6379")`
- `tests/api/test_rate_limiter.py` — create: scaffold with `mock_valkey` fixture

**Change description**
Add `VALKEY_URL` env var to the app config (A3: new var, not yet defined).
Scaffold `tests/api/test_rate_limiter.py` with a `mock_valkey` fixture that patches the Valkey client,
returning controllable `ZADD`/`ZCOUNT` responses. Add stub test functions for AC1–AC7.

**Review criteria**
- [ ] `VALKEY_URL` in config — not hardcoded anywhere in source
- [ ] S005 (SECURITY): no default URL with credentials hardcoded
- [ ] Test file collectible without real Valkey connection

**Test command**
```bash
pytest tests/api/test_rate_limiter.py --collect-only -q
```

**Parallel**: safe
**Size estimate**: ~5 lines config, ~30 lines test scaffold

---

### T002: Implement RateLimiter sliding window class

**Status**: TODO

**File(s)**
- `backend/api/middleware/rate_limiter.py` — create: sliding window rate limiter using Valkey ZADD/ZCOUNT

**Change description**
Create `RateLimiter` class with:
- `__init__(self, resource: str, limit: int = 60, window: int = 60)` — parametric (AC5)
- `async def check(self, user_id: str, valkey_client) -> tuple[bool, int, int]` — returns `(allowed, remaining, reset_ts)`
- Key pattern: `ratelimit:{resource}:{user_id}` (AC4)
- Sliding window: `ZADD key now now`, `ZREMRANGEBYSCORE key 0 window_start`, `ZCOUNT key window_start +inf`
- Use Valkey (valkey-py), NOT redis-py (C016 / AC3)

**Review criteria**
- [ ] Key format: `ratelimit:query:{user_id}` for query resource
- [ ] ZADD/ZREMRANGEBYSCORE/ZCOUNT — sliding window, not fixed bucket (AC2)
- [ ] `limit` and `window` are constructor params — not hardcoded
- [ ] Uses `valkey` package import — not `redis`

**Test command**
```bash
pytest tests/api/test_rate_limiter.py -k "sliding_window or key_format" -v
```

**Parallel**: after:T001
**Size estimate**: ~40 lines

---

### T003: Register middleware in app.py + add headers

**Status**: TODO

**File(s)**
- `backend/api/app.py` — modify: instantiate `RateLimiter` + add as dependency on `/v1/query` route
- `backend/api/routes/query.py` — modify: inject `X-RateLimit-Remaining` and `X-RateLimit-Reset` response headers

**Change description**
In `app.py`: create Valkey connection pool at startup using `VALKEY_URL` from config.
Inject `RateLimiter(resource="query", limit=60, window=60)` as a FastAPI dependency on the query route.
In `query.py`: after rate-limit check, set response headers:
`X-RateLimit-Remaining: {remaining}` and `X-RateLimit-Reset: {reset_ts}` (AC7).
On limit exceeded: return HTTP 429 with A005 error shape `{"error": {"code": "RATE_LIMIT_EXCEEDED", ...}}`.

**Review criteria**
- [ ] Valkey pool created at startup — not per-request (PERF P005 analogue)
- [ ] `X-RateLimit-Remaining` header present on all `/v1/query` 200 responses
- [ ] `X-RateLimit-Reset` header present (Unix timestamp)
- [ ] 429 response follows A005 shape
- [ ] AC1: 60 requests succeed; 61st returns 429

**Test command**
```bash
pytest tests/api/test_rate_limiter.py -k "headers or 429" -v
```

**Parallel**: after:T002
**Size estimate**: ~20 lines changed across 2 files

---

### T004: Fail-open behavior + AC coverage tests

**Status**: TODO

**File(s)**
- `backend/api/middleware/rate_limiter.py` — modify: add try/except around Valkey calls
- `tests/api/test_rate_limiter.py` — modify: fill all AC test bodies

**Change description**
Wrap all Valkey operations in `try/except Exception`: on error, log warning and return `(True, limit, reset_ts)` — fail-open (AC6).
Fill test bodies:
- `test_60th_request_allowed`, `test_61st_request_429` (AC1)
- `test_sliding_window_not_fixed_bucket` (AC2)
- `test_valkey_import_not_redis` (AC3 — import check)
- `test_key_includes_user_id` (AC4)
- `test_rate_limiter_parametric` (AC5)
- `test_fail_open_on_valkey_error` (AC6)
- `test_rate_limit_headers_present` (AC7)

**Review criteria**
- [ ] Valkey error → request allowed (not rejected)
- [ ] Warning logged on Valkey failure (not silent)
- [ ] All 7 AC tests passing
- [ ] No `redis` package imported anywhere in rate_limiter.py

**Test command**
```bash
pytest tests/api/test_rate_limiter.py -v
```

**Parallel**: after:T002
**Size estimate**: ~10 lines code, ~40 lines tests

---
