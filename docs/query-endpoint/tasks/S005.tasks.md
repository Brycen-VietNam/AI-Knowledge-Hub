# Tasks: query-endpoint / S005 — Integration tests — query endpoint full coverage
Created: 2026-04-08 | Agent: api-agent | Status: REVIEWED ✅ 2026-04-13

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S005: Integration tests — query endpoint full coverage |
| Total tasks | 3 |
| Parallel groups | G1: [T001, T002], G2: [T003] |
| Critical path | T001 → T003 |
| Agent | api-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Extend test_query.py: AC1–AC6 parametrized tests | DONE ✅ | safe | T003 |
| T002 | Extend test_rate_limiter.py: AC10 mock Valkey | DONE ✅ | safe | T003 |
| T003 | Run full suite + enforce 80% coverage gate | DONE ✅ | after:T001,T002 | — |

---

## LAYER 2 — Task Detail

### T001: Extend test_query.py: AC1–AC6 parametrized tests

**Status**: TODO

**File(s)**
- `tests/api/test_query.py` — modify: fill remaining AC gaps, add parametrized lang test

**Change description**
Complete all remaining test bodies:
- AC1: `@pytest.mark.parametrize("lang", ["ja", "en", "vi", "ko"])` — happy path, assert 200 + answer
- AC2: RBAC isolation — mock `search()` to assert `group_ids` passed correctly; simulate group A/B separation
- AC3: input validation — query >512 chars → 422; top_k=0 → 422; top_k=101 → 422
- AC4: no auth header → 401
- AC5: 0-group user → 200 with results (not 403) — D04 regression guard
- AC6: request_id present in success + error responses
- AC9: `low_confidence=True` when mock LLM returns `confidence=0.3`
- AC11: `NoRelevantChunksError` → 200 `{answer: null, reason: "no_relevant_chunks"}`

**Review criteria**
- [ ] AC1 parametrized across ja/en/vi/ko — all 4 pass
- [ ] AC2 asserts `group_ids` argument to `mock_search` (not content comparison)
- [ ] AC3 all 3 invalid input variants trigger 422
- [ ] AC9 asserts `low_confidence=True` in response body
- [ ] All test functions named `test_ac{N}_*` for traceability

**Test command**
```bash
pytest tests/api/test_query.py -v
```

**Parallel**: safe
**Size estimate**: ~80 lines tests

---

### T002: Extend test_rate_limiter.py: AC10 mock Valkey

**Status**: TODO

**File(s)**
- `tests/api/test_rate_limiter.py` — modify: add AC10 rate limit simulation test

**Change description**
AC10: patch `RateLimiter.check` to return `(False, 0, reset_ts)` — simulates exceeded window.
Assert query route returns HTTP 429 with A005 shape and `X-RateLimit-Remaining: 0`.
Add complementary test: `check` returns `(True, 59, reset_ts)` → 200 with `X-RateLimit-Remaining: 59`.
Ensure Valkey mock is the only external dependency (no real Valkey in CI).

**Review criteria**
- [ ] AC10 test asserts 429 status + `RATE_LIMIT_EXCEEDED` code
- [ ] AC10 test asserts `X-RateLimit-Remaining: 0` header
- [ ] Complementary 200 test asserts `X-RateLimit-Remaining: 59`
- [ ] No real Valkey connection in test (pure mock)

**Test command**
```bash
pytest tests/api/test_rate_limiter.py -k "ac10 or remaining" -v
```

**Parallel**: safe
**Size estimate**: ~25 lines tests

---

### T003: Run full suite + enforce 80% coverage gate

**Status**: TODO

**File(s)**
- `tests/api/test_query.py` — no code change; run with coverage flags
- `tests/api/test_rate_limiter.py` — no code change; include in coverage run

**Change description**
Run the full test suite with coverage enforcement.
Command: `pytest tests/api/ --cov=backend/api/routes/query.py --cov=backend/api/middleware/rate_limiter.py --cov-fail-under=80 -v`
If coverage < 80%: identify missing branches, add targeted tests in T001/T002 scope (do not write new task files — patch inline).
Verify AC12: coverage report must show ≥80% for both `query.py` and `rate_limiter.py`.
Record final pass/fail counts in the WARM sync note.

**Review criteria**
- [ ] All tests pass (0 failures, 0 errors)
- [ ] `query.py` coverage ≥ 80%
- [ ] `rate_limiter.py` coverage ≥ 80%
- [ ] AC12 explicitly satisfied: `--cov-fail-under=80` exits 0
- [ ] No skips on happy-path ACs (AC1–AC6)

**Test command**
```bash
pytest tests/api/ --cov=backend/api/routes/query.py --cov=backend/api/middleware/rate_limiter.py --cov-fail-under=80 -v
```

**Parallel**: after:T001,T002
**Size estimate**: 0 lines (run-only task)

---
