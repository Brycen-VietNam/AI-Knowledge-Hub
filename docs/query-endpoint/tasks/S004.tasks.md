# Tasks: query-endpoint / S004 — Structured error handling & observability
Created: 2026-04-08 | Agent: api-agent | Status: DONE ✅ 2026-04-13

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S004: Structured error handling & observability |
| Total tasks | 3 |
| Parallel groups | G1: [T001], G2: [T002, T003] |
| Critical path | T001 → T002 |
| Agent | api-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Add input validators to QueryRequest | DONE ✅ | safe | T002, T003 |
| T002 | Register RAG exception handlers in app.py | DONE ✅ | after:T001 | — |
| T003 | Verify request_id threads through + error tests | DONE ✅ | after:T001 | — |

---

## LAYER 2 — Task Detail

### T001: Add input validators to QueryRequest

**Status**: TODO

**File(s)**
- `backend/api/schemas.py` — modify: add Pydantic validators to `QueryRequest`

**Change description**
Add Pydantic field constraints to `QueryRequest`:
- `query: str = Field(..., max_length=512)` — S003 input sanitization (SECURITY S003)
- `top_k: int = Field(default=5, ge=1, le=100)` — valid range 1–100 (AC3)
Pydantic `ValidationError` → FastAPI auto-returns HTTP 422; no custom handler needed.
Strip control characters from `query` using a `@field_validator` (SECURITY S003).

**Review criteria**
- [ ] `query` max 512 chars enforced at schema level
- [ ] `top_k` range 1–100 enforced (ge=1, le=100)
- [ ] Control chars stripped from `query` before it reaches the route handler
- [ ] 422 returned for invalid input (not 400 or 500)

**Test command**
```bash
pytest tests/api/test_query.py -k "validation or top_k or query_length" -v
```

**Parallel**: safe
**Size estimate**: ~12 lines changed

---

### T002: Register RAG exception handlers in app.py

**Status**: TODO

**File(s)**
- `backend/api/app.py` — modify: register `@app.exception_handler` for RAG exceptions

**Change description**
Register FastAPI exception handlers for:
- `LanguageDetectionError` → HTTP 422, code `LANG_DETECT_FAILED`
- `UnsupportedLanguageError` → HTTP 422, code `UNSUPPORTED_LANGUAGE`
- `EmbedderError` → HTTP 503, code `EMBEDDER_UNAVAILABLE`
- `QueryTimeoutError` → HTTP 504, code `QUERY_TIMEOUT`
- `LLMUnavailableError` → HTTP 503, code `LLM_UNAVAILABLE`
All responses must use A005 shape: `{"error": {"code": "...", "message": "...", "request_id": "..."}}`.
Reuse the `error_response()` helper from `backend/auth/_errors.py` (or equivalent).
No stack traces in response body (A005 / SECURITY).

**Review criteria**
- [ ] All 5 exception types handled with correct HTTP codes
- [ ] A005 shape: `error.code`, `error.message`, `error.request_id` present
- [ ] No stack trace or internal path in any error response
- [ ] `request_id` sourced from request state (not re-generated per handler)

**Test command**
```bash
pytest tests/api/test_query.py -k "error" -v
```

**Parallel**: after:T001
**Size estimate**: ~35 lines

---

### T003: Verify request_id threads through + error tests

**Status**: TODO

**File(s)**
- `backend/api/routes/query.py` — modify: ensure `request_id` stored in request state at route entry
- `tests/api/test_query.py` — modify: add AC4 (401 regression), AC5 (no 403 for 0-group), AC6 (request_id present)

**Change description**
Verify that the `request_id = str(uuid4())` generated at route entry is stored in `request.state.request_id`.
Exception handlers must read from `request.state.request_id` — not generate a new one.
Fill test bodies:
- `test_request_id_in_success_response` (AC6 — success path)
- `test_request_id_in_error_response` (AC6 — error path, check same id)
- `test_401_not_regressed` (AC4 — no auth → 401)
- `test_no_403_for_zero_group_user` (AC5 — D04 guard)
- `test_no_stack_trace_in_error` (AC2 — assert "Traceback" not in body)

**Review criteria**
- [ ] `request_id` same in success AND error responses for same request
- [ ] AC4: missing/invalid token → 401 (not 403 or 500)
- [ ] AC5: 0-group user → 200 (not 403)
- [ ] AC2: no "Traceback" string in any error response body
- [ ] All 5 test functions passing

**Test command**
```bash
pytest tests/api/test_query.py -k "request_id or 401 or 403 or stack_trace" -v
```

**Parallel**: after:T001
**Size estimate**: ~8 lines code, ~35 lines tests

---
