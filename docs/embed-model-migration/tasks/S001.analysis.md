# Analysis: S001 — OllamaEmbedder refactor (T001–T005)
Depth: shallow + targeted deep on TOUCH files | Files scanned: 4 (embedder.py, test_embedder.py, chunker.py, config.py) + caller grep

---

## Code Map (relevant only)

### [backend/rag/embedder.py](backend/rag/embedder.py)
```
EMBEDDING_MODEL = os.getenv("EMBEDDING_MODEL", "mxbai-embed-large")    # L17 — flip in T001

class OllamaEmbedder:
  __init__(self)                                                       # L28 — uses module-level EMBEDDING_MODEL
  embed_one(self, text)                          → list[float]         # L32 — public, REMOVE in T005
  _MAX_EMBED_CHARS: int  (env OLLAMA_MAX_EMBED_CHARS, default 1400)    # L38 — keep
  _embed_one(self, text)                         → list[float]         # L40 — RENAME to _embed(prompt) in T002
    └─ truncates text[:1400] BEFORE httpx POST                         # L42 — must move to AFTER prefix
  batch_embed(self, chunks, batch_size=32)       → list[list[float]]   # L52 — REPLACE in T004

async def insert_embeddings(chunks, vectors, doc, db)                  # L62 — UNCHANGED (S002 territory)
```

### [tests/rag/test_embedder.py](tests/rag/test_embedder.py) (Docker-safe path — confirmed)
```
test_embed_one_returns_list_of_floats           L24  — DELETE in T005 (covers removed API)
test_embed_one_raises_embedder_error_on_500     L41  — REWRITE in T002 to test _embed() helper
test_batch_embed_single_batch                   L60  — REWRITE in T004 → batch_embed_passage
test_batch_embed_multiple_batches               L74  — REWRITE in T004
test_batch_embed_order_preserved                L88  — REWRITE in T004
test_insert_embeddings_success                  L109 — KEEP (no S001 surface)
test_insert_embeddings_failure_sets_status      L134 — KEEP
```

### Mock pattern to reuse (verified L26–L37)
```python
mock_resp = MagicMock(); mock_resp.status_code = 200
mock_resp.json.return_value = {"embedding": [0.1]*1024}
with patch("backend.rag.embedder.httpx.AsyncClient") as mock_client_cls:
    mock_client = AsyncMock()
    mock_client.post = AsyncMock(return_value=mock_resp)
    mock_client_cls.return_value.__aenter__ = AsyncMock(return_value=mock_client)
    mock_client_cls.return_value.__aexit__ = AsyncMock(return_value=False)
```
→ T002/T003 prefix tests should inspect `mock_client.post.call_args.kwargs["json"]["prompt"]` to assert the outgoing prefixed string.

---

## Caller Map (pre-flight for T005)

| Caller | Method | Line | Story owning the swap |
|--------|--------|------|-----------------------|
| `backend/api/routes/documents.py` | `embedder.batch_embed(chunks)` | L95 | **S002** |
| `backend/rag/query_processor.py` | `_embedder.embed_one(text)` | L49 | **S003** ⚠️ NEW FINDING — see below |
| `backend/rag/retriever.py` | (none directly — uses query_processor via search.py) | — | S003 |

---

## ❌ Conflicts / Gaps Found

### ❌ FINDING-1 (BLOCKING for plan, not S001 itself): S003 target file is wrong
The plan §S003 says modify `backend/rag/retriever.py:_dense_search` to call `embedder.embed_query()`. But the actual call-site is **[backend/rag/query_processor.py:49](backend/rag/query_processor.py#L49)** — `retriever.py`/`search.py` consume `query_processor.embed_query()`. S003 must touch `query_processor.py`, not (or in addition to) `retriever.py`. Must flag to /plan or fold into S003 /tasks.
- **Impact on S001 T005**: pre-flight grep WILL find `query_processor.py` calling `embed_one`. T005 cannot land until S003 swaps that caller too. Coordination note in S001.tasks.md is correct in spirit but understated the scope.

### ❌ FINDING-2: Name collision risk
After S001 lands, `OllamaEmbedder.embed_query` exists. After S003 lands, `query_processor.embed_query` continues to exist as a wrapper. **Same name, different layers** — readable but confusing in stack traces. Not a blocker, just a reviewer note. Recommendation: keep `query_processor.embed_query` (it’s the public façade); call `_embedder.embed_query(text)` inside.

### ✅ FINDING-3 RESOLVED (2026-04-28): `.env.example` DOES exist
Glob initially returned zero — turned out to be a tool-permission filter on `.env*` files. Verified via `ls`: file exists at repo root (1064 bytes, modified 2026-04-24). T001 TOUCH list unchanged — modify is correct.

### ⚠️ FINDING-4: existing test naming will break test_cmd
Tests in `tests/rag/test_embedder.py` use `test_embed_one_*` and `test_batch_embed_*` names. T002 test command `pytest -k "embed_query"` will not collect `embed_one` tests (good), but T005's "delete obsolete tests" must explicitly drop `test_embed_one_*` and `test_batch_embed_single_batch` / `_multiple_batches` / `_order_preserved`. Listed in Code Map above — clean delete list.

### ⚠️ FINDING-5: existing `_embed_one` test reads vector len 3, not 1024
[test_embed_one_returns_list_of_floats L37](tests/rag/test_embedder.py#L37) returns `[0.1, 0.2, 0.3]`. New tests for `embed_query`/`embed_passage` must use `[0.x] * 1024` to satisfy AC2/AC3 dim assertion. T002/T003 helper fixture suggested:
```python
FAKE_VECTOR_1024 = [0.0] * 1024
```

### ✅ NOT a violation
- A004 (config not hardcoded): T001 keeps env-driven default — OK.
- S005 (no hardcoded secrets): no secrets in scope — OK.
- P002 (batch ≥ 32): T004 preserves the existing `asyncio.gather` per slice pattern — OK.
- R002 (no PII in metadata): S001 surface doesn't touch metadata — OK.

---

## Patterns to Follow

1. **Module-level config from env**: see `embedder.py:17` and `chunker.py:15-16`. T001 follows existing style — no changes needed.
2. **Truncation as instance attribute**: `_MAX_EMBED_CHARS` is class-level. Keep that pattern; T002's prefix-then-truncate logic should live in the new `_embed(prompt)` helper:
   ```python
   async def _embed(self, prompt: str) -> list[float]:
       prompt = prompt[: self._MAX_EMBED_CHARS]   # AC6: applied AFTER caller prepends prefix
       # ... existing httpx POST + EmbedderError ...
   ```
3. **Async test marker**: every test uses `@pytest.mark.asyncio` — keep.

---

## Recommended Implementation Order

```
T001  ── create _embed(prompt) helper signature only? NO — T001 is just env flip.
        Touch: embedder.py L17, tests add 1 default-model assertion.
        Skip .env.example (FINDING-3 → defer to S004).

T002  ── Refactor: rename _embed_one → _embed, take pre-prefixed prompt.
        Add embed_query(text): _embed(f"query: {text}").
        Tests: 4 new (prefix, dim=1024, truncation-with-prefix, error).

T003  ── Add embed_passage(text): _embed(f"passage: {text}").
        Tests: 3 new (prefix, dim=1024, truncation-with-prefix).
        Reuses _embed from T002 — must land after T002.

T004  ── batch_embed_passage: same loop shape as old batch_embed,
        but inner gather calls self.embed_passage(c.text).
        Tests: order, batches=32 boundary, all-prefix, error propagation.

T005  ── DELETE embed_one, batch_embed, and obsolete tests (L24–L101).
        ⚠ Pre-flight grep will hit query_processor.py:49 and documents.py:95.
        Both MUST be swapped on S002/S003 branches before T005 merges.
        Add regression test: assert not hasattr(OllamaEmbedder, "embed_one").
```

---

## Plan/Spec Updates Applied

1. ✅ **S003 plan corrected** (2026-04-28): TOUCH list now lists `backend/rag/query_processor.py` (primary caller swap) + `backend/rag/retriever.py` (AC6 cosine fix only). Token estimate bumped ~2k → ~2.5k. Update Log entry added.
2. ✅ **FINDING-3 dismissed**: `.env.example` exists; T001 TOUCH list unchanged.

---

## Token budget
Estimated implementation cost (T001–T005): ~3k tokens (matches plan)
Analysis saved: `docs/embed-model-migration/tasks/S001.analysis.md`

**Next**: `/implement T001` (env flip — smallest, lowest-risk start) OR address FINDING-1 first via plan/spec update.
