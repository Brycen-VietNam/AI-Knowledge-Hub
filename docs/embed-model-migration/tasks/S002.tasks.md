# Tasks: embed-model-migration / S002 — Wire ingest path to `batch_embed_passage`
Created: 2026-04-28 | Agent: api-agent | Status: TODO

Spec: `docs/embed-model-migration/spec/embed-model-migration.spec.md` §S002 (AC1–AC5)
Plan: `docs/embed-model-migration/plan/embed-model-migration.plan.md` §S002
Test file location: `tests/api/test_documents_upload.py` (existing) + new `tests/rag/test_ingest_pipeline.py` (pipeline body coverage)

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S002: Wire ingest path to `batch_embed_passage` |
| Total tasks | 3 |
| Parallel groups | G1: [T001], G2: [T002], G3: [T003] |
| Critical path | T001 → T002 → T003 |
| Agent | api-agent |
| Est. session | 1 (~2k tokens) |

### Task Status Board
| Task | Title | Status | Parallel | Blocks | AC Coverage |
|------|-------|--------|----------|--------|-------------|
| T001 | Swap `batch_embed` → `batch_embed_passage` in ingest_pipeline + comment fix | DONE | safe | T002 | AC1, AC2 |
| T002 | Pipeline-body unit test: prefix routing + dim=1024 + single commit | DONE | after:T001 | T003 | AC3, AC4, AC5 |
| T003 | Multilingual ingest smoke test (JA/EN/VI/KO) — `embeddings.lang` populated | DONE | after:T002 | — | AC3 |

### Pre-flight Verification
```
# Confirm only one caller touches batch_embed in api/routes:
grep -rn "batch_embed\b" backend/api/ --include="*.py"
# Expected: backend/api/routes/documents.py:95 (single hit)

# Confirm new API exists in embedder (S001 T004 merged):
grep -n "batch_embed_passage" backend/rag/embedder.py
# Expected: method definition at module top half
```

---

## LAYER 2 — Task Detail

### T001: Swap `batch_embed` → `batch_embed_passage` in `ingest_pipeline` + comment fix

**Status**: DONE

**File(s)**
- `backend/api/routes/documents.py` — modify
- `tests/api/test_documents_upload.py` — verify still green (no edit expected; pipeline is mocked)

**Change description**
- Line 95: `vectors = await embedder.batch_embed(chunks)` → `vectors = await embedder.batch_embed_passage(chunks)`.
- Line 67 (docstring): update comment `OllamaEmbedder.batch_embed generates vectors;` → `OllamaEmbedder.batch_embed_passage generates passage-prefixed vectors;`.
- Header memory comments: add `# Task: T001 — embed-model-migration S002` near existing `# Task:` block.
- No signature change to `insert_embeddings` (AC2). No DB schema change. No new imports.

**Review criteria**
- [ ] Single line change at L95; docstring at L67 updated to match new method name (AC1)
- [ ] `insert_embeddings(chunks, vectors, doc, db)` call unchanged (AC2)
- [ ] No reverse imports — A001/A002 (api → rag direction preserved)
- [ ] Existing `test_documents_upload.py` (~9 tests) still green — pipeline is mocked at `backend.api.routes.documents.ingest_pipeline`, so swap is invisible to those tests
- [ ] No call to deprecated `batch_embed` remains in `backend/api/`

**Test command**
```bash
pytest tests/api/test_documents_upload.py -v
```

**Parallel**: safe
**Size estimate**: ~3 lines

---

### T002: Pipeline-body unit test — prefix routing + dim=1024 + single commit

**Status**: DONE

**File(s)**
- `tests/rag/test_ingest_pipeline.py` — create

**Change description**
- New test module under `tests/rag/` (Docker-safe, outside `backend/`).
- Tests target `backend.api.routes.documents.ingest_pipeline` directly (not via HTTP).
- TDD ordering: write tests first; T001 swap should make all pass.
- Tests:
  - `test_pipeline_calls_batch_embed_passage`: patch `OllamaEmbedder` → assert `.batch_embed_passage` called once with the chunks list; assert `.batch_embed` NOT called (AC1).
  - `test_pipeline_passes_raw_chunk_text`: capture `batch_embed_passage` arg; assert each chunk's `.text` does NOT start with `"passage: "` (prefix is applied internally by S001 — double-prefix would raise ValueError) (AC4 — prefix contract).
  - `test_pipeline_stores_dim_1024_vectors`: have `batch_embed_passage` return `[[0.0]*1024, ...]`; spy on `insert_embeddings`; assert each forwarded vector has len == 1024 (AC5).
  - `test_pipeline_single_commit_no_per_doc_loop`: assert `db.commit` called exactly once after `db.add_all` (P002, P004, AC4).

**Review criteria**
- [ ] No HTTP layer in tests — pipeline imported directly
- [ ] Mocks: `OllamaEmbedder` class, `async_session_factory`, `update_fts`, `chunk_document` — keep test deterministic
- [ ] dim=1024 fixture used (`[0.0]*1024`), not legacy `[0.1, 0.2, 0.3]` (AC5)
- [ ] Rule satisfied: P002 (batch ≥32 still default — no per-doc loop reintroduced), P004 (single commit), R002 (no PII checked via spy on Embedding rows)
- [ ] No real Ollama call — `httpx.AsyncClient` not patched here because `batch_embed_passage` itself is mocked

**Test command**
```bash
pytest tests/rag/test_ingest_pipeline.py -v
```

**Parallel**: after:T001
**Size estimate**: ~80 lines tests + 0 prod code

---

### T003: Multilingual ingest smoke test (JA/EN/VI/KO) — `embeddings.lang` populated

**Status**: DONE

**File(s)**
- `tests/rag/test_ingest_pipeline.py` — modify (append smoke case)

**Change description**
- Append `test_pipeline_multilingual_lang_populated`: 4 minimal docs, one each in `lang ∈ {ja, en, vi, ko}`. Use existing `chunk_document` (real, not mocked) so `Chunk.lang` is set per chunk.
- Mock `batch_embed_passage` to return `[0.0]*1024` per chunk; spy `db.add_all` to capture inserted `Embedding` rows.
- Assert: rows count > 0 per language; every row's `.lang` matches its source doc's lang; no row has `lang is None` (AC3).
- Use lightweight content per language (1–2 short sentences) so chunker yields ≥1 chunk each. CJK content for ja/ko exercises CJK code path (R005) without asserting tokenizer internals (out of scope).

**Review criteria**
- [ ] 4 langs covered, each yielding at least 1 chunk (AC3)
- [ ] `Embedding.lang` populated per source doc (AC3 explicit)
- [ ] Test uses real `chunk_document` — not mocked — to validate lang propagation end-to-end through chunker → embedder → insert_embeddings
- [ ] No reliance on Ollama running — `OllamaEmbedder` patched at class level
- [ ] Rule satisfied: R002 (`user_group_id` flows from `doc`, never from chunk; spy verifies)

**Test command**
```bash
pytest tests/rag/test_ingest_pipeline.py::test_pipeline_multilingual_lang_populated -v
# Then full file:
pytest tests/rag/test_ingest_pipeline.py -v
```

**Parallel**: after:T002
**Size estimate**: ~40 lines tests

---

## Coordination Notes
- **S001 T005 gate**: After S002 T001 lands AND S003 caller swap lands, S001 T005 (remove `embed_one`/`batch_embed`) becomes unblocked. Notify rag-agent on S001 task board when this story merges.
- **S001 dependency**: All three tasks here assume `OllamaEmbedder.batch_embed_passage` exists — verified DONE 2026-04-28 in `backend/rag/embedder.py`.
- **Test path**: New tests live under `tests/rag/` (not `tests/api/`) because they exercise the pipeline body, which imports rag modules. Per CLAUDE.md test convention, test scope follows the deepest agent boundary touched — here that's rag (chunker + embedder).

## Outputs Mapping (Plan §S002)
- [ ] Ingest caller switched to `batch_embed_passage` (T001 → AC1)
- [ ] 4-language fixture ingest passes integration test (T003 → AC3)
- [ ] Stored vector dim = 1024 verified in test (T002 → AC5)
- [ ] No regression in existing documents.py tests (T001 review criterion)
- [ ] R002 / P002 / P004 contracts preserved (T002 → AC4)
