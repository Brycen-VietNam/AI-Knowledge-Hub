# Tasks: embed-model-migration / S003 — Wire query path to `embed_query` + fix retriever cosine operator
Created: 2026-04-28 | Agent: rag-agent | Status: DONE ✅ 2026-04-29

Spec: `docs/embed-model-migration/spec/embed-model-migration.spec.md` §S003 (AC1–AC5)
Plan: `docs/embed-model-migration/plan/embed-model-migration.plan.md` §S003 (incl. AC6 cosine bug fold-in)
Test files:
- `tests/rag/test_query_processor.py` (existing — patches `_embedder.embed_one`; needs swap to `embed_query`)
- `tests/rag/test_retriever_rbac.py` (existing — uses `_make_row(distance=…)` + `score == 1.0 - distance`; needs cosine score formula update)

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S003: Wire query path to `embed_query` + fix retriever cosine operator |
| Total tasks | 4 |
| Parallel groups | G1: [T001, T003], G2: [T002, T004] |
| Critical path | T001 → T002; T003 → T004 (two independent chains) |
| Agent | rag-agent |
| Est. session | 1 (~2.5k tokens) |

### Task Status Board
| Task | Title | Status | Parallel | Blocks | AC Coverage |
|------|-------|--------|----------|--------|-------------|
| T001 | Swap `_embedder.embed_one` → `embed_query` in `query_processor.embed_query` façade | DONE ✅ | safe | T002 | AC1 |
| T002 | Update `test_query_processor.py` mocks: `embed_one` → `embed_query` (TDD pair to T001) | DONE ✅ | with:T001 | — | AC1 |
| T003 | Cosine fix in `retriever._dense_search`: `<->` → `<=>`, score = `1.0 - dist/2.0` | DONE ✅ | safe | T004 | AC3, AC6 |
| T004 | Update `test_retriever_rbac.py` distance/score expectations for cosine | DONE ✅ | with:T003 | — | AC2, AC3, AC6 |

> TDD pairing: T001+T002 land together as one PR hunk; T003+T004 land together as a second hunk. Test edits **before** prod edits per CLAUDE.md TDD rule.

> AC4 (cross-lingual smoke `"検索する方法"` → EN doc) and AC5 (p95 < 400ms) are deferred to S005 eval harness — they require live Ollama + ingested corpus, not unit-testable. Documented in §Deferred Coverage below.

### Pre-flight Verification
```
# Confirm only one query-time embedder call-site remains (S001 T005 gate):
grep -rn "embed_one\b" backend/ --include="*.py"
# Expected after T001: zero hits in backend/ (legacy method on OllamaEmbedder still present pending S001 T005)

# Confirm retriever still uses L2 operator pre-fix:
grep -n "<->\|<=>" backend/rag/retriever.py
# Expected pre-T003: line 51 has `<->`. Post-T003: line 51 has `<=>`.

# Confirm new embedder API exists (S001 DONE):
grep -n "async def embed_query" backend/rag/embedder.py
# Expected: method definition present
```

---

## LAYER 2 — Task Detail

### T001: Swap `_embedder.embed_one` → `_embedder.embed_query` in `query_processor.embed_query` façade

**Status**: TODO

**File(s)**
- `backend/rag/query_processor.py` — modify

**Change description**
- Line 49: `return await _embedder.embed_one(text)` → `return await _embedder.embed_query(text)`.
- Line 44 (docstring): update `typically 768-dim for multilingual-e5-large` → `1024-dim for multilingual-e5-large` (existing docstring is wrong — E5-large is 1024, not 768).
- Header memory comment: append `# Task: S003-T001 — embed-model-migration: embed_one → embed_query (E5 prefix internal)`.
- No signature change to public façade `query_processor.embed_query(text)` — name collision with `OllamaEmbedder.embed_query` is intentional (plan §S003 note).

**Review criteria**
- [ ] Single line change at L49; docstring dim fix at L44 (AC1)
- [ ] Façade signature `async def embed_query(text: str) -> list[float]` unchanged (call-site in `search.py` not impacted)
- [ ] No `embed_one` reference remains in `backend/rag/query_processor.py`
- [ ] Caller does NOT prepend `"query: "` — prefix applied internally by `OllamaEmbedder.embed_query` (per S001 D-S001-01 double-prefix guard would `ValueError` if violated)
- [ ] Rule satisfied: A002 (rag-agent stays inside `backend/rag/`); A004 unaffected (no weight constants touched)

**Test command**
```bash
pytest tests/rag/test_query_processor.py -v
```

**Parallel**: safe
**Size estimate**: ~2 lines

---

### T002: Update `test_query_processor.py` mocks — `embed_one` → `embed_query`

**Status**: TODO

**File(s)**
- `tests/rag/test_query_processor.py` — modify

**Change description**
- Lines 106, 113, 120 currently mock `mock_embedder.embed_one` and assert `embed_one.assert_called_once_with(...)`. Swap all three to `embed_query`.
- Update `test_embed_query_returns_vector` fixture vector length: `[0.1, 0.2, 0.3] * 256` (768 dim) → `[0.0] * 1024` and assertion `len(result) == 768` → `len(result) == 1024` (AC1 dim alignment with E5-large).
- Update header comment block (L4–L5): add `# Task: S003-T002 — swap embed_one mocks → embed_query (E5 migration)`.
- Add new test `test_embed_query_passes_raw_text_no_prefix`: pass `"hello"` to façade; capture mock call arg; assert it equals `"hello"` exactly (no `"query: "` prefix at façade layer — prefix is OllamaEmbedder's responsibility per S001 AC2).

**Review criteria**
- [ ] All three `embed_one` references on the embedder mock replaced with `embed_query`
- [ ] Vector dim assertion = 1024 (AC1)
- [ ] New no-prefix test asserts façade passes raw text through (guards against accidental double-prefixing)
- [ ] Existing error-propagation test still passes (only the mock attribute name changes — `EmbedderError` behavior identical)
- [ ] Tests fail if T001 not landed (red → green pairing — TDD ordering)

**Test command**
```bash
pytest tests/rag/test_query_processor.py -v
# Expected: 3 existing tests adapted + 1 new no-prefix test; all green after T001 lands
```

**Parallel**: with:T001 (TDD pair — same hunk)
**Size estimate**: ~12 lines (3 mock-name swaps + 1 dim assertion update + ~8 lines new no-prefix test)

---

### T003: Cosine fix in `retriever._dense_search` — `<->` → `<=>` + corrected score formula

**Status**: TODO

**File(s)**
- `backend/rag/retriever.py` — modify

**Change description**
- Line 51 SQL: `e.embedding <-> cast(:query_vec AS vector) AS distance` → `e.embedding <=> cast(:query_vec AS vector) AS distance` (cosine distance operator, matches HNSW `vector_cosine_ops` index from migration `002_add_pgvector_hnsw.sql:25`).
- Line 67 score formula: `score=1.0 - r.distance` → `score=1.0 - (r.distance / 2.0)`. Cosine distance ∈ [0, 2]; halving normalizes to similarity ∈ [0, 1] so weighted merge in `_merge` (RAG_DENSE_WEIGHT=0.7) keeps comparable magnitudes with BM25 `ts_rank`.
- Header memory comment: append `# Task: S003-T003 — embed-model-migration: cosine operator <-> → <=> (HNSW vector_cosine_ops match, P003)`.
- BM25 path (`_bm25_search`), `_merge`, `retrieve` wrapper, RBAC WHERE clauses, timeout — all UNTOUCHED.

**Review criteria**
- [ ] Operator `<=>` matches HNSW index `vector_cosine_ops` (AC3, P003 — sequential scan eliminated)
- [ ] Score formula `1.0 - (distance / 2.0)` documented inline as "cosine distance ∈ [0,2] → similarity ∈ [0,1]"
- [ ] R001 RBAC `WHERE` clause unchanged (still BEFORE `ORDER BY/LIMIT`)
- [ ] S001 SQL injection compliance preserved — `text().bindparams()`, no f-string
- [ ] A004 weights `RAG_DENSE_WEIGHT=0.7` / `RAG_BM25_WEIGHT=0.3` not modified
- [ ] No change to `_bm25_search` or `_merge` — single-purpose hunk

**Test command**
```bash
pytest tests/rag/test_retriever_rbac.py -v
# Manual EXPLAIN ANALYZE (post-merge, before /report):
# psql -c "EXPLAIN ANALYZE SELECT ... e.embedding <=> ... LIMIT 10"
# Expected: "Index Scan using idx_embeddings_hnsw" (NOT "Seq Scan")
```

**Parallel**: safe
**Size estimate**: ~3 lines (1 operator char + 1 formula + 1 inline comment)

---

### T004: Update `test_retriever_rbac.py` distance/score expectations for cosine

**Status**: TODO

**File(s)**
- `tests/rag/test_retriever_rbac.py` — modify

**Change description**
- `_make_row` helper (~L64) docstring/comment: clarify `distance` now means cosine distance ∈ [0, 2].
- Line 98: `assert results[0].score == pytest.approx(0.9)` (was `1.0 - 0.1`) → `pytest.approx(0.95)` (`1.0 - 0.1/2.0`).
- Line 124–128 `test_result_score_is_one_minus_distance` → rename to `test_result_score_is_cosine_similarity_from_distance`; update fixture distance + expected score per new formula.
- Line 161: `pytest.approx(0.75)` → recompute from underlying `_make_row` distance (likely `1.0 - 0.5/2.0 = 0.75` — verify; rename test inline comment to "cosine half-scaled".
- Add header `# Task: S003-T004 — embed-model-migration: cosine score formula update`.
- Add one new assertion in any `_dense_search` SQL-shape test: confirm SQL string contains `<=>` (not `<->`). If no such test exists, add `test_dense_search_uses_cosine_operator`: introspect the `text()` SQL via mock-spy and assert `"<=>"` substring present (AC3/AC6 regression guard).

**Review criteria**
- [ ] All score assertions recomputed under `score = 1.0 - distance/2.0`
- [ ] New regression test asserts `<=>` present in dense SQL (prevents future revert to `<->`)
- [ ] Merge tests (lines ~204+) unchanged — `_merge` is operator-agnostic, only score values feed in
- [ ] BM25 RBAC tests unchanged
- [ ] Tests fail if T003 not landed (TDD red → green)

**Test command**
```bash
pytest tests/rag/test_retriever_rbac.py -v
```

**Parallel**: with:T003 (TDD pair — same hunk)
**Size estimate**: ~15 lines (3 expected-value updates + 1 helper docstring + ~10 lines new operator-regression test)

---

## Deferred Coverage (AC4, AC5)

| AC | Why deferred | Where covered |
|----|--------------|---------------|
| AC4: cross-lingual `"検索する方法"` → EN doc top-10 | Requires live Ollama + ingested 4-lang corpus. Not unit-testable. | S005 eval harness (`backend/rag/eval/multilingual_recall.py`) — fixtures include JA→EN cross-lingual category per spec §S005 AC2. |
| AC5: query-embed p95 < 400ms on `t3.medium` | Latency SLA — needs real model + AWS box. | S005 reports it (per-call timing log) + S004 ops smoke verifies dim+latency on fresh box. P001 budget `< 1800ms` total enforced by existing `asyncio.wait_for` in `retrieve()` (untouched). |

> Both ACs roll up to S005 verdict. Not blocking S003 sign-off — S003 owns the **wiring**, S005 owns the **measurement**.

---

## Coordination Notes

- **S001 T005 gate**: After T001 lands, zero `embed_one` callers remain in `backend/`. S001 T005 (remove `embed_one`/`_embed_one`/`batch_embed` from `OllamaEmbedder`) becomes unblocked. Notify rag-agent on S001 task board when T001 merges.
- **No S002 conflict**: S002 touches `backend/api/routes/documents.py`; S003 touches `backend/rag/query_processor.py` + `backend/rag/retriever.py`. Disjoint files — parallel-safe.
- **Score-magnitude side effect for hybrid weights**: cosine fix changes dense score range from `[1 - L2_dist]` (could be negative, depends on vector norms) to `[0, 1]` cleanly bounded. `RAG_DENSE_WEIGHT=0.7` × `RAG_BM25_WEIGHT=0.3` blend remains valid; if S005 recall numbers look skewed, weight retune is **out of scope** per spec §Out of Scope.

## Outputs Mapping (Plan §S003)

- [ ] `query_processor.embed_query` calls `_embedder.embed_query` (T001 → AC1)
- [ ] `_dense_search` uses `<=>` cosine operator + corrected score formula (T003 → AC3, AC6)
- [ ] Cross-lingual smoke test passes (deferred → S005 AC2/AC3)
- [ ] No regression in BM25 path or hybrid scoring tests (T004 review criterion + existing merge tests untouched)
- [ ] Latency assertion or telemetry hook (deferred → S005 + S004)
- [ ] EXPLAIN ANALYZE confirms HNSW index used (T003 manual verification step pre-/report)
