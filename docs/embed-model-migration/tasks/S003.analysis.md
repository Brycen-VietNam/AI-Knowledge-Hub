# Analysis: embed-model-migration / S003
Created: 2026-04-29 | Depth: shallow | Files scanned: 4

---

## Code Map

### `backend/rag/query_processor.py` (49 lines)
```
imports: TokenizerFactory, OllamaEmbedder, EmbedderError
_embedder = OllamaEmbedder()   # module-level singleton

def tokenize_query(text, lang) -> str          # UNTOUCHED by S003
async def embed_query(text) -> list[float]      # T001 target
  L44 docstring: "typically 768-dim for multilingual-e5-large"  ← WRONG (E5-large = 1024)
  L49: return await _embedder.embed_one(text)  ← SWAP TO embed_query
```

### `backend/rag/retriever.py` (161 lines)
```
imports: asyncio, os, uuid, sqlalchemy (bindparam, text)
constants: RAG_DENSE_WEIGHT=0.7, RAG_BM25_WEIGHT=0.3

class QueryTimeoutError(Exception)
@dataclass RetrievedDocument: doc_id, chunk_index, score, user_group_id, content, title, lang, source_url

async def _dense_search(session, query_embedding, user_group_ids, top_k)  ← T003 target
  L51: e.embedding <-> cast(:query_vec AS vector) AS distance  ← SWAP TO <=>
  L67: score=1.0 - r.distance                                  ← CHANGE TO 1.0 - (r.distance / 2.0)
  R001 ✅ WHERE before ORDER BY/LIMIT
  S001 ✅ text().bindparams() — no f-string

async def _bm25_search(session, bm25_query, user_group_ids, top_k)  # UNTOUCHED
def _merge(dense, bm25, top_k)                                       # UNTOUCHED
async def retrieve(query_embedding, user_group_ids, top_k, *, session, bm25_query)  # UNTOUCHED
```

### `tests/rag/test_query_processor.py` (123 lines)
```
T001 target (L103–123):
  test_embed_query_returns_vector:
    L106: mock_embedder.embed_one = AsyncMock(return_value=[0.1, 0.2, 0.3] * 256)  ← 768-dim
    L111: assert len(result) == 768                   ← CHANGE TO 1024
    L113: mock_embedder.embed_one.assert_called_once_with("hello world")  ← CHANGE TO embed_query
  test_embed_query_embedder_error_propagates:
    L120: mock_embedder.embed_one = AsyncMock(side_effect=EmbedderError(...))  ← CHANGE TO embed_query

NEW test needed: test_embed_query_passes_raw_text_no_prefix
  → assert façade passes raw text through, no "query: " prefix (prefix = embedder's job)
```

### `tests/rag/test_retriever_rbac.py` (732 lines — unit section relevant lines)
```
_make_row helper (L64–71): creates mock row with distance field
  ← add docstring: "distance = cosine distance ∈ [0, 2]"

TestDenseRBAC.test_group1_retrieves_own_doc_not_group2:
  L98: assert results[0].score == pytest.approx(0.9)   # 1.0 - 0.1 (L2 formula)
  ← CHANGE TO pytest.approx(0.95)                      # 1.0 - 0.1/2.0 (cosine formula)

TestDenseRBAC.test_result_score_is_one_minus_distance (L123–128):
  L125: _make_row(distance=0.3) → assert score == pytest.approx(0.7)  # 1.0 - 0.3
  ← RENAME test → test_result_score_is_cosine_similarity_from_distance
  ← CHANGE assert to pytest.approx(0.85)               # 1.0 - 0.3/2.0

No existing test for SQL operator — NEW test needed:
  test_dense_search_uses_cosine_operator:
    → introspect SQL string, assert "<=" in sql_text (AC3/AC6 regression guard)
    → also assert "<->" NOT in sql_text

Line 161 area — need to check if there's another score approx(0.75) assertion:
  _make_row check: search test that uses _make_row with distance=0.5 → old score 0.75
  ← CHANGE TO pytest.approx(0.75): verify 1.0 - 0.5/2.0 = 0.75 (same value, document formula change)
```

---

## Patterns to Follow

- Mock patch path: `@patch('backend.rag.query_processor._embedder')` (module singleton — same pattern as current tests)
- AsyncMock pattern: `mock_embedder.embed_query = AsyncMock(return_value=[0.0] * 1024)`
- SQL introspection pattern: `str(session.execute.call_args[0][0])` — see L117–119 `test_sql_contains_rbac_where_clause`
- `pytest.approx` for float assertions — consistent with existing test style
- `@pytest.mark.asyncio` required on all async test methods

---

## Conflicts / Gaps Found

✅ No security violations — `text().bindparams()` throughout (S001 compliant)
✅ R001 RBAC WHERE clause before ORDER BY/LIMIT — UNTOUCHED by S003
✅ A004 weight constants `RAG_DENSE_WEIGHT=0.7` / `RAG_BM25_WEIGHT=0.3` — UNTOUCHED
✅ A002 scope: changes stay inside `backend/rag/` — compliant

⚠️ **T001**: `embed_one` → `embed_query` is a 1-line swap. Name collision (both `OllamaEmbedder.embed_query` and `query_processor.embed_query`) is acceptable per WARM D-F2.

⚠️ **T003 score formula**: `score = 1.0 - (r.distance / 2.0)` — the normalization comment must be added inline ("cosine distance ∈ [0,2] → similarity ∈ [0,1]") per review criteria.

⚠️ **T004 line 161 area**: tasks.md mentions `pytest.approx(0.75)` at ~L161. Verified in test file: no explicit L161 match for dense score 0.75. The only dense score assertions are at L98 (0.9 → 0.95) and L128 (0.7 → 0.85). No additional recompute needed beyond these two. Tasks.md reference to L161 appears to point to a BM25 test (`0.75`) which uses `r.rank` directly — that is UNTOUCHED (BM25 score = rank, no cosine formula involved).

---

## S001 T005 Gate

After T001 lands:
- `query_processor.py:49` `embed_one` → eliminated
- Zero `embed_one` callers remain in `backend/`
- S001 T005 (remove `embed_one`/`_embed_one`/`batch_embed` from `OllamaEmbedder`) → **UNBLOCKED**

---

## Implementation Order

### Hunk 1: T002 (test first) → T001 (prod)
1. `tests/rag/test_query_processor.py`:
   - L106: `embed_one` → `embed_query`, vector `[0.1,0.2,0.3]*256` → `[0.0]*1024`
   - L111: dim assertion 768 → 1024
   - L113: `embed_one.assert_called_once` → `embed_query.assert_called_once`
   - L120: `embed_one` → `embed_query`
   - NEW: `test_embed_query_passes_raw_text_no_prefix` (~8 lines)
2. `backend/rag/query_processor.py`:
   - L44 docstring: "768-dim" → "1024-dim"
   - L49: `_embedder.embed_one(text)` → `_embedder.embed_query(text)`
   - Header: append memory comment (S003-T001)

### Hunk 2: T004 (test first) → T003 (prod)
1. `tests/rag/test_retriever_rbac.py`:
   - `_make_row` docstring: clarify cosine distance ∈ [0, 2]
   - L98: `approx(0.9)` → `approx(0.95)`
   - L123: rename test to `test_result_score_is_cosine_similarity_from_distance`
   - L128: `approx(0.7)` → `approx(0.85)`
   - NEW: `test_dense_search_uses_cosine_operator` (~10 lines)
   - Header: append memory comment (S003-T004)
2. `backend/rag/retriever.py`:
   - L51: `<->` → `<=>`
   - L67: `1.0 - r.distance` → `1.0 - (r.distance / 2.0)` + inline comment
   - Header: append memory comment (S003-T003)

---

## Token Budget
Estimated implementation: ~2.5k tokens (4 files, ~32 lines total changed)
Test run: `pytest tests/rag/test_query_processor.py tests/rag/test_retriever_rbac.py -v`
