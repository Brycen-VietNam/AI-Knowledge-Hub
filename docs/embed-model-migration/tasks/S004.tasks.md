# Tasks: embed-model-migration / S004 — Truncate script + AWS Ollama setup (POC)
Created: 2026-04-29 | Agent: ops | Status: DONE ✅

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S004: Truncate-and-reset script + AWS Ollama setup (POC — community tag) |
| Total tasks | 4 |
| Parallel groups | G1: [T001, T002, T003] — all independent; G2: [T004] — after T001+T002+T003 |
| Critical path | T001 → T004 |
| Agent | ops |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Truncate-and-reset script | DONE | safe | T004 |
| T002 | AWS Ollama setup doc (ollama_setup.md) | DONE | safe | T004 |
| T003 | License doc + LICENSE.e5 file | DONE | safe | T004 |
| T004 | Smoke test + .env.example coordination check | DONE | after:T001,T002,T003 | — |

---

## LAYER 2 — Task Detail

### T001: Truncate-and-reset script

**Status**: DONE

**File(s)**
- `scripts/truncate_and_reset.py` — create

**Change description**
Create an idempotent Python script that truncates `embeddings` and `audit_logs` tables
(re-ingest targets) using SQLAlchemy `text()` with named params. Gate on `--confirm` CLI
flag to prevent accidental runs. Log row counts before and after truncation. Uses `DATABASE_URL`
env var — no hardcoded credentials.

**Review criteria**
- [ ] `--confirm` flag required; exits with message if missing (S001 AC2)
- [ ] All SQL via `sqlalchemy.text()` with named bindparams — no f-string interpolation (S001 SQL injection)
- [ ] Logs row count before truncate (INFO) and after (INFO: "0 rows remain")
- [ ] Idempotent: running twice produces no error, no side effects
- [ ] `DATABASE_URL` read from `os.getenv("DATABASE_URL")` — never hardcoded (SECURITY S005)
- [ ] No imports from `backend/rag/` or `backend/api/` — standalone ops script (ARCH A001)
- [ ] Script exits with code 0 on success, non-zero on DB error

**Test command**
```bash
# Unit test (mock DB connection — no live DB needed)
pytest tests/ops/test_truncate_and_reset.py -v
```

**Parallel**: safe
**Size estimate**: ~45 lines (script) + ~30 lines (test)

---
<!-- End T001 -->

### T002: AWS Ollama setup doc

**Status**: DONE

**File(s)**
- `docs/embed-model-migration/ops/ollama_setup.md` — create

**Change description**
Write ops runbook for provisioning `zylonai/multilingual-e5-large` on AWS `t3.medium`.
Primary path: `ollama pull zylonai/multilingual-e5-large` with pinned digest. Include Docker
run command for Ollama server. Appendix B: self-convert fallback (D08 path — llama.cpp convert
from `intfloat/multilingual-e5-large` safetensors) for when zylonai tag is unavailable.

**Review criteria**
- [ ] Digest pinned: `sha256:c1522b1cf095b82080a9b804d86b4aa609e71a48bbdbcde7ea7864bb9b0cd76b`
- [ ] `EMBEDDING_MODEL` env value documented as `zylonai/multilingual-e5-large` (full tag)
- [ ] Docker run command for Ollama included (expose port 11434)
- [ ] Smoke verification step: `curl -X POST http://localhost:11434/api/embeddings -d '{"model":"zylonai/multilingual-e5-large","prompt":"test"}'` → assert response contains `"embedding"` array len=1024
- [ ] Appendix B covers: `git clone llama.cpp`, `convert-hf-to-gguf.py`, `ollama create` from Modelfile
- [ ] AWS instance spec noted: `t3.medium` (2 vCPU, 4GB RAM); F16 model ~1.1GB — fits comfortably
- [ ] `OLLAMA_MAX_EMBED_CHARS=1400` env var documented

**Test command**
```bash
# Doc review only — no automated test for markdown content
# Manual: open docs/embed-model-migration/ops/ollama_setup.md and follow smoke step
echo "MANUAL: verify smoke curl returns 1024-float array"
```

**Parallel**: safe
**Size estimate**: ~80 lines (markdown doc)

---
<!-- End T002 -->

### T003: License doc + LICENSE.e5 file

**Status**: DONE

**File(s)**
- `docs/embed-model-migration/ops/license.md` — create
- `docs/embed-model-migration/ops/LICENSE.e5` — create (verbatim MIT copy from upstream)

**Change description**
Create two files: `license.md` documents sourcing provenance (upstream `intfloat/multilingual-e5-large`
MIT → redistributed via `zylonai` Ollama tag, digest pinned, internal-only use, POC scope statement,
D11 product-phase carry-over note). `LICENSE.e5` is a verbatim copy of the MIT license text from
`intfloat/multilingual-e5-large` HuggingFace repo for record-keeping.

**Review criteria**
- [ ] `license.md` states: upstream = `intfloat/multilingual-e5-large` (MIT), distribution = `zylonai` Ollama tag (MIT-declared)
- [ ] Digest pinned in `license.md`: `sha256:c1522b1cf095b82080a9b804d86b4aa609e71a48bbdbcde7ea7864bb9b0cd76b`
- [ ] Internal-only consumption note: "MIT redistribution obligations not triggered (internal use only, no external distribution)"
- [ ] POC scope statement: links to `embed-model-migration.mem.md` POC → PRODUCT MIGRATION CHECKLIST section (D11)
- [ ] Backup GGUF blob location field filled in (or marked TODO for ops to define)
- [ ] `LICENSE.e5` contains verbatim MIT license text with `intfloat` copyright holder
- [ ] No hardcoded credentials or internal hostnames in either file (SECURITY S005)

**Test command**
```bash
# Verify both files exist and LICENSE.e5 contains MIT text
python -c "
import os
assert os.path.exists('docs/embed-model-migration/ops/license.md')
assert os.path.exists('docs/embed-model-migration/ops/LICENSE.e5')
content = open('docs/embed-model-migration/ops/LICENSE.e5').read()
assert 'MIT License' in content or 'Permission is hereby granted' in content
print('PASS: license files present and valid')
"
```

**Parallel**: safe
**Size estimate**: ~40 lines (license.md) + ~20 lines (LICENSE.e5)

---
<!-- End T003 -->

### T004: Smoke test + .env.example coordination check

**Status**: DONE

**File(s)**
- `tests/ops/test_truncate_and_reset.py` — verify exists (created in T001)
- `docs/env.example` — verify only (S001 already set `EMBEDDING_MODEL=zylonai/multilingual-e5-large`; S004 must NOT re-edit if value matches)

**Change description**
Verify `.env.example` already contains `EMBEDDING_MODEL=zylonai/multilingual-e5-large` (landed
by S001 T001) — no edit needed. If missing or different, update only the default value line.
Run T001's unit test to confirm script exits correctly. Coordinate check: confirm S004 ops docs
reference the same model tag as `.env.example`.

**Review criteria**
- [ ] `.env.example` contains `EMBEDDING_MODEL=zylonai/multilingual-e5-large` (plan L3 coordination — S001 owns the flip; S004 only documents)
- [ ] If `.env.example` already correct: no file modification needed (idempotent)
- [ ] If `.env.example` missing/wrong: update single line only — do not change other env vars
- [ ] Model tag in `ollama_setup.md` matches `.env.example` value exactly (no drift)
- [ ] T001 unit test passes: `pytest tests/ops/test_truncate_and_reset.py -v` → all PASS
- [ ] `OLLAMA_MAX_EMBED_CHARS=1400` present in `.env.example` (AC7 from spec §S004)

**Test command**
```bash
pytest tests/ops/test_truncate_and_reset.py -v
```

**Parallel**: after:T001,T002,T003
**Size estimate**: ~5 lines changed (env.example if needed) + coordination verification

---
<!-- End T004 -->

## AC → Task Mapping
| AC | Description | Task |
|----|-------------|------|
| AC1 | Truncate script idempotent with `--confirm` gate | T001 |
| AC2 | Script logs row counts before/after | T001 |
| AC3 | SQL via SQLAlchemy text() — no f-string injection | T001 |
| AC4 | AWS Ollama setup doc — primary path (zylonai pull) | T002 |
| AC5 | Smoke `curl` returns 1024-float array verified | T002 |
| AC6 | Appendix B — self-convert fallback (D08 path) | T002 |
| AC7 | License doc with digest pin + POC scope + D11 carry-over | T003 |
| AC8 | LICENSE.e5 verbatim MIT copy | T003 |
| —  | .env.example coordination + full test run | T004 |

## Layer 3 Coordination (from plan)
- `.env.example`: S001 T001 already set `EMBEDDING_MODEL=zylonai/multilingual-e5-large`. T004 verifies only — no re-edit unless value missing.
- `OLLAMA_MAX_EMBED_CHARS=1400`: check `.env.example` in T004; add if absent.
- S004 has no code dependency on S001–S003 (docs + script only). Can land at any time before S005.
- S005 dependency: DB must be truncated (T001 script run) before S002 re-ingests fixtures for eval.
