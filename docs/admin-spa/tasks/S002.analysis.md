# Analysis: S002 — Document Management
Generated: 2026-04-17 | Depth: shallow | Files scanned: 10

---

## Code Map

### frontend/admin-spa/src/api/client.ts
```
export const apiClient = axios.create({ baseURL: import.meta.env.VITE_API_BASE_URL })
  interceptors.request  — injects Bearer token from useAuthStore.getState().token
  interceptors.response — 401 (non-auth URL) → setSessionExpired() + logout() + navigate('/login')
export function setNavigate(fn)  — wired in App.tsx useEffect
```

### frontend/admin-spa/src/store/authStore.ts
```
useAuthStore (Zustand):
  state: token, username, isAdmin, sessionExpiredMessage, _refreshTimer
  login(token, username, isAdmin)
  logout()
  setSessionExpired() → sessionExpiredMessage = 'session_expired'
```

### frontend/admin-spa/src/components/auth/ProtectedRoute.tsx
```
ProtectedRoute({ children })
  reads: token, isAdmin from useAuthStore
  if !token || !isAdmin → <Navigate to="/login" replace />
  else → renders children
```

### frontend/admin-spa/src/hooks/useAdminGuard.ts
```
useAdminGuard()  — imperative guard; useEffect → navigate('/login') if !token||!isAdmin
  used by: DashboardPage (declarative ProtectedRoute + imperative guard pattern)
```

### frontend/admin-spa/src/App.tsx
```
Routes:
  /login    → <LoginPage />
  /dashboard → <ProtectedRoute><DashboardPage /></ProtectedRoute>
  /         → <Navigate to="/login" replace />
  *         → <Navigate to="/login" replace />
⚠️  /documents route MISSING — T005 phải insert trước catch-all "*"
```

### frontend/admin-spa/src/pages/DashboardPage.tsx
```
DashboardPage()
  useAdminGuard()  ← pattern: page dùng hook + ProtectedRoute bọc ở App.tsx
  return <main className="dashboard-page"><h1>{t('nav.dashboard')}</h1></main>
```

### frontend/admin-spa/src/i18n/locales/en.json (và ja/vi/ko)
```
Keys hiện tại: login.*, lang.*, nav.*
nav.documents đã có = "Documents" — dùng cho sidebar link (không cần thêm)
⚠️  "documents" key block CHƯA CÓ trong bất kỳ locale nào — T005 phải tạo mới
```

### frontend/admin-spa/package.json
```
dependencies: axios, react, react-router-dom, react-i18next, zustand
devDependencies: axios-mock-adapter ✅, vitest, @testing-library/react, jsdom
→ KHÔNG có recharts (S004), KHÔNG có form library — dùng native form (đúng cho S002)
```

### tests/api/client.test.ts (pattern reference)
```
import MockAdapter from 'axios-mock-adapter'
const mock = new MockAdapter(apiClient)
beforeEach: mock.reset() + useAuthStore.setState({...full state...})
describe('...', () => it(..., async () => { mock.onGet/Post/Delete.reply(...) }))
```

---

## Patterns to Follow

### 1. API function pattern (từ client.ts)
```typescript
// documentsApi.ts — import apiClient, không tạo axios instance mới
import { apiClient } from './client'
// Async function → trả về response.data
export async function listDocuments(params: ListDocumentsParams): Promise<DocumentListResponse> {
  const response = await apiClient.get('/v1/admin/documents', { params: { ... } })
  return response.data
}
```

### 2. FormData upload pattern (từ plan + D06)
```typescript
export async function uploadDocument(
  file: File, title?: string, lang?: string, groupId?: number, sourceUrl?: string
): Promise<UploadDocumentResponse> {
  const form = new FormData()
  form.append('file', file)
  if (title) form.append('title', title)
  if (lang) form.append('lang', lang)
  if (groupId != null) form.append('user_group_id', String(groupId))
  if (sourceUrl) form.append('source_url', sourceUrl)
  const response = await apiClient.post('/v1/documents/upload', form)
  return response.data   // { doc_id: string, status: string, source_url: string|null }
}
```

### 3. Test pattern (từ client.test.ts)
```typescript
import MockAdapter from 'axios-mock-adapter'
import { apiClient } from '../../src/api/client'
const mock = new MockAdapter(apiClient)
beforeEach(() => { mock.reset(); /* reset store */ })
// GET với params:
mock.onGet('/v1/admin/documents').reply(200, { items: [], total: 0, limit: 20, offset: 0 })
// POST FormData:
mock.onPost('/v1/documents/upload').reply(202, { doc_id: 'abc', status: 'processing', source_url: null })
// DELETE 204:
mock.onDelete('/v1/admin/documents/abc').reply(204)
```

### 4. Component pattern (từ DashboardPage.tsx)
```typescript
// Page: import useTranslation + useAdminGuard
import { useTranslation } from 'react-i18next'
import { useAdminGuard } from '../hooks/useAdminGuard'
export function DocumentsPage() {
  const { t } = useTranslation()
  useAdminGuard()
  // ...
}
```

### 5. Route insert pattern (từ App.tsx)
```typescript
// Thêm trước catch-all — thứ tự QUAN TRỌNG
<Route path="/documents" element={<ProtectedRoute><DocumentsPage /></ProtectedRoute>} />
<Route path="/" element={<Navigate to="/login" replace />} />
<Route path="*" element={<Navigate to="/login" replace />} />
```

---

## Conflicts / Gaps Found

### Backend (phải patch trước /implement frontend)

❌ **G1 BLOCKER** `backend/api/routes/upload.py:75`
```python
# HIỆN TẠI — sai:
if user.auth_type != "api_key":   # chặn admin JWT
# CẦN SỬA:
if user.auth_type != "api_key" and not user.is_admin:
```

❌ **G2 BLOCKER** `backend/api/routes/upload.py:184`
```python
# HIỆN TẠI — sai:
content={"document_id": str(doc.id), "status": "processing"}
# CẦN SỬA (D11):
content={"doc_id": str(doc.id), "status": "processing", "source_url": doc.source_url}
```

❌ **G3 BLOCKER** `backend/api/routes/admin.py:75–117`
```python
# HIỆN TẠI — thiếu filter params:
async def admin_list_documents(user, db, limit=20, offset=0)
# CẦN SỬA:
async def admin_list_documents(user, db, limit=20, offset=0,
    status: str | None = None, lang: str | None = None, user_group_id: int | None = None)
# SQL: WHERE 1=1 [AND d.status=:status] [AND d.lang=:lang] [AND d.user_group_id=:ugid]
# PHẢI dùng parameterized — không f-string (S001)
```

⚠️ **G4 SHOULD** `backend/api/routes/upload.py` — source_url chưa được nhận từ Form
```python
# CẦN THÊM param:
source_url: str | None = Form(default=None)
# VÀ lưu vào doc:
doc = Document(..., source_url=source_url)
```

### Frontend

⚠️ **App.tsx** — `/documents` route chưa có (T005 phải thêm — xác nhận)

⚠️ **i18n** — `"documents"` key block CHƯA CÓ trong 4 locale files (T005 tạo mới)

⚠️ **nav có sẵn** — `nav.documents` đã có trong 4 locales → sidebar link có thể dùng ngay (không cần thêm)

---

## Param Mapping: listDocuments → API

```typescript
// Frontend (camelCase) → API query params (snake_case)
{ userGroupId: 5 } → ?user_group_id=5
{ status: 'ready' } → ?status=ready
{ lang: 'ja' } → ?lang=ja
{ limit: 20, offset: 40 } → ?limit=20&offset=40

// axios tự serialize object params:
apiClient.get('/v1/admin/documents', { params: {
  limit: params.limit,
  offset: params.offset,
  ...(params.status && { status: params.status }),
  ...(params.lang && { lang: params.lang }),
  ...(params.userGroupId != null && { user_group_id: params.userGroupId }),
}})
```

---

## i18n Key Block (31 keys — tất cả 4 locales)

```json
"documents": {
  "page_title": "Documents",
  "upload_btn": "Upload Document",
  "filter_status": "Status",
  "filter_lang": "Language",
  "filter_group": "Group",
  "filter_all": "All",
  "col_title": "Title",
  "col_lang": "Language",
  "col_group": "Group",
  "col_status": "Status",
  "col_created_at": "Created At",
  "col_chunks": "Chunks",
  "col_actions": "Actions",
  "status_pending": "Pending",
  "status_processing": "Processing",
  "status_ready": "Ready",
  "status_error": "Error",
  "delete_btn": "Delete",
  "delete_confirm_title": "Delete Document",
  "delete_confirm_msg": "Delete document '{{title}}'? This cannot be undone.",
  "upload_modal_title": "Upload Document",
  "upload_file_label": "File",
  "upload_file_hint": "PDF, DOCX, HTML, TXT, MD",
  "upload_title_label": "Title (optional)",
  "upload_lang_label": "Language",
  "upload_lang_auto": "Auto-detect",
  "upload_group_label": "Group (optional)",
  "upload_source_url_label": "Source URL (optional)",
  "upload_submit": "Upload",
  "upload_cancel": "Cancel",
  "upload_success": "Document uploaded successfully",
  "no_group": "No Group",
  "empty": "No documents found.",
  "prev": "Previous",
  "next": "Next"
}
```
> **31 keys.** Thêm vào tất cả 4 locales (en/ja/vi/ko) với bản dịch tương ứng.

---

## CSS Classes cần thêm vào index.css

Các class mới cho S002 (chưa có trong index.css):
```css
/* Table */
.documents-table, .table-empty
/* Status badges */
.badge, .badge-ready, .badge-processing, .badge-pending, .badge-error
/* Dialog */
.confirm-dialog-overlay, .confirm-dialog
/* Buttons */
.btn-secondary, .btn-danger, .btn-icon
/* Toast */
.toast, .toast-success
/* Upload modal */
/* (reuse .confirm-dialog-overlay pattern) */
/* Filter bar */
.filter-bar
/* Pagination */
.pagination
```
> Tất cả dùng CSS class toàn cục (không CSS modules, không inline) — pattern từ D07.

---

## Recommended Implementation Order

```
G1 (pre-impl): patch upload.py:75   ← BLOCKER
G2 (pre-impl): patch upload.py:184  ← BLOCKER
G3 (pre-impl): patch admin.py       ← BLOCKER
G4 (pre-impl): wire source_url upload.py  ← SHOULD

Frontend (parallel group G1):
  T001 documentsApi.ts  — API functions + types
  T002 DeleteConfirmDialog.tsx
  T003 DocumentTable.tsx
  T005 i18n + App.tsx route

Frontend (parallel group G2, after T001):
  T004 UploadModal.tsx

Frontend (parallel group G3, after all):
  T006 DocumentsPage.tsx
```

---

## Token Budget
Estimated implementation: ~6k tokens (6 tasks + 4 backend patches)
Backend patches (G1–G4): ~1k tokens
Frontend T001–T006: ~5k tokens
```
Analysis saved: docs/admin-spa/tasks/S002.analysis.md
Next: patch G1–G4 backend → /implement S002 T001–T006
```
