# Tasks: admin-spa / S004 — Metrics Dashboard
Created: 2026-04-20 | Agent: api-agent (T001) + frontend-agent (T002–T007) | Status: REVIEWED ✅

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S004: Metrics Dashboard |
| Total tasks | 7 |
| Parallel groups | G1: [T001, T002, T003] → G2: [T004, T005, T006] → G3: [T007] |
| Critical path | T002 → T004 → T007 |
| Agent | api-agent (T001), frontend-agent (T002–T007) |
| Est. session | 1 |

### Pre-implementation Gap — Backend `/v1/metrics` Mismatch

**Current backend returns** (admin.py, S000 T007):
```json
{"document_count": N, "query_count_24h": N, "active_users_count": N, "health": "ok"}
```
**Spec AC5 requires:**
```json
{
  "documents": {"total": N, "ready": N, "processing": N, "error": N},
  "users": {"total": N, "active": N},
  "groups": {"total": N},
  "queries": {"last_7_days": [{"date": "YYYY-MM-DD", "count": N}, ...]},
  "health": {"database": "ok", "api": "ok"}
}
```
→ **T001 patches backend before any frontend tasks.**

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Patch `/v1/metrics` response to spec contract | DONE ✅ | safe | — |
| T002 | Add recharts dep + `metricsApi.ts` + types | DONE ✅ | safe | T004, T005, T006 |
| T003 | `useAutoRefresh.ts` hook | DONE ✅ | safe | T007 |
| T004 | `MetricCards.tsx` | DONE ✅ | after:T002 | T007 |
| T005 | `QueryVolumeChart.tsx` | DONE ✅ | after:T002 | T007 |
| T006 | `HealthIndicators.tsx` | DONE ✅ | after:T002 | T007 |
| T007 | `DashboardPage.tsx` + i18n + CSS | DONE ✅ | after:T003,T004,T005,T006 | — |

---

## LAYER 2 — Task Detail

---

### T001: Patch `/v1/metrics` response to spec contract

**Status**: DONE ✅

**File(s)**
- `backend/api/routes/admin.py` — modify: replace `get_metrics()` SQL + response shape
- `tests/api/test_admin.py` — modify: update existing metrics test + add 3 new assertions

**Change description**
The current `get_metrics()` returns a flat dict that doesn't match the spec AC5 contract.
Replace with 5 queries:
1. Documents breakdown by status (COUNT + GROUP BY status)
2. Users total + active
3. Groups total
4. 7-day daily query volume (GROUP BY date from audit_logs)
5. Health sub-object `{"database": "ok", "api": "ok"}` — always ok when endpoint responds

SQL pattern (R001-compliant, no f-strings — S001 precedent):
```python
doc_rows = (await db.execute(text("""
    SELECT status, COUNT(*) as cnt
    FROM documents GROUP BY status
"""))).mappings().all()
doc_counts = {r["status"]: r["cnt"] for r in doc_rows}

user_row = (await db.execute(text("""
    SELECT COUNT(*) as total,
           SUM(CASE WHEN is_active THEN 1 ELSE 0 END) as active
    FROM users
"""))).mappings().one()

group_total = (await db.execute(text("SELECT COUNT(*) FROM user_groups"))).scalar()

query_rows = (await db.execute(text("""
    SELECT DATE(timestamp) as day, COUNT(*) as cnt
    FROM audit_logs
    WHERE timestamp >= NOW() - INTERVAL '7 days'
    GROUP BY day ORDER BY day ASC
"""))).mappings().all()

return JSONResponse(content={
    "documents": {
        "total": sum(doc_counts.values()),
        "ready": doc_counts.get("ready", 0),
        "processing": doc_counts.get("processing", 0),
        "error": doc_counts.get("error", 0),
    },
    "users": {"total": user_row["total"], "active": user_row["active"] or 0},
    "groups": {"total": group_total},
    "queries": {"last_7_days": [
        {"date": str(r["day"]), "count": r["cnt"]} for r in query_rows
    ]},
    "health": {"database": "ok", "api": "ok"},
})
```

**Review criteria**
- [ ] Response shape exactly matches spec AC5 contract (nested objects, not flat)
- [ ] All SQL uses `text()` with no string interpolation (S001 — no f-strings)
- [ ] `documents.total` = sum of all statuses (including "pending" etc)
- [ ] `queries.last_7_days` = array, not scalar
- [ ] `health` sub-object present with `"database"` and `"api"` keys
- [ ] Rule satisfied: R001 (parameterized SQL)
- [ ] Existing test updated + 3 new assertions: doc breakdown, 7-day array, health shape

**Test command**
```bash
pytest tests/api/test_admin.py -k "metrics" -v
```

**Parallel**: safe
**Size estimate**: ~45 lines changed (admin.py ~35 + test_admin.py ~10)

---
<!-- End T001 -->

---

### T002: Add recharts dep + `metricsApi.ts` + types

**Status**: DONE ✅

**File(s)**
- `frontend/admin-spa/package.json` — modify: add `"recharts": "^2.12.0"` to dependencies
- `frontend/admin-spa/src/api/metricsApi.ts` — create: `MetricsData` interface + `getMetrics()` call
- `frontend/admin-spa/tests/api/metricsApi.test.ts` — create: 3 tests (success, 403, network error)

**Change description**
Add recharts as a dependency (tree-shake — import only BarChart, XAxis, YAxis, Tooltip, ResponsiveContainer).

`MetricsData` interface matches spec AC5 contract exactly:
```typescript
export interface DailyQueryCount { date: string; count: number }
export interface MetricsData {
  documents: { total: number; ready: number; processing: number; error: number }
  users: { total: number; active: number }
  groups: { total: number }
  queries: { last_7_days: DailyQueryCount[] }
  health: { database: 'ok' | 'error'; api: 'ok' | 'error' }
}
export async function getMetrics(): Promise<MetricsData> {
  const res = await client.get<MetricsData>('/v1/metrics')
  return res.data
}
```

Tests: mock 200 → returns MetricsData, mock 403 → throws, mock network error → throws.

**Review criteria**
- [ ] `MetricsData` interface matches spec AC5 exactly (nested, not flat)
- [ ] `getMetrics()` uses shared `client` from `src/api/client.ts` (includes auth interceptor)
- [ ] recharts added to `dependencies` (not devDependencies)
- [ ] 3 tests: happy path, 403, network error

**Test command**
```bash
cd frontend/admin-spa ; npx vitest run tests/api/metricsApi.test.ts
```

**Parallel**: safe
**Size estimate**: ~35 lines changed

---
<!-- End T002 -->

---

### T003: `useAutoRefresh.ts` hook

**Status**: DONE ✅

**File(s)**
- `frontend/admin-spa/src/hooks/useAutoRefresh.ts` — create
- `frontend/admin-spa/tests/hooks/useAutoRefresh.test.ts` — create: 3 tests

**Change description**
```typescript
// useAutoRefresh.ts
import { useEffect } from 'react'
export function useAutoRefresh(fn: () => void, intervalMs = 60_000) {
  useEffect(() => {
    fn()
    const id = setInterval(fn, intervalMs)
    return () => clearInterval(id)
  }, [fn, intervalMs])
}
```
Tests use `vi.useFakeTimers()`:
1. Calls `fn()` immediately on mount
2. Calls `fn()` again after `intervalMs` (advance timer)
3. Clears interval on unmount (no extra calls after `clearInterval`)

**Review criteria**
- [ ] `fn()` called once on mount (initial load per AC6)
- [ ] `setInterval` with correct `intervalMs`
- [ ] `clearInterval` called on cleanup (no memory leak)
- [ ] Tests use `vi.useFakeTimers()` + `vi.advanceTimersByTime()`

**Test command**
```bash
cd frontend/admin-spa ; npx vitest run tests/hooks/useAutoRefresh.test.ts
```

**Parallel**: safe
**Size estimate**: ~15 lines changed

---
<!-- End T003 -->

---

### T004: `MetricCards.tsx`

**Status**: DONE ✅

**File(s)**
- `frontend/admin-spa/src/components/MetricCards.tsx` — create
- `frontend/admin-spa/tests/components/MetricCards.test.tsx` — create: 4 tests

**Change description**
4 cards: Total Documents, Active Documents (status=ready), Total Users, Total Groups.
Props: `{ data: MetricsData }`.
Each card: `.metric-card` with `.metric-card__label` + `.metric-card__value`.

```typescript
export function MetricCards({ data }: { data: MetricsData }) {
  const { t } = useTranslation()
  return (
    <div className="metric-cards">
      <div className="metric-card"><span className="metric-card__label">{t('dashboard.total_docs')}</span>
        <span className="metric-card__value">{data.documents.total}</span></div>
      <div className="metric-card"><span className="metric-card__label">{t('dashboard.active_docs')}</span>
        <span className="metric-card__value">{data.documents.ready}</span></div>
      <div className="metric-card"><span className="metric-card__label">{t('dashboard.total_users')}</span>
        <span className="metric-card__value">{data.users.total}</span></div>
      <div className="metric-card"><span className="metric-card__label">{t('dashboard.total_groups')}</span>
        <span className="metric-card__value">{data.groups.total}</span></div>
    </div>
  )
}
```

Tests: renders 4 cards with correct values from mock `MetricsData`.

**Review criteria**
- [ ] 4 cards: total_docs, active_docs (=ready), total_users, total_groups (AC2)
- [ ] Uses `t('dashboard.*')` i18n keys (added in T007)
- [ ] CSS classes follow `.metric-card` pattern (defined in T007)
- [ ] Tests verify all 4 card values rendered correctly

**Test command**
```bash
cd frontend/admin-spa ; npx vitest run tests/components/MetricCards.test.tsx
```

**Parallel**: after:T002
**Size estimate**: ~35 lines changed

---
<!-- End T004 -->

---

### T005: `QueryVolumeChart.tsx`

**Status**: DONE ✅

**File(s)**
- `frontend/admin-spa/src/components/QueryVolumeChart.tsx` — create
- `frontend/admin-spa/tests/components/QueryVolumeChart.test.tsx` — create: 3 tests

**Change description**
Recharts `BarChart` showing 7-day query volume. Tree-shake imports:
```typescript
import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts'
```
Props: `{ data: DailyQueryCount[] }`.
Map `data` to `[{ name: 'MM/DD', value: N }]` for recharts.

Tests: mock recharts (vi.mock), assert wrapper renders with data, assert empty array renders without crash.

**Review criteria**
- [ ] Imports only: `BarChart`, `Bar`, `XAxis`, `YAxis`, `Tooltip`, `ResponsiveContainer` (tree-shake per plan risk)
- [ ] Props type: `{ data: DailyQueryCount[] }` from `metricsApi.ts`
- [ ] Renders without crash when `data` is empty array (AC7 edge case)
- [ ] recharts mocked in tests (`vi.mock('recharts', ...)`)

**Test command**
```bash
cd frontend/admin-spa ; npx vitest run tests/components/QueryVolumeChart.test.tsx
```

**Parallel**: after:T002
**Size estimate**: ~30 lines changed

---
<!-- End T005 -->

---

### T006: `HealthIndicators.tsx`

**Status**: DONE ✅

**File(s)**
- `frontend/admin-spa/src/components/HealthIndicators.tsx` — create
- `frontend/admin-spa/tests/components/HealthIndicators.test.tsx` — create: 4 tests

**Change description**
2 indicators: Database + Backend API. Each shows green dot + "Online" or red dot + "Offline".
Props: `{ health: MetricsData['health'] }`.

```typescript
export function HealthIndicators({ health }: { health: MetricsData['health'] }) {
  const { t } = useTranslation()
  return (
    <div className="health-indicators">
      <HealthBadge label={t('dashboard.health_db')} status={health.database} />
      <HealthBadge label={t('dashboard.health_api')} status={health.api} />
    </div>
  )
}
function HealthBadge({ label, status }: { label: string; status: 'ok' | 'error' }) {
  const { t } = useTranslation()
  return (
    <div className={`health-badge health-badge--${status}`}>
      <span className="health-dot" />
      <span>{label}: {status === 'ok' ? t('dashboard.health_ok') : t('dashboard.health_error')}</span>
    </div>
  )
}
```

Tests:
1. Renders "Online" for both when status = "ok"/"ok"
2. Renders "Offline" for DB when status = "error"
3. Renders "Offline" for API when status = "error"
4. Both "Offline" when both error

**Review criteria**
- [ ] 2 indicators: `database` + `api` (AC4)
- [ ] Green (`ok`) / Red (`error`) visual distinction via CSS class
- [ ] Uses `t('dashboard.health_db')`, `t('dashboard.health_ok')`, `t('dashboard.health_error')` keys
- [ ] Tests cover ok/error combos

**Test command**
```bash
cd frontend/admin-spa ; npx vitest run tests/components/HealthIndicators.test.tsx
```

**Parallel**: after:T002
**Size estimate**: ~30 lines changed

---
<!-- End T006 -->

---

### T007: `DashboardPage.tsx` (replace stub) + i18n `dashboard` namespace + CSS classes

**Status**: DONE ✅

**File(s)**
- `frontend/admin-spa/src/pages/DashboardPage.tsx` — modify: replace stub with full implementation
- `frontend/admin-spa/src/i18n/locales/en.json` — modify: add `dashboard` namespace (14 keys)
- `frontend/admin-spa/src/i18n/locales/ja.json` — modify: add `dashboard` namespace (14 keys)
- `frontend/admin-spa/src/i18n/locales/vi.json` — modify: add `dashboard` namespace (14 keys)
- `frontend/admin-spa/src/i18n/locales/ko.json` — modify: add `dashboard` namespace (14 keys)
- `frontend/admin-spa/src/index.css` — modify: add S004 CSS classes
- `frontend/admin-spa/tests/pages/DashboardPage.test.tsx` — create: 6 tests

**Change description**
Replace stub `DashboardPage.tsx` with full orchestration:
```typescript
// Wires: getMetrics() + useAutoRefresh + MetricCards + QueryVolumeChart + HealthIndicators
export function DashboardPage() {
  const { t } = useTranslation()
  useAdminGuard()
  const [data, setData] = useState<MetricsData | null>(null)
  const [error, setError] = useState(false)

  const fetchMetrics = useCallback(async () => {
    try { setData(await getMetrics()); setError(false) }
    catch { setError(true) }
  }, [])

  useAutoRefresh(fetchMetrics, 60_000)

  return (
    <main className="dashboard-page">
      <h1>{t('nav.dashboard')}</h1>
      {error && <p className="metrics-unavailable">{t('dashboard.metrics_unavailable')}</p>}
      {data && (
        <>
          <MetricCards data={data} />
          <QueryVolumeChart data={data.queries.last_7_days} />
          <HealthIndicators health={data.health} />
        </>
      )}
    </main>
  )
}
```

**i18n `dashboard` namespace (14 keys × 4 locales):**
```json
"dashboard": {
  "total_docs": "Total Documents",
  "active_docs": "Active Documents",
  "total_users": "Total Users",
  "total_groups": "Total Groups",
  "chart_title": "Query Volume (Last 7 Days)",
  "chart_y_label": "Queries",
  "health_title": "System Health",
  "health_db": "Database",
  "health_api": "Backend API",
  "health_ok": "Online",
  "health_error": "Offline",
  "metrics_unavailable": "Metrics unavailable",
  "auto_refresh_note": "Auto-refreshes every 60s",
  "loading": "Loading..."
}
```

**CSS classes to add to `index.css`:**
`.dashboard-page`, `.metric-cards`, `.metric-card`, `.metric-card__label`, `.metric-card__value`, `.health-indicators`, `.health-badge`, `.health-badge--ok`, `.health-badge--error`, `.health-dot`, `.metrics-unavailable`

**Tests (6):**
1. Renders cards when metrics fetch succeeds
2. Shows "Metrics unavailable" when fetch fails (AC7)
3. Does NOT crash when data is null (initial state)
4. Calls `getMetrics` on mount
5. `useAutoRefresh` is called with 60_000 interval (mock hook)
6. HealthIndicators rendered with health data

**Review criteria**
- [ ] `DashboardPage.tsx` stub fully replaced (no placeholder `<h1>` only)
- [ ] `useAutoRefresh(fetchMetrics, 60_000)` wired (AC6)
- [ ] Error state shows `.metrics-unavailable` text (AC7)
- [ ] All 14 `dashboard.*` i18n keys present in all 4 locale files
- [ ] CSS: `.metric-card`, `.health-badge--ok/--error`, `.health-dot` defined
- [ ] 6 tests pass

**Test command**
```bash
cd frontend/admin-spa ; npx vitest run tests/pages/DashboardPage.test.tsx
```

**Parallel**: after:T003,T004,T005,T006
**Size estimate**: ~50 lines changed (DashboardPage ~30, i18n ~14 keys × 4 files, CSS ~20 lines)

---
<!-- End T007 -->
