# Tasks: admin-spa / S005 — Build & Docker Packaging
Created: 2026-04-20 | Agent: frontend-agent | Status: DONE

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S005: Build & Docker Packaging |
| Total tasks | 5 |
| Parallel groups | G1: [T001, T002, T003] (safe — independent files), G2: [T004] (after G1), G3: [T005] (after T001) |
| Critical path | T001 → T005 |
| Agent | frontend-agent |
| Est. session | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Create admin-spa Dockerfile | DONE | safe | T005 |
| T002 | Create admin-spa nginx.conf | DONE | safe | T004 |
| T003 | Create admin-spa .env.example | DONE | safe | — |
| T004 | Add admin-spa service to docker-compose.yml | DONE | after:T001,T002 | — |
| T005 | Create build smoke test script | DONE | after:T001 | — |

---

## LAYER 2 — Task Detail

### T001: Create admin-spa Dockerfile

**Status**: TODO

**File(s)**
- `frontend/admin-spa/Dockerfile` — action: create

**Change description**
Multi-stage Docker build: node:20-alpine builds the Vite project, nginx:alpine serves the static output.
Accepts `VITE_API_BASE_URL` as a build ARG (Vite bakes env vars at build time).
Pattern mirrors `frontend/Dockerfile` (frontend-spa S005, D005/D013).

**Review criteria**
- [ ] Stage 1: `node:20-alpine`, `npm ci` (not `npm install`), `npm run build` succeeds
- [ ] Stage 2: `nginx:alpine`, dist copied to `/usr/share/nginx/html`
- [ ] `VITE_API_BASE_URL` accepted as ARG → ENV (not hardcoded)
- [ ] `EXPOSE 80` present; `CMD ["nginx", "-g", "daemon off;"]`
- [ ] No secrets or credentials in image layers (S005 — SECURITY.md)

**Test command**
```bash
# Smoke: build passes (run from frontend/admin-spa/)
docker build --build-arg VITE_API_BASE_URL=http://localhost:8000 -t admin-spa-test .
```

**Parallel**: safe
**Size estimate**: ~25 lines

---

### T002: Create admin-spa nginx.conf

**Status**: TODO

**File(s)**
- `frontend/admin-spa/nginx.conf` — action: create

**Change description**
nginx SPA routing: `try_files $uri $uri/ /index.html` so React Router handles all client-side paths.
Adds cache headers for Vite's content-hashed static assets and security headers.
Pattern mirrors `frontend/nginx.conf`.

**Review criteria**
- [ ] `try_files $uri $uri/ /index.html` present — SPA fallback works
- [ ] Cache headers on `js|css|png|…` (1y, immutable)
- [ ] Security headers: `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`
- [ ] `listen 80` matches Dockerfile `EXPOSE 80`

**Test command**
```bash
# Verified via T005 smoke test (docker run -p 8081:80) — no unit test needed
echo "nginx.conf verified via build smoke test"
```

**Parallel**: safe
**Size estimate**: ~22 lines

---

### T003: Create admin-spa .env.example

**Status**: TODO

**File(s)**
- `frontend/admin-spa/.env.example` — action: create

**Change description**
Documents the single required Vite env var (`VITE_API_BASE_URL`) with a comment block explaining
the build-time baking behavior. Matches pattern from `frontend/.env.example`.
Referenced in docker-compose.yml `build.args`.

**Review criteria**
- [ ] `VITE_API_BASE_URL=http://localhost:8000` present (default for local dev)
- [ ] Comment explains VITE_* are baked at build time (Docker build-arg note)
- [ ] No secrets, passwords, or tokens (S005 — SECURITY.md)

**Test command**
```bash
# No automated test — file is documentation only
echo ".env.example created"
```

**Parallel**: safe
**Size estimate**: ~12 lines

---

### T004: Add admin-spa service to docker-compose.yml

**Status**: TODO

**File(s)**
- `docker-compose.yml` — action: modify

**Change description**
Append `admin-spa` service block after `frontend-spa`. Context: `./frontend/admin-spa`.
Port: `8081:80` (D05 — avoids conflict with 8000/8080/3000).
Build arg: `VITE_API_BASE_URL` from env (same pattern as frontend-spa).
Depends on: `app` service.

**Review criteria**
- [ ] Service name: `admin-spa`
- [ ] Port: `8081:80` (matches D05)
- [ ] `build.context: ./frontend/admin-spa`
- [ ] `build.args.VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000}` (not hardcoded — S001)
- [ ] `depends_on: - app`
- [ ] Existing services unchanged

**Test command**
```bash
# Validate docker-compose syntax
docker compose config --quiet
```

**Parallel**: after:T001,T002
**Size estimate**: ~10 lines added

---

### T005: Create build smoke test script

**Status**: TODO

**File(s)**
- `tests/admin-spa/S005.build.sh` — action: create

**Change description**
Shell script that runs `npm run build` inside `frontend/admin-spa/`, then `docker build` to verify
the full packaging pipeline. Exits non-zero on first failure (set -e).
Used in CI and for manual verification post-deploy.

**Review criteria**
- [ ] `set -e` at top (fail-fast)
- [ ] `npm run build` run from correct directory (`frontend/admin-spa/`)
- [ ] `docker build` with `--build-arg VITE_API_BASE_URL=http://localhost:8000`
- [ ] Script is executable (`chmod +x` note in comment)
- [ ] No hardcoded secrets

**Test command**
```bash
# Manual run (requires Node + Docker available)
bash tests/admin-spa/S005.build.sh
```

**Parallel**: after:T001
**Size estimate**: ~20 lines
