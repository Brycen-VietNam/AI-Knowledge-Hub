# Tasks: admin-spa / S002 — Document Management
Created: 2026-04-17 | Agent: frontend-agent | Status: TODO

> **Spec correction applied (D06):**
> AC4 updated: Upload modal dùng **file input** (PDF/DOCX/HTML/TXT/MD), KHÔNG phải textarea.
> AC5 updated: Upload gọi `POST /v1/documents/upload` (multipart/form-data), KHÔNG phải `POST /v1/documents` (JSON).
> Source: D06 resolved at /clarify, Risk Register in admin-spa.plan.md.

> **Backend gaps identified (Session #086) — phải patch trước /implement:**
> - **G1 BLOCKER** `upload.py:75` — auth gate chặn admin JWT → fix `and not user.is_admin`
> - **G2 BLOCKER** `upload.py:184` — response key `document_id` → `doc_id` (D11)
> - **G3 BLOCKER** `admin.py:75–117` — `GET /v1/admin/documents` thiếu filter params `status/lang/user_group_id`
> - **G4 SHOULD** `upload.py` — expose `source_url` Form field (migration 007 đã có, không cần migration mới)

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S002: Document Management |
| Total tasks | 6 |
| Parallel groups | G1: [T001,T002,T003,T005], G2: [T004], G3: [T006] |
| Critical path | T001 → T004 → T006 |
| Agent | frontend-agent |
| Est. sessions | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | documentsApi.ts — 3 API functions + types | TODO | safe | T004, T006 |
| T002 | DeleteConfirmDialog.tsx + test | TODO | safe | T006 |
| T003 | DocumentTable.tsx + status badge + test | TODO | safe | T006 |
| T004 | UploadModal.tsx — file input (D06) + test | TODO | after:T001 | T006 |
| T005 | i18n S002 keys (4 locales) + App.tsx /documents route | TODO | safe | T006 |
| T006 | DocumentsPage.tsx — orchestration + test | TODO | after:T001,T002,T003,T004,T005 | — |

---

## LAYER 2 — Task Detail

---

### T001: documentsApi.ts — 3 API functions + TypeScript types + test

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/src/api/documentsApi.ts` — create
- `frontend/admin-spa/tests/api/documentsApi.test.ts` — create

**Change description**
Tạo API module cho document management, reuse `apiClient` từ `frontend/admin-spa/src/api/client.ts`.
3 functions:
- `listDocuments({ limit, offset, status?, lang?, userGroupId? })` → `GET /v1/admin/documents`
- `uploadDocument(file, title?, lang?, groupId?, sourceUrl?)` → `POST /v1/documents/upload` (multipart/form-data per D06)
- `deleteDocument(docId)` → `DELETE /v1/admin/documents/{docId}`

TypeScript interfaces (trong cùng file):
```typescript
export interface DocumentItem {
  id: string
  title: string
  lang: string
  user_group_id: number | null
  user_group_name?: string
  status: 'pending' | 'processing' | 'ready' | 'error'
  created_at: string
  chunk_count: number
  source_url: string | null   // G4: migration 007 đã có column này
}
export interface DocumentListResponse {
  items: DocumentItem[]
  total: number
  limit: number
  offset: number
}
export interface ListDocumentsParams {
  limit?: number
  offset?: number
  status?: string        // G3: backend filter param (sau khi patch admin.py)
  lang?: string          // G3: backend filter param
  userGroupId?: number   // G3: backend filter param → snake_case user_group_id khi gửi
}
export interface UploadDocumentResponse {
  doc_id: string         // D11: chuẩn hóa từ document_id → doc_id (sau khi patch upload.py)
  status: string
  source_url: string | null
}
```

`uploadDocument`: tạo `FormData`, append `file`, append `title`/`lang`/`user_group_id`/`source_url` nếu có.

**Review criteria**
- [ ] `uploadDocument` dùng `FormData` + `Content-Type: multipart/form-data` — KHÔNG JSON body (D06)
- [ ] `uploadDocument` append `source_url` vào FormData nếu được truyền (G4)
- [ ] Upload response parse field `doc_id` (không phải `document_id`) — D11
- [ ] `listDocuments` map `userGroupId` → query param `user_group_id` (snake_case theo API contract)
- [ ] Import `apiClient` từ `./client` — không tạo axios instance mới
- [ ] `deleteDocument` return `void` (204 no body)
- [ ] Test: mock `apiClient` bằng `axios-mock-adapter`, test cả 3 functions + params mapping + source_url append

**Test command**
```bash
cd frontend/admin-spa && npm run test -- tests/api/documentsApi.test.ts
```

**Parallel:** safe | **Size est.:** ~55 lines api + ~45 lines test

---

### T002: DeleteConfirmDialog.tsx — shared confirm dialog + test

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/src/components/DeleteConfirmDialog.tsx` — create
- `frontend/admin-spa/tests/components/DeleteConfirmDialog.test.tsx` — create

**Change description**
Generic confirm dialog — dùng cho delete document (và có thể tái dùng cho S003 delete group/user).
Props interface:
```typescript
interface DeleteConfirmDialogProps {
  open: boolean
  title: string      // e.g. t('documents.delete_confirm_title')
  message: string    // e.g. t('documents.delete_confirm_msg', { title: doc.title })
  onConfirm: () => void
  onCancel: () => void
}
```
Render: chỉ render khi `open=true`. CSS class `confirm-dialog-overlay` + `confirm-dialog`. Buttons: Cancel (class `btn-secondary`) + Delete (class `btn-danger`).

**Review criteria**
- [ ] Không render khi `open=false` (early return / conditional mount)
- [ ] `onConfirm` gọi khi click Delete, `onCancel` gọi khi click Cancel hoặc click overlay
- [ ] Không hardcode text — nhận `title` + `message` từ props
- [ ] Test: renders khi open=true, hidden khi open=false, onConfirm/onCancel called correctly

**Test command**
```bash
cd frontend/admin-spa && npm run test -- tests/components/DeleteConfirmDialog.test.tsx
```

**Parallel:** safe | **Size est.:** ~30 lines component + ~35 lines test

---

### T003: DocumentTable.tsx — table + status badge + test

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/src/components/DocumentTable.tsx` — create
- `frontend/admin-spa/tests/components/DocumentTable.test.tsx` — create

**Change description**
Table hiển thị document list với 7 cột: Title, Language, Group, Status, Created At, Chunk Count, Actions.
Props:
```typescript
interface DocumentTableProps {
  documents: DocumentItem[]    // import từ documentsApi.ts
  onDelete: (id: string, title: string) => void
}
```
Status badge inline component (không file riêng):
- `ready` → `<span class="badge badge-ready">` (green)
- `processing` → `<span class="badge badge-processing">` (yellow)
- `pending` → `<span class="badge badge-pending">` (gray)
- `error` → `<span class="badge badge-error">` (red)

Created At: format `new Date(created_at).toLocaleDateString()`.
Actions: Delete button (class `btn-icon btn-danger`) gọi `onDelete(doc.id, doc.title)`.
Empty state: `<p className="table-empty">{t('documents.empty')}</p>` khi `documents.length === 0`.

**Review criteria**
- [ ] Import `DocumentItem` từ `../api/documentsApi` — không re-define type
- [ ] Status badge dùng CSS class (không inline style), 4 trạng thái đủ
- [ ] `onDelete` nhận cả `id` và `title` (title dùng cho confirm dialog message ở parent)
- [ ] Empty state hiển thị đúng khi array rỗng
- [ ] Test: renders 3 documents, status badge classes, empty state, onDelete callback

**Test command**
```bash
cd frontend/admin-spa && npm run test -- tests/components/DocumentTable.test.tsx
```

**Parallel:** safe | **Size est.:** ~55 lines component + ~45 lines test

---

### T004: UploadModal.tsx — file input (D06) + test

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/src/components/UploadModal.tsx` — create
- `frontend/admin-spa/tests/components/UploadModal.test.tsx` — create

**Change description**
Upload modal với file input per D06 (không textarea). Reuse pattern `confirm-dialog-overlay` từ T002.
Props:
```typescript
interface UploadModalProps {
  open: boolean
  groups: { id: number; name: string }[]   // cho Group dropdown
  onClose: () => void
  onSuccess: () => void   // gọi sau khi upload thành công → parent refresh list
}
```

Form fields:
1. `<input type="file" accept=".pdf,.docx,.html,.txt,.md">` — required, per D06
2. Title: `<input type="text">` — optional, placeholder = `t('documents.upload_title_label')`, default = filename stem (lấy từ `file.name` khi file chọn)
3. Language: `<select>` — options: auto-detect (blank), en, ja, vi, ko
4. Group: `<select>` — options: No Group (blank) + groups từ props
5. Source URL: `<input type="url">` — optional, placeholder = `t('documents.upload_source_url_label')` (G4 — admin nhập link nguồn gốc document)

Submit logic (gọi `documentsApi.uploadDocument`):
- loading state: disable submit btn, show "Uploading..."
- success: gọi `onSuccess()` + `onClose()` (parent hiển thị toast)
- error 413: show inline error "File too large"
- error khác: show inline error "Upload failed. Please try again."

**Review criteria**
- [ ] `accept=".pdf,.docx,.html,.txt,.md"` — file input attribute đúng per D06
- [ ] Title auto-fill từ filename stem khi file chọn: `file.name.replace(/\.[^.]+$/, '')`
- [ ] Source URL field — optional `<input type="url">`, truyền vào `uploadDocument(..., sourceUrl)` (G4)
- [ ] Import `uploadDocument` từ `../api/documentsApi` — không duplicate logic
- [ ] Submit disabled khi không có file (file là required)
- [ ] Test: file selection auto-fills title, submit disabled without file, upload calls documentsApi với sourceUrl, error 413 shows correct message

**Test command**
```bash
cd frontend/admin-spa && npm run test -- tests/components/UploadModal.test.tsx
```

**Parallel:** after:T001 | **Size est.:** ~65 lines component + ~50 lines test

---

### T005: i18n S002 keys (4 locales) + App.tsx /documents route

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/src/i18n/locales/en.json` — modify (add `"documents"` key block)
- `frontend/admin-spa/src/i18n/locales/ja.json` — modify
- `frontend/admin-spa/src/i18n/locales/vi.json` — modify
- `frontend/admin-spa/src/i18n/locales/ko.json` — modify
- `frontend/admin-spa/src/App.tsx` — modify (add `/documents` route)

**Change description**
Thêm `"documents"` key block vào 4 locale files. Cấu trúc key block (en):
```json
"documents": {
  "page_title": "Documents",
  "upload_btn": "Upload Document",
  "filter_status": "Status", "filter_lang": "Language", "filter_group": "Group", "filter_all": "All",
  "col_title": "Title", "col_lang": "Language", "col_group": "Group",
  "col_status": "Status", "col_created_at": "Created At", "col_chunks": "Chunks", "col_actions": "Actions",
  "status_pending": "Pending", "status_processing": "Processing", "status_ready": "Ready", "status_error": "Error",
  "delete_btn": "Delete",
  "delete_confirm_title": "Delete Document",
  "delete_confirm_msg": "Delete document '{{title}}'? This cannot be undone.",
  "upload_modal_title": "Upload Document",
  "upload_file_label": "File", "upload_file_hint": "PDF, DOCX, HTML, TXT, MD",
  "upload_title_label": "Title (optional)",
  "upload_lang_label": "Language", "upload_lang_auto": "Auto-detect",
  "upload_group_label": "Group (optional)",
  "upload_submit": "Upload", "upload_cancel": "Cancel",
  "upload_success": "Document uploaded successfully",
  "upload_source_url_label": "Source URL (optional)",
  "no_group": "No Group", "empty": "No documents found.", "prev": "Previous", "next": "Next"
}
```

App.tsx: thêm import `DocumentsPage` + route `/documents` trong `<ProtectedRoute>`:
```typescript
import { DocumentsPage } from './pages/DocumentsPage'
// ...
<Route path="/documents" element={<ProtectedRoute><DocumentsPage /></ProtectedRoute>} />
```

Đặt route trước `/` catch-all redirect.

**Review criteria**
- [ ] `"documents"` key block có đủ tất cả keys (31 keys, bao gồm `upload_source_url_label`) trong tất cả 4 locales
- [ ] `"delete_confirm_msg"` dùng `{{title}}` interpolation (i18next syntax)
- [ ] App.tsx `/documents` route wrap trong `<ProtectedRoute>` — không public
- [ ] Nav sidebar (nếu có) hoặc DashboardPage có link đến `/documents` — verify ở T006

**Test command**
```bash
cd frontend/admin-spa && npm run test -- --reporter=verbose 2>&1 | grep -E "i18n|pass|fail"
```

**Parallel:** safe | **Size est.:** ~124 lines JSON tổng (4 files × ~31 keys) + ~5 lines App.tsx

---

### T006: DocumentsPage.tsx — orchestration + test

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/src/pages/DocumentsPage.tsx` — create
- `frontend/admin-spa/tests/pages/DocumentsPage.test.tsx` — create

**Change description**
Page component orchestrate: DocumentTable + UploadModal + DeleteConfirmDialog + pagination + filter.

State:
```typescript
const [documents, setDocuments] = useState<DocumentItem[]>([])
const [total, setTotal] = useState(0)
const [offset, setOffset] = useState(0)
const [filterStatus, setFilterStatus] = useState('')
const [filterLang, setFilterLang] = useState('')
const [filterGroup, setFilterGroup] = useState<number | undefined>()
const [uploadOpen, setUploadOpen] = useState(false)
const [deleteTarget, setDeleteTarget] = useState<{ id: string; title: string } | null>(null)
const [toastMessage, setToastMessage] = useState<string | null>(null)
const [groups, setGroups] = useState<{ id: number; name: string }[]>([])
```

`LIMIT = 20`. On mount + filter/offset change: gọi `listDocuments(...)`.
Groups: fetch `GET /v1/admin/groups` via `adminApi` (S003) — dùng `documentsApi`? Không — dùng inline fetch or `adminApi` từ S003 scope. **Cho S002**: fetch groups với `apiClient.get('/v1/admin/groups')` inline trong useEffect riêng — tránh import S003 chưa exist.

Delete flow: `onDelete(id, title)` → set `deleteTarget` → render `<DeleteConfirmDialog open>` → onConfirm gọi `deleteDocument(id)` → remove từ list + show toast → `setDeleteTarget(null)`.

Upload flow: `onSuccess` → `setToastMessage(t('documents.upload_success'))` → re-fetch list.

Toast: simple `<div class="toast toast-success">` hiển thị `toastMessage`, tự dismiss sau 3s (`setTimeout`).

Pagination: Prev/Next buttons — disabled khi `offset=0` (Prev) hoặc `offset+LIMIT >= total` (Next).

Filter bar: status + lang + group dropdowns — onChange reset offset về 0 và re-fetch.

**Review criteria**
- [ ] `listDocuments` được gọi khi filter/offset thay đổi — không chỉ mount
- [ ] Delete thực sự gọi `deleteDocument(id)` + remove từ state (không fetch lại toàn bộ)
- [ ] Toast tự dismiss sau 3s — `useEffect` cleanup `clearTimeout`
- [ ] Groups fetch dùng `apiClient.get('/v1/admin/groups')` — không import S003 module
- [ ] Pagination disabled states đúng: Prev disabled khi `offset===0`, Next disabled khi `offset+LIMIT>=total`
- [ ] Test: initial fetch, filter change re-fetches, delete flow (dialog → confirm → remove), upload success toast, pagination prev/next
- [ ] Rule R003: component wrapped trong ProtectedRoute tại App.tsx (T005) — admin-only access ✓

**Test command**
```bash
cd frontend/admin-spa && npm run test -- tests/pages/DocumentsPage.test.tsx
```

**Full test suite (all S002):**
```bash
cd frontend/admin-spa && npm run test -- tests/api/documentsApi.test.ts tests/components/DeleteConfirmDialog.test.tsx tests/components/DocumentTable.test.tsx tests/components/UploadModal.test.tsx tests/pages/DocumentsPage.test.tsx
```

**Parallel:** after:T001,T002,T003,T004,T005 | **Size est.:** ~80 lines page + ~70 lines test

---

## Summary

| # | Tasks | Scope |
|---|-------|-------|
| ACs covered | AC1–AC10 (all S002 ACs) | |
| New files | 10 (5 src + 5 tests) | |
| Modified files | 5 (4 locales + App.tsx) | |
| Spec corrections | AC4 (file input + source_url per D06/G4), AC5 (`/v1/documents/upload` per D06) | |
| Backend gaps | G1 auth gate, G2 key mismatch, G3 filter missing, G4 source_url — patch trước implement | |
| Rules checked | R003 (auth on route ✓), S003 (input sanitize via accept attr ✓), A002 (no reverse deps ✓) |

```
/tasks complete
  Story: S002 | Feature: admin-spa
  Tasks created: 6
  File: docs/admin-spa/tasks/S002.tasks.md
  Parallel groups: G1[T001,T002,T003,T005], G2[T004], G3[T006]
  Est. total tokens to implement: ~5.5k
  Next: /analyze T001
```
