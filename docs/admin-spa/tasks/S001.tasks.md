# Tasks: admin-spa / S001 — Admin Login + Admin Gate
Created: 2026-04-17 | Agent: frontend-agent | Status: TODO

---

## LAYER 1 — Story Task Summary

| Field | Value |
|-------|-------|
| Story | S001: Admin Login + Admin Gate |
| Total tasks | 8 |
| Parallel groups | G1: [T001], G2: [T002,T003,T004], G3: [T005,T006,T007], G4: [T008] |
| Critical path | T001 → T002 → T005 → T008 |
| Agent | frontend-agent |
| Est. sessions | 1 |

### Task Status Board
| Task | Title | Status | Parallel | Blocks |
|------|-------|--------|----------|--------|
| T001 | Project scaffold (package.json, vite, tsconfig, main) | DONE | safe | T002, T003, T004 |
| T002 | authStore.ts — token + is_admin + login/logout | DONE | after:T001 | T005, T006 |
| T003 | api/client.ts — Axios + Bearer interceptor + 401 | DONE | after:T001 | T005 |
| T004 | i18n setup — 4 locales + admin keys | DONE | after:T001 | T005, T007 |
| T005 | LoginForm.tsx + LoginPage.tsx — admin gate (AC1–AC4, AC9) | DONE | after:T002,T003,T004 | T008 |
| T006 | ProtectedRoute.tsx + useAdminGuard.ts (AC7, AC8) | DONE | after:T002 | T008 |
| T007 | LanguageSelector.tsx (AC10) | DONE | after:T004 | T008 |
| T008 | App.tsx — router + header + /login + /dashboard (AC5, AC6) | DONE | after:T005,T006,T007 | — |

---

## LAYER 2 — Task Detail

---

### T001: Project scaffold

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/package.json` — create
- `frontend/admin-spa/vite.config.ts` — create
- `frontend/admin-spa/tsconfig.json` — create
- `frontend/admin-spa/index.html` — create
- `frontend/admin-spa/src/main.tsx` — create
- `frontend/admin-spa/tests/setup.ts` — create

**Change description**
Tạo Vite project mới tại `frontend/admin-spa/` — hoàn toàn tách biệt với `frontend/` (D07).
Reuse exact same dependency versions từ `frontend/package.json`:
- Runtime: `axios ^1.7.2`, `i18next ^23.11.5`, `i18next-browser-languagedetector ^8.0.0`, `react ^18.3.1`, `react-dom ^18.3.1`, `react-i18next ^14.1.2`, `react-router-dom ^6.23.1`, `zustand ^4.5.2`
- Dev: `vitest ^1.6.0`, `@testing-library/react ^16`, `@testing-library/user-event ^14`, `@testing-library/jest-dom ^6`, `axios-mock-adapter ^1.22.0`, `jsdom ^24`, `typescript ^5.4.5`, `vite ^5.3.1`, `@vitejs/plugin-react ^4.3.1`

`vite.config.ts` giống `frontend/vite.config.ts` — test environment jsdom, setupFiles `./tests/setup.ts`.
`main.tsx` minimal: `<BrowserRouter><App /></BrowserRouter>` wrap với i18n init call.
`tests/setup.ts` import `@testing-library/jest-dom`.

**Review criteria**
- [ ] `name` trong package.json là `"knowledge-hub-admin-spa"` (khác với frontend-spa)
- [ ] `vite.config.ts` có `test.environment: 'jsdom'` và `test.globals: true`
- [ ] Không duplicate `frontend/` — đây là Vite project riêng biệt trong `frontend/admin-spa/`
- [ ] `tests/setup.ts` import `@testing-library/jest-dom` đúng

**Test command**
```bash
cd frontend/admin-spa && npm install && npm run test
```

**Parallel:** safe | **Size est.:** ~80 lines tổng

---

### T002: authStore.ts — token + is_admin + login/logout

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/src/store/authStore.ts` — create
- `frontend/admin-spa/tests/store/authStore.test.ts` — create

**Change description**
Zustand store cho admin-spa. Extend pattern từ `frontend/src/store/authStore.ts` nhưng thêm `is_admin: boolean` field.

```typescript
interface AuthState {
  token: string | null
  username: string | null
  isAdmin: boolean
  _refreshTimer: ReturnType<typeof setTimeout> | null
  login: (token: string, username: string, isAdmin: boolean) => void
  logout: () => void
  scheduleRefresh: (expSeconds: number, refreshFn: () => void) => void
}
```

Key difference vs frontend-spa: `login()` nhận `isAdmin: boolean` param — set từ `/v1/auth/token` response `is_admin` field.
`logout()` xóa token + isAdmin + cancel timer (không cần clearHistory vì admin-spa không có queryStore).

**Review criteria**
- [ ] `isAdmin` default `false` — không bao giờ default true
- [ ] `login()` signature: `(token, username, isAdmin)` — nhận isAdmin từ caller, không tự compute
- [ ] `logout()` set `{ token: null, username: null, isAdmin: false, _refreshTimer: null }`
- [ ] `scheduleRefresh()` logic giống frontend-spa (5 min before exp)
- [ ] Test: login sets isAdmin=true/false, logout clears isAdmin

**Test command**
```bash
cd frontend/admin-spa && npm run test -- tests/store/authStore.test.ts
```

**Parallel:** after:T001 | **Size est.:** ~55 lines store + ~40 lines test

---

### T003: api/client.ts — Axios + Bearer interceptor + 401 handler

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/src/api/client.ts` — create
- `frontend/admin-spa/tests/api/client.test.ts` — create

**Change description**
Copy pattern từ `frontend/src/api/client.ts` với điều chỉnh:
- `apiClient` dùng `import.meta.env.VITE_API_BASE_URL` (không hardcode — S005)
- Request interceptor: attach `Authorization: Bearer <token>` từ authStore
- Response interceptor: 401 → `authStore.logout()` + `_navigate('/login')` + set toast message "Session expired" (AC6)

Toast mechanism: authStore thêm field `sessionExpiredMessage: string | null` — set "Session expired" khi 401, clear khi user dismiss hoặc login lại.

```typescript
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      useAuthStore.getState().setSessionExpired()  // sets message
      useAuthStore.getState().logout()
      if (_navigate) _navigate('/login')
    }
    return Promise.reject(error)
  },
)
```

> ⚠️ authStore cần thêm `setSessionExpired()` action và `sessionExpiredMessage: string | null` field — T002 phải include này.

**Review criteria**
- [ ] `baseURL` từ `import.meta.env.VITE_API_BASE_URL` — không hardcode URL (S005)
- [ ] 401 interceptor: logout() + navigate('/login') + setSessionExpired()
- [ ] `setNavigate` export function — gọi từ App.tsx useEffect (pattern từ frontend-spa)
- [ ] Test: mock axios, verify 401 triggers logout + navigate

**Test command**
```bash
cd frontend/admin-spa && npm run test -- tests/api/client.test.ts
```

**Parallel:** after:T001 | **Size est.:** ~45 lines client + ~50 lines test

---

### T004: i18n setup — 4 locales + admin-specific keys

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/src/i18n/index.ts` — create
- `frontend/admin-spa/src/i18n/locales/en.json` — create
- `frontend/admin-spa/src/i18n/locales/ja.json` — create
- `frontend/admin-spa/src/i18n/locales/vi.json` — create
- `frontend/admin-spa/src/i18n/locales/ko.json` — create
- `frontend/admin-spa/tests/i18n/i18n.test.ts` — create

**Change description**
Reuse pattern từ `frontend/src/i18n/index.ts`. Keys bao gồm (thêm keys admin-specific):

```json
// en.json (template — tất cả 4 locale cần đủ keys)
{
  "login": {
    "title": "Admin Panel",
    "username": "Username",
    "password": "Password",
    "submit": "Sign In",
    "error_invalid": "Invalid credentials",
    "error_unavailable": "Service unavailable. Please try again.",
    "access_denied": "Access denied. Admin privileges required.",
    "session_expired": "Session expired. Please sign in again."
  },
  "lang": {
    "selector_label": "Language",
    "en": "English",
    "ja": "日本語",
    "vi": "Tiếng Việt",
    "ko": "한국어"
  },
  "nav": {
    "dashboard": "Dashboard",
    "documents": "Documents",
    "users_groups": "Users & Groups",
    "logout": "Sign Out"
  }
}
```

**Review criteria**
- [ ] Tất cả 4 locale files có đủ tất cả keys (không thiếu)
- [ ] `fallbackLng: 'en'` trong i18n init
- [ ] `detection.caches: ['localStorage']` — persist (D03)
- [ ] `detection.lookupLocalStorage: 'admin-spa-lang'` — key khác `frontend/` (tránh collision)
- [ ] Test: import i18n → `t('login.access_denied')` trả về đúng string

**Test command**
```bash
cd frontend/admin-spa && npm run test -- tests/i18n/i18n.test.ts
```

**Parallel:** after:T001 | **Size est.:** ~25 lines index.ts + ~20 lines × 4 locales + ~20 lines test

---

### T005: LoginForm.tsx + LoginPage.tsx — admin gate (AC1–AC4, AC9)

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/src/components/auth/LoginForm.tsx` — create
- `frontend/admin-spa/src/pages/LoginPage.tsx` — create
- `frontend/admin-spa/tests/pages/LoginPage.test.tsx` — create

**Change description**
`LoginForm` extend từ `frontend/src/components/auth/LoginForm.tsx` với sự khác biệt quan trọng:

```typescript
// TokenResponse thêm is_admin field (AC14 từ S000)
interface TokenResponse {
  access_token: string
  token_type: string
  expires_in: number
  is_admin: boolean   // ← NEW
}

// Sau khi nhận response:
const { access_token, expires_in, is_admin } = data
login(access_token, username, is_admin)  // store is_admin

if (!is_admin) {
  // AC4: hiển thị access denied message, KHÔNG navigate
  setError(t('login.access_denied'))
  return
}
// AC5: is_admin=true → navigate('/dashboard')
navigate('/dashboard')
```

`LoginPage` wrapper giống `frontend/src/pages/LoginPage.tsx`:
- Brand: "Admin Panel" / "BRYSEN GROUP"
- Hiển thị `sessionExpiredMessage` từ authStore nếu có (AC6 message khi redirect từ 401)

**Review criteria**
- [ ] `is_admin=false` → setError(t('login.access_denied')) + KHÔNG navigate (AC4)
- [ ] `is_admin=true` → navigate('/dashboard') (AC5)
- [ ] 401 response → `t('login.error_invalid')` — không expose backend detail (AC9)
- [ ] Non-401 error → `t('login.error_unavailable')` (AC9)
- [ ] `sessionExpiredMessage` hiển thị khi redirect do 401 expiry (AC6)
- [ ] Test: submit với is_admin=true → navigate, is_admin=false → error message shown

**Test command**
```bash
cd frontend/admin-spa && npm run test -- tests/pages/LoginPage.test.tsx
```

**Parallel:** after:T002,T003,T004 | **Size est.:** ~70 lines LoginForm + ~20 lines LoginPage + ~80 lines test

---

### T006: ProtectedRoute.tsx + useAdminGuard.ts (AC7, AC8)

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/src/components/auth/ProtectedRoute.tsx` — create
- `frontend/admin-spa/src/hooks/useAdminGuard.ts` — create
- `frontend/admin-spa/tests/components/ProtectedRoute.test.tsx` — create

**Change description**
`ProtectedRoute` extend từ `frontend/src/components/auth/ProtectedRoute.tsx` — thêm check `isAdmin`:

```typescript
// ProtectedRoute.tsx
export function ProtectedRoute({ children }: Props) {
  const { token, isAdmin } = useAuthStore()
  if (!token || !isAdmin) {
    return <Navigate to="/login" replace />
  }
  return <>{children}</>
}
```

`useAdminGuard` hook — imperative version dùng trong pages:
```typescript
// useAdminGuard.ts
export function useAdminGuard() {
  const { token, isAdmin } = useAuthStore()
  const navigate = useNavigate()
  useEffect(() => {
    if (!token || !isAdmin) {
      navigate('/login', { replace: true })
    }
  }, [token, isAdmin, navigate])
}
```

**Review criteria**
- [ ] `ProtectedRoute` check cả `!token` VÀ `!isAdmin` — không chỉ token (AC8)
- [ ] Redirect sang `/login` với `replace` (không tạo history entry)
- [ ] `useAdminGuard` dùng `useEffect` — không gọi navigate trong render body
- [ ] Test: token=null → redirect, token set + isAdmin=false → redirect, token + isAdmin=true → render children

**Test command**
```bash
cd frontend/admin-spa && npm run test -- tests/components/ProtectedRoute.test.tsx
```

**Parallel:** after:T002 | **Size est.:** ~25 lines ProtectedRoute + ~20 lines hook + ~50 lines test

---

### T007: LanguageSelector.tsx (AC10)

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/src/components/LanguageSelector.tsx` — create
- `frontend/admin-spa/tests/components/LanguageSelector.test.tsx` — create

**Change description**
Language picker ja/en/vi/ko — reuse pattern từ `frontend/src/components/query/LanguageSelector.tsx`.
Dùng `useTranslation` để lấy `t('lang.selector_label')` và tên ngôn ngữ.
`onChange` gọi `i18n.changeLanguage(code)` — auto-persist qua `i18next-browser-languagedetector` localStorage.

```typescript
const LANGS = [
  { code: 'en', label: 'lang.en' },
  { code: 'ja', label: 'lang.ja' },
  { code: 'vi', label: 'lang.vi' },
  { code: 'ko', label: 'lang.ko' },
]

export function LanguageSelector() {
  const { i18n } = useTranslation()
  return (
    <select
      aria-label={t('lang.selector_label')}
      value={i18n.language}
      onChange={(e) => i18n.changeLanguage(e.target.value)}
      className="lang-selector"
    >
      {LANGS.map(({ code, label }) => (
        <option key={code} value={code}>{t(label)}</option>
      ))}
    </select>
  )
}
```

**Review criteria**
- [ ] localStorage key `'admin-spa-lang'` — dùng detector từ T004 (không call localStorage trực tiếp ở đây)
- [ ] `aria-label` dùng i18n key — không hardcode string
- [ ] Test: render 4 options, change language triggers i18n.changeLanguage

**Test command**
```bash
cd frontend/admin-spa && npm run test -- tests/components/LanguageSelector.test.tsx
```

**Parallel:** after:T004 | **Size est.:** ~30 lines component + ~30 lines test

---

### T008: App.tsx — router + header + /login + /dashboard routes (AC5, AC6, AC7, AC8)

**Status:** TODO | **Agent:** frontend-agent

**File(s)**
- `frontend/admin-spa/src/App.tsx` — create
- `frontend/admin-spa/src/pages/DashboardPage.tsx` — create (placeholder cho S004)
- `frontend/admin-spa/tests/App.test.tsx` — create

**Change description**
`App.tsx` — router setup + header:

```typescript
// Routes:
// /login → LoginPage (public)
// /dashboard → ProtectedRoute → DashboardPage (admin only)
// / → Navigate to /login
// * → Navigate to /login

function App() {
  const navigate = useNavigate()
  const { token, username, isAdmin } = useAuthStore()

  useEffect(() => { setNavigate(navigate) }, [navigate])

  return (
    <>
      <header className="app-header">
        <div className="logo">
          <div className="logo-icon">✦</div>
          <div className="logo-text">
            <span className="logo-title">Admin Panel</span>
            <span className="logo-sub">BRYSEN GROUP</span>
          </div>
        </div>
        <LanguageSelector />
        {token && (
          <>
            <div className="header-divider" />
            <div className="user-pill">
              <div className="avatar">{username?.charAt(0).toUpperCase() ?? '?'}</div>
              <span className="user-name">{username ?? ''}</span>
            </div>
            <button onClick={() => useAuthStore.getState().logout()} className="logout-btn">
              {t('nav.logout')}
            </button>
          </>
        )}
      </header>
      <Routes>
        <Route path="/login" element={<LoginPage />} />
        <Route path="/dashboard" element={
          <ProtectedRoute><DashboardPage /></ProtectedRoute>
        } />
        <Route path="/" element={<Navigate to="/login" replace />} />
        <Route path="*" element={<Navigate to="/login" replace />} />
      </Routes>
    </>
  )
}
```

`DashboardPage.tsx` — placeholder: render `<h1>Dashboard</h1>` + nav links (S002–S004 được thêm sau). Dùng `useAdminGuard()` trong component.

**Review criteria**
- [ ] `/` và `*` → `/login` redirect (AC8 — mọi route khi chưa login đều về /login)
- [ ] `ProtectedRoute` wrap `/dashboard` — không thể access trực tiếp nếu !token || !isAdmin
- [ ] Logout button visible khi token != null → gọi `logout()` → AC7
- [ ] `setNavigate(navigate)` trong useEffect — đảm bảo 401 interceptor có navigate function
- [ ] Test: unauthenticated → redirect /login, authenticated admin → render dashboard

**Test command**
```bash
cd frontend/admin-spa && npm run test -- tests/App.test.tsx
```

**Parallel:** after:T005,T006,T007 | **Size est.:** ~70 lines App + ~15 lines DashboardPage + ~80 lines test

---

## Implementation Notes

### Reuse từ frontend-spa
Pattern copy (không symlink — D07: separate project):
- authStore.ts pattern → extend với `isAdmin`
- api/client.ts pattern → extend với `setSessionExpired`
- i18n/index.ts pattern → khác localStorage key (`admin-spa-lang` vs `i18nextLng`)
- LoginForm.tsx pattern → thêm `is_admin` check sau login
- ProtectedRoute.tsx pattern → thêm `isAdmin` check
- vite.config.ts → copy nguyên

### Key diffs vs frontend-spa
| Item | frontend-spa | admin-spa |
|------|-------------|-----------|
| Token store | `token + username` | `token + username + isAdmin` |
| Login gate | token valid → navigate('/query') | token valid + isAdmin=true → navigate('/dashboard') |
| isAdmin=false | N/A | show "Access denied" — no navigate |
| i18n localStorage key | `i18nextLng` (default) | `admin-spa-lang` |
| 401 response | logout + navigate | logout + navigate + setSessionExpired |
| Protected route | check token only | check token AND isAdmin |

### CSS
Admin-spa có CSS riêng. T001 tạo `frontend/admin-spa/src/index.css` với admin color scheme.
Không import từ `frontend/` — separate project.
